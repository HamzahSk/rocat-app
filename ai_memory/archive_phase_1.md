# ARSIP MEMORI — Phase 1 (Tahap 2–11.5)

Ringkasan 10 log mikro pertama (sesuai protokol auto-archive `memory_prompt.md`). Detail lengkap sudah dihapus dari `ai_memory/` agar folder ringan; file asli diarsip sesuai isi ringkas di bawah, kronologis.

1. **Tahap 2 & 3 — Script Infrastruktur & UI (2026-08-07)** — Metadata parser `==UserScript==` + fallback `// @tag`, fetch bridge Rhino (Response sync: status/ok/headers/body/text()/json()), storage file-based JSON + CRUD (ScriptRepositoryImpl, `scripts.json`), UI Mihon-style list/detail/import/playground. `:app:assembleDebug` + test hijau.
2. **Tahap 4 — Stabilisasi Crash & Import (2026-08-08)** — Fix crash ViewModel factory (Injekt) `AppViewModelFactory`, abs/`StateViewModel`, kerja I/O pindah `Dispatchers.IO`, validasi URL import, error handling Rhino engine anti force-close.
3. **Tahap 5 — Global Crash Handler (2026-08-08)** — `CrashHandler` (default uncaught handler) menulis report ke `crash_logs/` via `CrashLogStore` → `CrashActivity` (stack trace scrollable + copy + info path) → kill process. Terdaftar di manifest.
4. **Tahap 6 — Fix Compose SaveableStateRegistry (2026-08-08)** — Crush `rememberSaveable { mutableStateListOf(...) }` (SnapshotStateList tidak bisa disimpan ke Bundle) → `<listSaver>(save={toList()}, restore={toMutableStateList()})` untuk backstack di `RoCatNav.kt`.
5. **Tahap 7 — Network & SSL + Script Loader (2026-08-08)** — `network_security_config.xml` (cleartext + trust system/user) dipasang di manifest (fix `CertPathValidatorException`); `NetworkHelper` ala Mihon (UA browser, TLS modern, redirect, timeout 30s); `ScriptSourceFetcher.normalizeUrl()` (inject `https://`, rewrite GitHub blob→raw, `Dispatchers.IO`); `ImportScriptViewModel` error mapping friendly + contoh skrip Rhino-compatible.
6. **Tahap 8 — Script Execution API + RoCatDOM (2026-08-08)** — JsoupBridge global `RoCatDOM` (parse/select/selectText/selectAttr/selectHtml/has + metode elemen); `invokeFunction` (`search`/`detail` → `NativeJSON.stringify`); Playground "Test Execution" (Run Search/Run Detail); contoh MangaUpdates sync; fix `fetch` ConsString → `Context.toString`. 22 unit test hijau.
7. **Tahap 9 — Fix Rhino ClassLoader (2026-08-08)** — Rhino wajib mode interpretasi (`optimizationLevel = -1` di `ScriptContextFactory`), error Playground masuk area Result/log (bukan teks melayang), try-catch `Throwable` anti force-close, `valueToString()` normalisasi `42.0`→`42`. rhino 13/13 test hijau.
8. **Tahap 10 — Stealth Browser & Cloudflare (2026-08-08)** — `AndroidCookieJar` (WebView CookieManager sync), `CloudflareInterceptor` (headless WebView bypass Turnstile → `cf_clearance` + retry + destroy), `StealthHeadersInterceptor`; `NetworkHelper(Context)` pasang semuanya; `newScriptClient()` mewarisi stack penuh.
9. **Tahap 11 — UI Polish & Playground Dinamis (2026-08-08)** — `testArgs: List<TestArg>` dinamis (tambah/hapus Key+Value), Function Selector + nama kustom, `CopyableResultCard` Copy JSON/Text (clipboard+Toast), `StatusChip` Active/Inactive, konfirmasi dialog hapus, import padding.
10. **Tahap 11.5 — Hotfix Playground (2026-08-08)** — hapus card "Run main", auto-detect fungsi (regex) untuk dropdown, `testArgs` default kosong (input hanya muncul setelah + Add Input), `ResultFormatter.prettyJson` benar-benar pretty-print 2-spasi, hasil dibungkus `SelectionContainer`.