# Fase 39: Sinkronisasi Coil3 dan Loading Preview

- **Status:** Selesai
- **Ringkasan Perubahan:** `RoApp` mengimplementasikan `SingletonImageLoader.Factory` dan membangun Coil dengan `OkHttpNetworkFetcherFactory` yang mengambil `NetworkHelper.client()` secara lazy. Request Coil kini berbagi cookie, UA, stealth headers, DoH, dan Cloudflare interceptor dengan downloader/script. Cache key grid memakai `(imageUrl, headers)`; state loading/success/error preview aktif.
- **Verifikasi:** `bash ./gradlew assembleDebug` berhasil. `spotlessApply` tidak tersedia.
- **Tugas Selanjutnya:** Uji runtime pada URL yang memerlukan Referer/anti-bot di emulator.
