# Fase 40: Decode Gambar Panjang & Memori Reader

- **Status:** Selesai
- **Ringkasan Perubahan:** `ImagePreviewCard` kini memakai `rememberConstraintsSizeResolver`, `Precision.INEXACT`, dan software bitmap melalui `allowHardware(false)`. Decode mengikuti batas layout Compose, berjalan melalui pipeline background Coil, dan tidak lagi mengirim bitmap webtoon sangat tinggi sebagai hardware texture yang dapat melewati limit OpenGL. `RoApp` membatasi singleton memory cache Coil maksimal 25% RAM serta disk cache gambar 100 MiB untuk mengurangi tekanan memori pada reader dan perangkat low-end. LazyColumn tetap membuang composable off-screen secara native.
- **Verifikasi:** `bash ./gradlew assembleDebug` sukses. `spotlessApply` tidak tersedia di project.
- **Tugas Selanjutnya:** Uji runtime dengan beberapa chapter webtoon ekstrem pada perangkat low-end; bila sumber tertentu masih mengirim satu bitmap melebihi batas decoder platform, tambahkan decoder region/subsampling khusus sebagai tahap lanjutan.
