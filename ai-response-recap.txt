Reading additional input from stdin...
OpenAI Codex v0.149.1
--------
workdir: /home/runner/work/rocat-app/rocat-app
model: gpt-5.6-sol
provider: agentrouter
approval: never
sandbox: danger-full-access
reasoning effort: none
reasoning summaries: none
session id: 01a03d0e-3338-72e2-b74b-74c6afe41468
--------
user
eksekusi perintah dari file prompt.md
codex
Saya akan membaca `prompt.md`, meninjau perintahnya, lalu menjalankannya dengan urutan yang aman dan melaporkan hasilnya.
exec
/bin/bash -lc "sed -n '1,240p' prompt.md" in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:

### Prompt Fase 42: Network Interception & XHR Capture pada Headless WebView
**Role & Objective**
Kamu adalah **Senior Android Engineer** untuk RoCat. Kita masuk ke **Tahap 42: Network Interception pada Headless WebView**. Tujuannya adalah membuat objek page mampu menangkap *response* JSON/XHR dari *background request* situs SPA.
**Mandatory Tasks (WAJIB PERTAMA)**
 * **Pembaruan Memori:** Buat task_YYYYMMDD_HHMM_Fase_42.md di ai_memory/ berisi log implementasi *Network Interception* ini. Update 00_INDEX.md.
 * **Code Quality:** Pastikan *build* sukses dengan bash ./gradlew assembleDebug.
**Analisis Masalah**
Situs seperti SaveKit memuat tombol unduhan dari *request* JSON terpisah pasca-klik. DOM parser sering gagal karena telat atau elemen disembunyikan di Shadow DOM. Solusi terbaik adalah mencegat langsung *response* fetch/XHR di dalam WebView, lalu mengeksposnya ke *scraper* via API baru page.waitForResponse(). Karena WebViewClient.shouldInterceptRequest tidak bisa membaca *body response* secara *native*, kita harus menggunakan teknik *JS Monkey-patching*.
**Execution Plan (Maks 1 Jam):**
 1. **Injeksi Interceptor JS (HeadlessWebViewManager.kt):**
   * Buat *string* JS *polyfill* yang meng-*override* window.fetch dan XMLHttpRequest.prototype.open/send.
   * Setiap kali *request* selesai, kirim URL dan *body response* (JSON/Text) ke Kotlin melalui fungsi bridge baru, misalnya RoCatBrowserBridge.onNetworkResponse(url, body).
   * Suntikkan JS ini menggunakan evaluateJavascript tepat setelah WebView selesai inisialisasi atau pada onPageStarted.
 2. **State Management di Kotlin:**
   * Tambahkan koleksi thread-safe (misal ConcurrentHashMap<String, String>) di HeadlessWebViewManager untuk menyimpan *intercepted responses* berdasarkan URL.
 3. **Ekspansi API Browserless (page):**
   * Tambahkan fungsi page.waitForResponse(urlPattern, timeout) di JS Polyfill (RoCatBrowserWrapper.js).
   * Fungsi ini akan melakukan *polling* ke bridge *native* untuk mengecek apakah ada *intercepted response* yang URL-nya cocok dengan urlPattern.
   * Jika cocok, kembalikan *body response* tersebut sebagai *string* ke skrip pengguna.
**Constraints:**
 * Pastikan *monkey-patching* tidak merusak fungsi asli fetch dan XHR dari situs target.
 * Batasi ukuran *body* yang disimpan di memori agar tidak OOM (misal, hanya simpan *response* dengan tipe application/json atau di bawah 1MB).
2026-08-26T07:52:36.704177Z ERROR codex_core::tools::router: error=request_user_input is unavailable in Default mode
exec
/bin/bash -lc "rg --files | sed -n '1,120p'; printf '\\n--- status ---\\n'; git status --short" in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:
ai_memory/task_20260808_0507_tahap6_fix_saveable_state_registry.md
ai_memory/task_20260817_0557_tahap27_emulator_js_verification.md
ai_memory/task_20260808_1201_tahap8_script_api_dan_ui_execution.md
ai_memory/task_20260818_tahap33_headless_spa_fullpage.md
ai_memory/task_20260808_1503_tahap11_ui_improvements_and_dynamic_playground.md
ai_memory/task_20260818_tahap34_ui_overhaul.md
ai_memory/00_INDEX.md
ai_memory/task_20260809_1301_tahap22_2_script_canvas_templates.md
ai_memory/task_20260808_0436_tahap5_global_crash_handler.md
ai_memory/task_20260809_0331_tahap16_bugfixes_and_browser.md
ai_memory/task_20260817_0313_tahap25_browser_revamp_and_js_engine.md
ai_memory/task_20260808_0313_tahap4_stabilisasi_dan_perbaikan_script.md
ai_memory/task_20260809_0759_tahap20_network_settings.md
ai_memory/task_20260818_tahap35_settings_system.md
ai_memory/task_20260817_0429_tahap26_webview_js_critical_fix.md
ai_memory/task_20260809_1405_tahap23_docs_update_and_script_fix.md
ai_memory/archive_phase_1.md
ai_memory/task_20260808_1310_tahap9_fix_rhino_class_loader.md
ai_memory/task_20260809_0213_tahap15_localization_storage_db.md
ai_memory/task_20260817_1035_tahap29_puppeteer_engine.md
ai_memory/task_20260809_0717_tahap19_anichin_scraper_end_to_end.md
ai_memory/task_20260826_0000_Fase_41.md
ai_memory/task_20260817_0952_tahap28_sweb_js_refactor.md
ai_memory/task_20260808_2333_tahap13_full_script_canvas_and_grid.md
ai_memory/task_20260808_0540_tahap7_network_ssl_dan_script_loader.md
ai_memory/task_20260809_0604_tahap18_media_downloader_hls_stream.md
ai_memory/task_20260825_0000_Fase_40.md
ai_memory/task_20260816_0000_tahap24_media_headers_modern_ui.md
ai_memory/task_20260808_1404_tahap10_stealth_browser_and_cloudflare.md
ai_memory/task_20260808_2223_tahap12_script_driven_ui.md
ai_memory/task_20260808_1620_tahap12_media_renderer_and_smart_inputs.md
ai_memory/task_20260818_0000_tahap31_ui_media_fixes.md
ai_memory/evidence/webview_js_tahap28.log
ai_memory/task_20260824_1557_Fase_38.md
ai_memory/task_20260818_1200_tahap32_webview_sweb_integration.md
ai_memory/task_20260809_0456_tahap17_ui_and_storage_fixes.md
ai_memory/task_20260817_1428_tahap30_puppeteer_click_fix_v2.md
ai_memory/task_20260808_1549_tahap11_5_hotfix_playground.md
ai_memory/task_20260824_1615_Fase_39.md
ai_memory/task_20260807_0440_tahap2_3_script_infrastruktur_dan_ui.md
ai_memory/task_20260809_0005_tahap14_production_readiness.md
ai_memory/task_20260817_1145_tahap30_puppeteer_click_fix.md
test_browserless.js
data/build.gradle.kts
scripting/api/build.gradle.kts
data/src/test/java/app/rocat/data/script/ScriptSourceFetcherTest.kt
scripting/api/src/main/java/app/rocat/scripting/api/ScriptUiBridge.kt
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
scripting/api/src/main/java/app/rocat/scripting/api/network/ScriptFetch.kt
scripting/api/src/main/java/app/rocat/scripting/api/model/DefaultScriptEnvironment.kt
scripting/api/src/main/java/app/rocat/scripting/api/model/ScriptSerializers.kt
scripting/api/src/main/java/app/rocat/scripting/api/model/Script.kt
scripting/api/src/main/java/app/rocat/scripting/api/MediaHeaders.kt
scripting/api/src/main/java/app/rocat/scripting/api/ScriptEngine.kt
scripting/api/src/main/java/app/rocat/scripting/api/ScriptSettingsBridge.kt
scripting/rhino/build.gradle.kts
core/viewmodel/build.gradle.kts
data/src/main/java/app/rocat/data/script/ScriptSourceFetcher.kt
data/src/main/java/app/rocat/data/script/ScriptRepositoryImpl.kt
settings.gradle.kts
templates/komik-reader-template.js
prompt.md
gradle.properties
gradlew.bat
deleted_files.txt
tiktok-downloader-savekit-screenshot.js
ai-response-recap.txt
domain/build.gradle.kts
core/viewmodel/src/main/java/app/rocat/core/viewmodel/StateViewModel.kt
core/common/build.gradle.kts
scripting/rhino/src/test/java/app/rocat/scripting/rhino/FixedTestscrapeScraperTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatScriptTemplateTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatTahap35Test.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/CapCutNativeClickTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/TestBrowserlessScraperTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RhinoScriptEngineTest.kt
scripting/rhino/src/test/java/app/rocat/scripting/rhino/AnichinScraperTest.kt
domain/src/test/java/app/rocat/domain/script/ScriptMetadataParserTest.kt
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatCoreWrapper.kt
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RhinoScriptEngine.kt
scripting/rhino/src/main/java/app/rocat/scripting/rhino/JsoupBridge.kt
build.gradle.kts
memory_prompt.md
core/common/src/main/java/app/rocat/core/common/util/WebViewUtil.kt
core/common/src/main/java/app/rocat/core/common/util/Json.kt
domain/src/main/java/app/rocat/domain/script/ScriptSetting.kt
domain/src/main/java/app/rocat/domain/script/ScriptMetadata.kt
gradle/wrapper/gradle-wrapper.jar
gradle/wrapper/gradle-wrapper.properties
gradle/libs.versions.toml
LICENSE
gradlew
scrape_anichin.js
Cgbum.js
capcut_test.js
README.md
domain/src/main/java/app/rocat/domain/script/interactor/DeleteScript.kt
domain/src/main/java/app/rocat/domain/script/interactor/ImportScript.kt
domain/src/main/java/app/rocat/domain/script/interactor/GetScripts.kt
domain/src/main/java/app/rocat/domain/script/interactor/ExecuteScript.kt
domain/src/main/java/app/rocat/domain/script/interactor/UpsertScript.kt
domain/src/main/java/app/rocat/domain/script/interactor/SetScriptEnabled.kt
domain/src/main/java/app/rocat/domain/script/ScriptMetadataParser.kt
domain/src/main/java/app/rocat/domain/script/ScriptRepository.kt
core/common/src/main/java/app/rocat/core/common/injekt/Injekt.kt
DOCS_SCRIPTING.md
core/common/src/main/java/app/rocat/core/common/network/NetworkHelper.kt
core/common/src/main/java/app/rocat/core/common/network/OkHttpExtensions.kt
core/common/src/main/java/app/rocat/core/common/network/HttpException.kt
core/common/src/main/java/app/rocat/core/common/network/AndroidCookieJar.kt
core/common/src/main/java/app/rocat/core/common/network/Requests.kt
core/common/src/main/java/app/rocat/core/common/network/DoHResolver.kt
core/common/src/main/java/app/rocat/core/common/network/DnsProvider.kt
app/build.gradle.kts
app/proguard-rules.pro
core/common/src/main/java/app/rocat/core/common/network/interceptor/CloudflareInterceptor.kt

--- status ---
 M ai-response-recap.txt

codex
Instruksinya meminta perubahan implementasi, pengujian, dan catatan memori. Saya sedang memetakan bridge WebView dan wrapper `page` yang ada agar API baru mengikuti pola proyek dan tidak mengganggu perubahan pengguna yang sudah ada.
exec
/bin/bash -lc "rg -n \"class HeadlessWebViewManager|HeadlessWebViewManager|JavascriptInterface|RoCatBrowserBridge|waitForResponse|waitFor\" app scripting -g '*.kt' -g '*.js'; sed -n '1,260p' scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt; sed -n '1,260p' ai_memory/00_INDEX.md; sed -n '1,220p' ai_memory/task_20260826_0000_Fase_41.md" in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:17: * [waitForSelector]/[evaluate] only when the target genuinely needs a live browser
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:61:    fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:84:    // RoCatBrowserBridge overrides the ones the hidden WebView can really do.
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:90:     * sleeps, which is cheap and lets scripts implement `waitForTimeout`.
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:121:    fun waitForLoad(state: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean = false
scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt:170:        /** Default per-call timeout used by `open` / `waitForSelector`. */
scripting/rhino/src/test/java/app/rocat/scripting/rhino/FixedTestscrapeScraperTest.kt:305:            override fun waitForSelector(selector: String, timeoutMs: Long): Boolean = true
app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt:41:class HeadlessWebViewManager(private val appContext: Context) {
app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt:402:    fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt:484:    fun waitForLoad(state: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:23: *     `waitFor`, `getAttribute`) drive `RoCatPage.evaluate` with serialised args.
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:61:        override fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:126:        override fun waitForLoad(state: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:127:            calls += "waitForLoad:$state:$timeoutMs"
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:288:                var waited = page.waitForSelector(".profile", 5000);
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:289:                page.waitForTimeout(200);
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:344:    fun `waitForSelector throws a script-catchable error on timeout`() = runBlocking {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:351:                    page.waitForSelector("#ghost", 50);
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt:374:                var waited = page.waitForSelector(".dashboard", 5000);
scripting/api/src/main/java/app/rocat/scripting/api/ScriptEngine.kt:35:     * `RoCatPage.*` functions (`open`/`type`/`click`/`waitForSelector`/`evaluate`/
app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt:7: * script-facing `RoCatPage` global to the [HeadlessWebViewManager], making every call
app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt:14:class AppScriptBrowserBridge(private val manager: HeadlessWebViewManager) : ScriptBrowserBridge {
app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt:25:    override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt:26:        runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt:24: *  2. `open`/`type`/`click`/`waitForSelector` forward their arguments and return the
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt:70:        override fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt:129:                var waited  = RoCatPage.waitForSelector(".dashboard", 5000);
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt:214:                    RoCatPage.waitForSelector(".profile", 5000);
scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatPageBridgeTest.kt:250:                    RoCatPage.waitForSelector(".profile", 5000);
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:8: * to the [HeadlessWebViewManager], making every call **synchronous for the script** while
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:13: *  - **Page basics** — `open`, `type`, `click`, `waitForSelector`, `evaluate`, `getHtml`, `close`
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:14: *  - **Time control** — `sleep`, `waitForLoad`
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:28:class RoCatBrowserBridge(private val manager: HeadlessWebViewManager) : ScriptBrowserBridge {
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:39:    override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:40:        runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:75:    override fun waitForLoad(state: String, timeoutMs: Long): Boolean =
app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt:76:        runCatching { manager.waitForLoad(state, timeoutMs) }.getOrDefault(false)
app/src/main/java/app/rocat/di/AppModule.kt:23:import app.rocat.scripting.HeadlessWebViewManager
app/src/main/java/app/rocat/di/AppModule.kt:24:import app.rocat.scripting.RoCatBrowserBridge
app/src/main/java/app/rocat/di/AppModule.kt:67:        val headlessWebViewManager = HeadlessWebViewManager(app)
app/src/main/java/app/rocat/di/AppModule.kt:69:        registrar.addSingleton<ScriptBrowserBridge>(RoCatBrowserBridge(headlessWebViewManager))
scripting/rhino/src/test/java/app/rocat/scripting/rhino/CapCutNativeClickTest.kt:96:        override fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/TestBrowserlessScraperTest.kt:17: * the Puppeteer-like `page` global (goto / type / click / waitForSelector / scrollBottom
scripting/rhino/src/test/java/app/rocat/scripting/rhino/TestBrowserlessScraperTest.kt:91:        override fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
scripting/rhino/src/test/java/app/rocat/scripting/rhino/TestBrowserlessScraperTest.kt:200:        assertTrue("expected sleep/waitForTimeout", browser.calls.any { it.startsWith("sleep:") })
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:20: *  - `RoCatPage.waitForSelector(selector, timeoutMs)`       -> wait until a selector exists
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:26: *  - `RoCatPage.sleep(ms)` / `waitForLoad(state, timeoutMs)` -> time control
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:53:        put("waitForSelector", this, PageFn { args ->
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:54:            browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:74:        put("waitForLoad", this, PageFn { args ->
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt:75:            browser.waitForLoad(pageArgString(args, 0, "load"), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:13: *  - `page.waitForSelector` / `page.waitForLoad` / `page.waitForTimeout`
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:17: *  - `locator.click/fill/text/getAttribute/exists/waitFor/all/clickAll/type/scrollIntoView/getBoundingRect`
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:153:    Locator.prototype.waitFor = function (timeout) {
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:246:            this.waitForLoad("domcontentloaded", timeout);
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:248:            this.waitForLoad("load", timeout);
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:270:    Page.prototype.waitForLoad = function (state, timeout) {
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:283:    Page.prototype.waitForTimeout = function (ms) {
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:316:    Page.prototype.waitForSelector = function (selector, timeout) {
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:317:        return this.locator(selector).waitFor(timeout);
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:443:        waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt:444:        waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
package app.rocat.scripting.rhino

/**
 * The auto-injected **general-purpose browser automation polyfill** (Tahap 25).
 * `[RhinoScriptEngine]` evaluates this into every fresh Rhino scope **after** the
 * low-level `RoCatPage` native bridge and **before** any user script runs, exposing the
 * ergonomic global `RoCatBrowser` object with a Playwright/Puppeteer-like surface:
 *
 *  - `RoCatBrowser.launch(options)` / `.getInstance()` / `.connect()`  -> Browser
 *  - `browser.newPage()` / `browser.page()` / `browser.close()`        -> Page lifecycle
 *  - `page.goto(url, options)` / `page.back()` / `page.forward()` / `page.reload()`
 *  - `page.locator(selector)` / `page.click` / `page.fill` / `page.text`
 *  - `page.waitForSelector` / `page.waitForLoad` / `page.waitForTimeout`
 *  - `page.evaluate(fn, args)` / `page.content()` / `page.url()` / `page.title()`
 *  - `page.screenshot(options)` / `page.cookies()` / `page.setCookie` / `page.clearCookies()`
 *  - `page.scrollTo(x, y)` / `page.scrollBottom()`                      (Tahap 29)
 *  - `locator.click/fill/text/getAttribute/exists/waitFor/all/clickAll/type/scrollIntoView/getBoundingRect`
 *
 *  Tahap 29 adds a Puppeteer-like **global `page`** convenience facade that drives the
 *  singleton Browser directly (`page.goto(url)` / `page.click(sel)` / `page.type(sel, txt)`
 *  / `page.scrollBottom()` / `page.screenshot(...)`), so scripts with a single live tab
 *  don't need to touch `RoCatBrowser` at all.
 *
 * Everything is built on top of the synchronous `RoCatPage.*` primitives so it works in
 * Rhino-1.7.15 (no `async`/`await`, no `class`, no spread, no optional chaining — the
 * wrapper is deliberately ES5). Every page-side operation round-trips through one
 * `RoCatPage.evaluate(script)` call (WebView -> JS).
 *
 * When the host does not supply a browser bridge (`RoCatPage` absent), the wrapper still
 * defines `RoCatBrowser` but `launch()`/`newPage()` throw a clear error so scripts can
 * `try/catch` availability. Callers can also probe `typeof RoCatPage === "undefined"`.
 */
const val RO_CAT_BROWSER_WRAPPER_JS: String = """
var RoCatBrowser = (function () {
    var _defaultTimeout = 30000;
    var _browser = null;

    // ============ Internals ============

    function hasPage() {
        return typeof RoCatPage !== "undefined" && RoCatPage !== null;
    }

    function busyWait(ms) {
        if (hasPage() && RoCatPage.sleep) { RoCatPage.sleep(ms); return; }
        var end = Date.now() + ms;
        while (Date.now() < end) {}
    }

    function serializeArg(a) {
        if (typeof a === "function") return String(a);
        if (typeof a === "string" || typeof a === "number" || typeof a === "boolean" || a === null) {
            return JSON.stringify(a);
        }
        if (a === undefined) return "null";
        return JSON.stringify(a);
    }

    function toScript(fn, args) {
        if (typeof fn === "function") {
            var fnStr = fn.toString();
            var argStr = "";
            if (args && args.length) {
                var parts = [];
                for (var i = 0; i < args.length; i++) parts.push(serializeArg(args[i]));
                argStr = parts.join(", ");
            }
            return "(" + fnStr + ")(" + argStr + ")";
        }
        return String(fn);
    }

    function evaluateInPage(fn, args, fallback) {
        if (!hasPage()) return fallback;
        var res = RoCatPage.evaluate(toScript(fn, args));
        return (res === null || res === undefined) ? fallback : res;
    }

    function asResult(res, what) {
        if (res !== null && typeof res === "object") return res;
        if (res === true) return { success: true };
        return { success: false, error: (res === null || res === undefined) ? "No result: " + what : String(res) };
    }

    // ============ Locator Class ============

    function Locator(selector, page) {
        this.selector = selector;
        this.page = page;
    }

    Locator.prototype.click = function () {
        var self = this;
        // Tahap 30: prefer the native touch bridge (RoCatPage.click dispatches a real
        // ACTION_DOWN/ACTION_UP MotionEvent pair on the WebView). Untrusted synthetic
        // events from el.click()/dispatchEvent are ignored by SPA & anti-bot pages.
        if (hasPage() && RoCatPage.click) {
            var ok = RoCatPage.click(this.selector);
            if (ok === true) return { success: true };
            return { success: false, error: "Element not found or click failed: " + this.selector };
        }
        return asResult(evaluateInPage(function (sel) {
            var el = document.querySelector(sel);
            if (!el) return { success: false, error: "Element not found: " + sel };
            var opts = { bubbles: true, cancelable: true, view: window };
            try {
                el.dispatchEvent(new MouseEvent('pointerdown', opts));
                el.dispatchEvent(new MouseEvent('mousedown', opts));
                el.dispatchEvent(new MouseEvent('pointerup', opts));
                el.dispatchEvent(new MouseEvent('mouseup', opts));
                el.dispatchEvent(new MouseEvent('click', opts));
            } catch (e) {
                el.click();
            }
            return { success: true };
        }, [this.selector]), this.selector);
    };

    Locator.prototype.fill = function (text) {
        return asResult(evaluateInPage(function (sel, txt) {
            var el = document.querySelector(sel);
            if (!el) return { success: false, error: "Element not found: " + sel };
            try { el.focus(); } catch (e) {}
            el.value = txt;
            el.dispatchEvent(new Event('input', { bubbles: true }));
            el.dispatchEvent(new Event('change', { bubbles: true }));
            try { el.dispatchEvent(new Event('blur', { bubbles: true })); } catch (e) {}
            return { success: true };
        }, [this.selector, text]), this.selector);
    };

    Locator.prototype.text = function () {
        return evaluateInPage(function (sel) {
            var el = document.querySelector(sel);
            return el ? el.textContent.trim() : "";
        }, [this.selector], "");
    };

    Locator.prototype.getAttribute = function (attr) {
        return evaluateInPage(function (sel, name) {
            var el = document.querySelector(sel);
            return el ? el.getAttribute(name) : null;
        }, [this.selector, attr], null);
    };

    Locator.prototype.exists = function () {
        var res = evaluateInPage(function (sel) {
            return document.querySelector(sel) !== null;
        }, [this.selector], false);
        return res === true;
    };

    Locator.prototype.waitFor = function (timeout) {
        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
        var deadline = Date.now() + timeout;
        while (true) {
            if (this.exists()) return true;
            if (Date.now() >= deadline) {
                throw new Error("Timeout waiting for selector: " + this.selector);
            }
            busyWait(100);
        }
    };

    Locator.prototype.all = function () {
        var res = evaluateInPage(function (sel) {
            var els = document.querySelectorAll(sel);
            var out = [];
            for (var i = 0; i < els.length; i++) {
                var el = els[i];
                var attrs = {};
                if (el.attributes) {
                    for (var a = 0; a < el.attributes.length; a++) {
                        var at = el.attributes[a];
                        attrs[at.name] = at.value;
                    }
                }
                out.push({ text: el.textContent.trim(), html: el.innerHTML, attributes: attrs });
            }
            return out;
        }, [this.selector], []);
        return (res instanceof Array) ? res : [];
    };

    Locator.prototype.clickAll = function () {
        var res = evaluateInPage(function (sel) {
            var els = document.querySelectorAll(sel);
            var out = [];
            for (var i = 0; i < els.length; i++) {
                try {
                    els[i].click();
                    out.push({ index: i, success: true });
                } catch (e) {
                    out.push({ index: i, success: false, error: e.message });
                }
            }
            return out;
        }, [this.selector], []);
        return (res instanceof Array) ? res : [];
    };

    Locator.prototype.type = function (text, delay) {
        delay = (typeof delay === "number") ? delay : 50;
        for (var i = 1; i <= text.length; i++) {
            this.fill(text.substring(0, i));
            busyWait(delay);
        }
        return { success: true };
    };

    Locator.prototype.scrollIntoView = function () {
        return evaluateInPage(function (sel) {
            var el = document.querySelector(sel);
            if (!el) return false;
            try { el.scrollIntoView({ behavior: 'smooth', block: 'center' }); } catch (e) { el.scrollIntoView(); }
            return true;
        }, [this.selector], false) === true;
    };

    Locator.prototype.getBoundingRect = function () {
        return evaluateInPage(function (sel) {
            var el = document.querySelector(sel);
            if (!el) return null;
            var r = el.getBoundingClientRect();
            return { x: r.x, y: r.y, width: r.width, height: r.height, top: r.top, right: r.right, bottom: r.bottom, left: r.left };
        }, [this.selector], null);
    };

    // ============ Page Class ============

    function Page() {
        this._url = "";
        this._title = "";
    }

    Page.prototype.goto = function (url, options) {
        options = options || {};
        var timeout = options.timeout || _defaultTimeout;
        if (!hasPage()) throw new Error("RoCatBrowser: no browser bridge available");
        var opened = RoCatPage.open(url, timeout);
        if (!opened) throw new Error("RoCatBrowser: failed to open " + url);
        this._url = RoCatPage.url() || url;
        this._title = RoCatPage.title() || "";
        var waitUntil = options.waitUntil;
        if (waitUntil === "domcontentloaded" || waitUntil === "interactive") {
            this.waitForLoad("domcontentloaded", timeout);
        } else if (waitUntil === "load" || waitUntil === "complete") {
            this.waitForLoad("load", timeout);
        }
        return this;
    };

    Page.prototype.type = function (selector, text, delay) {
        return this.locator(selector).type(text, delay);
    };

    Page.prototype.scrollTo = function (x, y) {
        if (!hasPage() || !RoCatPage.scrollTo) return false;
        return RoCatPage.scrollTo(
            (typeof x === "number") ? x : 0,
# MEMORY INDEX — rocat-app

- **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.

- **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
- **Fase 39 (2026-08-24)**: [Sinkronisasi Coil3 dan loading preview](task_20260824_1615_Fase_39.md) — Coil memakai `NetworkHelper.client()` global secara lazy; cache key grid memasukkan headers; indikator loading/error preview aktif.
- **Tahap 35 (2026-08-18)**: [Sistem pengaturan skrip & canvas UI](task_20260818_tahap35_settings_system.md) — metadata `@settings`, `RoCat.settings`, Room v2, halaman pengaturan per skrip, komponen canvas baru (text/divider/checkbox/toggle/dropdown/number/colorpicker/textarea/autocomplete/group/layout + flex).
- **Tahap 31 (2026-08-18)**: [UI/media fixes](task_20260818_0000_tahap31_ui_media_fixes.md) — isolasi loading tombol, fullscreen media, SAF writer hardening, template copy.

Proyek: `rocat-app` — Android app modular ala Mihon untuk mengelola & menjalankan custom userscript (Rhino engine). Workspace: `rocat-app/` di root repo ini.

## Status Proyek Terkini
- **Fase 41 SELESAI** (2026-08-26): Video preview/player memakai aspect ratio dinamis; fullscreen adaptif portrait/landscape dan dialog immersive. `assembleDebug` sukses; Spotless tidak tersedia.
- **Fase 40 SELESAI** (2026-08-25): Reader memakai software bitmap dan decode berbasis constraint untuk menghindari limit tekstur OpenGL pada halaman webtoon panjang; memory cache Coil dibatasi 25% RAM dan disk cache 100 MiB. Build debug berhasil; Spotless tidak tersedia.
- **Fase 39 SELESAI** (2026-08-24): Sinkronisasi jaringan Coil3 dengan `NetworkHelper.client()` dan perbaikan indikator loading preview. Build debug berhasil; formatter Spotless tidak tersedia.
- **Tahap 35 SELESAI** (2026-08-18): Sistem Pengaturan Skrip & Canvas UI. **(35.1) Metadata `@settings`** — `ScriptMetadataParser.parseSetting` membaca `// @settings key: type: default=..., label=..., min=..., max=..., step=..., options=a,b,c, maxLength=..., rows=...` (regex `settingLine`/`parameterTokens`); tipe `string|password|boolean|number|select|multiline|color|email` (`ScriptSettingType`, fallback string); `ScriptSetting` (domain) + `ScriptMetadata.settings` + `normalizedDefault`. **(35.2) `RoCat.settings` API** — `ScriptSettingsBridge` (default no-op di `scripting:api`), `ScriptEnvironment.settings`; Rhino `createScope` meng-inject `__rocatSettingsJson__` (snapshot ter-typed boolean/number/string via `typedSettingsJson`/`coerceToJsonValue`) + `__rocatSettingsBridge__` + `RO_CAT_SETTINGS_WRAPPER_JS` → `RoCat.settings` (get/getAll/set/setTemp/getTemp), `RoCat.onSettingsChanged(fn)`, `RoCat.saveHistory/clearHistory(key,value)`, `RoCat.openSettings()`; gating `typeof RoCat.settings === "undefined"` tanpa bridge. **(35.3) Persistence** — Room v2 (`MIGRATION_1_2`): tabel `script_settings` (PK script_id,key) + `script_input_history` (indeks, `GROUP BY value ORDER BY MAX(timestamp) DESC`); entity/DAO baru + `ScriptSettingsManager` (load/setValue validasi clamp/export/import/history/deleteAll + `bridgeFor` + fallback `ScriptDefaultSettings` autoRun/timeout/downloadPath dst di belakang deklarasi skrip). **(35.4) Halaman Pengaturan** — `ScriptSettingsScreen`/`ScriptSettingsViewModel` (route `Screen.ScriptSettings(scriptId)`, ikon settings di `ScriptCanvasScreen`, navigasi dari `RoCat.openSettings()` via `settingsOpenRequests`); kontrol per tipe (Switch/dropdown/color picker/number min-max-step/password/email/textarea); Reset/Export/Import JSON; i18n EN/ID baru. **(35.5) Komponen Canvas** — `ScriptUIComponent` +`Text/Divider/Checkbox/Toggle/Dropdown/Number/ColorPicker/TextArea/Autocomplete/Group/Layout` + properti `flex` (abstract, `flexWeight`); `ScriptUiBridge` +11 method no-op; Rhino `RoCatUiBridge` +`argStringArray`/`argDouble`; `RoCatCoreWrapper.renderOne` dispatch tipe baru; `parseComponents`/`parseComponent` descriptor JSON rekursif; canvas VM `replaceInTree`/`collectFields` rekursif + `loadHistory`/`saveAutocompleteHistory`; `ScriptsViewModel.delete` + `deleteAll`. Fix: regex parser di luar `companion object` dalam `object`, `argStringArray` id array JS = `Int`, `MenuAnchorType` top-level material3, `MaterialTheme.colorScheme` di luar `remember`. Build `:app:assembleDebug` SUCCESS; test domain 11/11, rhino 67 (64 hijau; 3 gagal pre-existing `FixedTestscrapeScraperTest` fixture hilang); `RoCatTahap35Test` baru 8/8.
- **Tahap 33 SELESAI** (2026-08-18): Sinkronisasi klik SPA & screenshot full-page headless. `HeadlessWebViewManager.screenshot()` membaca `scrollHeight`, me-layout WebView sementara ke tinggi konten dinamis (maks. **5000 px**, anti-OOM), mengaktifkan `enableSlowWholeDocumentDraw()` best-effort untuk tile di luar viewport, lalu memulihkan viewport/scroll. Klik native kini memakai koordinat dokumen (`rect + window.scrollX/Y`) → scroll fisik → konversi kembali ke viewport → CSS-px ke view-px; compositor dipaksa redraw sebelum/sesudah tap + settle state SPA. `capcut_test.js` v10 membersihkan marker lama dan menunggu form via `waitForSelector`; docs §7.2b ditambahkan. API `RoCatPage` tidak berubah.
- **Tahap 30 SELESAI** (2026-08-17): Perbaikan Interaksi Event DOM (SPA) pada Headless WebView — **klik native touch**. Analisis akar: `el.click()`/`dispatchEvent` menghasilkan event **untrusted** (`isTrusted=false`) yang diabaikan SPA (React/Vue event delegation) & anti-bot (CapCut/hCaptcha/Cloudflare). **(30.1)** `HeadlessWebViewManager.click(selector)` di-upgrade: `measureIfNeeded` (layout WebView headless ke viewport default 1366×768), `elementBounds` (`getBoundingClientRect` + `scrollIntoView`), `dispatchNativeTap` (**`MotionEvent.ACTION_DOWN`→gap→`ACTION_UP`** via `WebView.dispatchTouchEvent`, source `SOURCE_TOUCHSCREEN`, di pusat elemen → halaman melihat `isTrusted=true` touch/pointer/mouse/click), fallback `clickViaJs` (urutan pointer/mouse lama) bila elemen tak ditemukan / tap ditolak; semua helper anti-crash (`catch Throwable`, `runCatching`). **(30.2)** `RoCatBrowserWrapper` (JS polyfill): `Locator.prototype.click` kini memprioritaskan **`RoCatPage.click(selector)`** (bridge native) bila tersedia → `page.click` & `locator.click` memakai tap native, fallback JS hanya tanpa bridge. **(30.3)** `capcut_test.js` v4→v9 ditulis ulang: hapus rantai klik `evaluate` rumit + pseudo-selector `:has-text()` → tandai elemen `data-rocat-click="1"` lalu **`page.click('[data-rocat-click="1"]')`** langsung; verifikasi email/password + screenshot sebelum/sesudah tetap. **(30.4)** `DOCS_SCRIPTING.md` Bab 7 + **§7.2a Klik Native Touch (Tahap 30)** (kenapa JS click gagal + alur MotionEvent). **(30.5)** `CapCutNativeClickTest.kt` (2 test rhino: onLaunch tak sentuh browser; createAccount alur sinkron penuh → bridge `click:[data-rocat-click="1"]` → verifikasi email/password); assertion `TestBrowserlessScraperTest` diperbarui (click = bridge call). Build `:app:assembleDebug` **SUCCESS**; rhino **59 test, 54 hijau** (5 gagal = pre-existing data script hilang); fetch/RoCatDOM tak tersentuh. **(30.6) [v2 — Debug tap tidak berfungsi]**: akar = (1) koordinat DOM **CSS-px ≠ view-px** — `getBoundingClientRect` dalam CSS-px, `MotionEvent` dalam px view → wajib `scaleX=viewWidth/window.innerWidth` (poll innerWidth sampai stabil, clamp 0.1..10); tanpa ini tap meleset di layar hi-dpi/`device-width`/halaman tanpa viewport-meta (980px); (2) view headless tidak punya **fokus/window-focus** → `prepareForInteraction` (`isFocusable`+`requestFocus`+`requestFocusFromTouch`+`onWindowFocusChanged(true)`); (3) renderer belum menyerap ukuran 1366×768 pasca-`measure` → kick compositor (`draw` scratch bitmap) + `TAP_SETTLE_MS=120` sebelum DOWN + `TAP_GAP_MS=80`. `capcut_test.js` v9 + `CapCutNativeClickTest` dirombak (invoke `createAccount`, FakeBrowser branch `data-rocat-click`/`offsetParent`/`otp`). Docs §7.2a diperbarui. Build `:app:assembleDebug` SUCCESS; rhino 59 test 54 hijau (5 gagal pre-existing).
- **Tahap 29 SELESAI** (2026-08-17): Browserless/Puppeteer-like API Engine & Dokumentasi. **(29.1)** Arsitektur headless WebView diverifikasi lengkap: `HeadlessWebViewManager` (lazy `ensureWebView`, main-thread `Handler`+`CountDownLatch` block script thread, teardown aman di `ScriptCanvasViewModel.onCleared`, `WebViewUtil.setDefaultSettings` full-JS). **(29.2)** `ScriptBrowserBridge` +`scrollTo(x,y)`/`scrollBottom()` (default no-op backward-compat); `RoCatPageBridge` +`put scrollTo/scrollBottom`; `HeadlessWebViewManager` implementasi via `window.scrollTo` / `documentElement.scrollHeight`; `RoCatBrowserBridge` override `runCatching`. **(29.2 polyfill)** `RoCatBrowserWrapper` +`Page.prototype.type(sel,text,delay)` +`scrollTo`/`scrollBottom` + **global `page`** baru (facade Puppeteer-like 22 method → singleton `RoCatBrowser.getInstance().page()`; hanya aktif bila browser bridge ada — `typeof page==="undefined"` di eksekusi polos). **(29.4)** `DOCS_SCRIPTING.md` Bab **7. Browserless / Headless WebView API**: dual-mode statis/interaktif, eksekusi sinkron anti-crash, tabel method `page`, `goto`/`locator`/`evaluate`/`screenshot`, pola lazy-load `scrollBottom`, boilerplate `page.goto`+`RoCatDOM.parse`+`RoCatUI.addImage`; globals & Lampiran diperbarui. **(29.5)** `test_browserless.js` baru (runDemo browserless + runStatic fetch/RoCatDOM) + `TestBrowserlessScraperTest.kt` (3 test rhino drive skrip asli: onLaunch tak sentuh browser, runDemo sinkron penuh + parse `Budi` via RoCatDOM + screenshot + close, runStatic tanpa WebView) + `RoCatBrowserAutomationTest` +3 test (`page` global gating, facade, scroll forward). Build `:app:assembleDebug` SUCCESS; rhino **57 test, 52 hijau** (5 gagal = pre-existing data script `scrape_anichin.js`/`fixed_testscrape.js` hilang).
- **Tahap 28 SELESAI** (2026-08-17): Refactor Mesin JavaScript WebView Berbasis Referensi sweb-master. **(28.1)** Analisis `sweb-master/app/src/main/java/landau/sweb/MainActivity.java` — konfigurasi `createWebView()` referensi: `SINGLE_COLUMN`, `allowUniversalAccessFromFileURLs`, `appCacheEnabled`/`LOAD_DEFAULT`, `domStorageEnabled`, zoom, `onReceivedSslError` Proceed/Cancel, `shouldOverrideUrlLoading` `intent://`→`browser_fallback_url`. **(28.2)** `WebViewUtil.setDefaultSettings` +`allowUniversalAccessFromFileURLs=true` +`layoutAlgorithm=SINGLE_COLUMN` (fidelitas sweb; `setAppCacheEnabled` TIDAK bisa disalin — API dihapus di compileSdk 33+, diganti `LOAD_DEFAULT`); `BrowserScreen.kt` +`onReceivedSslError` (AlertDialog `SslErrorHandler.proceed/cancel`, konstanta **`SSL_IDMISMATCH`** — `SSL_HOSTMISMATCH` tak ada), +`shouldOverrideUrlLoading` fallback `intent://`; i18n EN/ID +`insecureConnectionTitle/insecureConnectionMessage/proceed` (`StringKey.cancel` sudah ada; placeholder `%1\$s`/`%2\$s` wajib di-escape di Strings.kt). **(28.3)** Verifikasi emulator (Chrome-113 WebView, API 34): matriks kontrol (sweb penuh / tanpa SINGLE_COLUMN / bare / UA inferred / browser sistem) — halaman CapCut yang sedang berjalan = **redesign dark** (`arco-theme="dark"`, SSR `signup.html` terbukti via curl) dengan **error hydration React #418/#423/#425 + CSP inline-script ditolak** di SETIAP konfigurasi (termasuk browser sistem, yang juga tak paint penuh: 87.6% putih); DOM probe: `scrollHeight:0`, stylesheets 27→59 ter-load, tema/layout = hasil React mount yang gagal; tanpa AndroidRuntime FATAL & tanpa crash renderer → **bukan blank sistem, tapi keterbatasan page/engine-version**; `getInferredUserAgent` di-revert (tak membantu, pecah konsistensi identitas OkHttp/WebView → tetap Chrome/141 bersama). Konfigurasi final = mirror sweb-master + UA bersama. Bukti: `ai_memory/evidence/capcut_signup_tahap28.png` + `webview_js_tahap28.log`. Build SUCCESS + unit test hijau. **Catatan penting bagi tahap berikutnya**: untuk page modern yang gagal paint di engine WebView lama (Chrome 113), langkah efektif berikutnya = upaya memperbarui System WebView emulator (sangat terbatas di CI) ATAU mengejar upgrade API level/WebView provider; jangan habiskan siklus pada settings WebView yang sudah terbukti bukan penyebab.
- **Tahap 27 SELESAI** (2026-08-17): Otomatisasi Emulator & Verifikasi Rendering Web Modern (CapCut). **(27.1) CI/CD** — `.github/workflows/emulator-webview-test.yml` (reactivecircus/android-emulator-runner v2, API 34 x86_64 Pixel 6 headless `swiftshader_indirect`): identitas package disesuaikan ke **`app.rocat.debug`** (debug pakai suffix `.debug`; diverifikasi `aapt dump badging` → launchable `app.rocat.ui.main.MainActivity`), path APK disesuaikan ke output ABI splits `app/build/outputs/apk/debug/app-x86_64-debug.apk` (fallback universal). **(27.2) Monkey→Intent** — `adb shell monkey` acak dihapus; `am start -n app.rocat.debug/app.rocat.ui.main.MainActivity --es app.rocat.EXTRA_URL "https://www.capcut.com/id-id/signup"` + `sleep 20`; **deep-link baru**: `MainActivity` baca `EXTRA_URL`, `RoCatApp(initialUrl)` skip gerbang first-launch storage (browser tak butuh storage), `RoCatAppNav` start di `KEY_BROWSER`, `BrowserViewModel.acceptInitialUrl` (consumed-once) + `navigateTo` (nonce+1), `BrowserScreen(initialUrl)`. **(27.3) Log & screencap** — `adb logcat -d -v time -s WebViewJS:* AndroidRuntime:E chromium:E WebViewFactory:E` + `exec-out screencap -p` → artifact `webview-render-evidence` (`if: always()`). **(27.4) Evaluasi nyata di emulator** — TERVERIFIKASI TERENDER (bukan blank): logcat `WebViewJS: page title: Daftar - CapCut` (SPA hydrate, hanya warning preload benign, tanpa error JS/AndroidRuntime/chromium); screenshot dianalisis Pillow = **14.0% non-putih** + palet lavender `(243,237,247)` & teks gelap `(73,69,79)` (blank murni ≈100% putih). Perbaikan pencegahan: **`onRenderProcessGone`** recovery (destroy WebView + `webViewEpoch += 1` + **`androidx.compose.runtime.key(webViewEpoch)`** — AndroidView Compose 1.7 TIDAK punya param `key`!), warning `onPageFinished with blank title` (deteksi blank via logcat), `onReceivedTitle` → `page title:` bukti positif, `WebViewUtil` +`mediaPlaybackRequiresUserGesture=false` +`CookieManager.setAcceptCookie(true)`, `RoApp` +`WebView.setWebContentsDebuggingEnabled(BuildConfig.DEBUG)`. **(27.5)** `:app:assembleDebug` SUCCESS (152 task), app & core:common unit test NO-SOURCE; KVM butuh `chmod 666 /dev/kvm` di runner.
- **Tahap 26 SELESAI** (2026-08-17): Resolusi Kritis JavaScript Engine via Dokumentasi Internal. **(26.1) Analisis DOCS_WEBVIEW.md** — instruksi kunci: tambah **Jetpack Webkit** `androidx.webkit:webkit:1.8.0` (BELUM ada di proyek = akar masalah blank page di device WebView lama), JS enabled, `setSupportMultipleWindows(true)` TANPA override `onCreateWindow` (paling aman, blok popup), WebChromeClient fullscreen/permission/console. **(26.2) Fix JS engine** — `libs.versions.toml` + `app/build.gradle.kts`: +`androidx-webkit 1.8.0`; `BrowserScreen.kt` WebChromeClient: `onShowCustomView`/`onHideCustomView` (overlay fullscreen hitam + tombol Close + `BackHandler` LIFO), `onPermissionRequest` grant resources (situs modern tak menggantung), `onConsoleMessage` → **Logcat tag `WebViewJS`** (ERROR/WARNING/DEBUG/INFO + `sourceId:lineNumber: message` — SyntaxError/Cross-Origin/CSP terlihat); `closeFullscreen()` + `Box` root (Column di-re-indent); i18n `closeFullscreen`. **(26.3) Pembersihan** — semua settings Tahap 25 sudah 100% sesuai dokumen, tak ada yang dihapus; `WebViewUtil` KDoc dicatat eksplisit kepatuhan dokumen. **(26.4) Testing** — build + compile SUCCESS; verifikasi render & isi Logcat butuh device. **(26.5)** `:app:assembleDebug` SUCCESS (152 task; kegagalan `packageDebug` pertama transien), `:core:common:testDebugUnitTest` hijau, `:app:testDebugUnitTest` NO-SOURCE; formatter tetap manual (tak ada plugin spotless/ktlint).
- **Tahap 25 SELESAI** (2026-08-17): Perombakan UI/UX In-App Browser, Mode Desktop & Optimalisasi JS Engine. **(25.1) Engine WebView** — `WebViewUtil.setDefaultSettings` + `mixedContentMode = MIXED_CONTENT_COMPATIBILITY_MODE` (JS/DOM storage/database tetap aktif → SPA React/Vue/Angular tak blank); konstanta `DESKTOP_USER_AGENT` (Chrome 141 Win x64); fungsi baru `applyDesktopMode(webView, desktop, mobileUserAgent)`: swap `userAgentString` + `useWideViewPort`/`loadWithOverviewMode` selalu true. **(25.2) Mode Desktop & fitur** — pref `desktopMode` (Boolean) di `SettingsRepository` (persisten); `BrowserViewModel` BARU (pola mihon `StateViewModel` + Injekt): `UiState` (urlInput/currentUrl/canGoBack/Forward/progress/desktopMode/isLoading/`loadNonce`), callback WebViewClient/ChromeClient push nav-state (`NavigationState`), perintah ke WebView lewat `BrowserCommand` sealed (`Reload`/`SetDesktopMode`) via `SharedFlow(extraBufferCapacity=4)` — **WebView tetap milik UI layer** (anti-leak, `DisposableEffect destroy()` tetap); `submitUrl` menormalkan URL (logic `normalizeUrl` dipindah ke VM); pull-to-refresh via **`material3.pulltorefresh.PullToRefreshBox`** + `rememberPullToRefreshState` (material3 1.3.1). **(25.3) UI modern MD3** — address bar **pil** (`RoundedCornerShape(50)` + `Surface(surfaceContainerHigh)` + `BasicTextField`): ikon gembok `Lock`/`LockOpen` (indikator SSL dari scheme https), tombol **clear** (Close), tombol **Go**, Enter fisik + IME; tombol **refresh↔stop**; **overflow 3 titik** `MoreVert` → `ModalBottomSheet` (`ListItem`+`Switch`): Desktop mode, Muat Ulang, Salin Tautan (clipboard+Toast), Buka di Browser Eksternal (`ACTION_VIEW`); **`LinearProgressIndicator` tipis** di bawah address bar tersambung `onProgressChanged`. i18n EN/ID baru: `moreOptions/desktopMode/copyLink/openInBrowser/linkCopied/clearText/reload/secureSite/insecureSite`. Anti double-load start page: factory AndroidView `loadUrl` + set `lastAppliedNonce`, `LaunchedEffect(loadNonce)` skip nonce sama. Build `:app:compileDebugKotlin` + `:app:assembleDebug` SUCCESS (152 task); rhino 46/51 hijau (5 gagal **pre-existing** — data `scrape_anichin.js`/`fixed_testscrape.js` tak ada di checkout, bukan file git-tracked); `gradlew` wrapper jar tidak ada → build pakai Gradle 8.11.1 manual (`/tmp/gradle-8.11.1/`).
- **Tahap 24 SELESAI** (2026-08-16): Media HTTP Headers & Modernisasi UI. **(24.1) Bridge header media** — `ScriptUiBridge.addImage/addVideo/addAudio` + `addGrid` kini menerima `headers: Map<String,String>` (default `{}`); `ScriptUIComponent` Image/Video/Audio/Grid/GridItem bawa `headers`; helper baru `scripting/api/MediaHeaders.kt`: `urlOrigin` (scheme+host+port, hanya http/https, host di-trimStart `.`), `baseUrlFromMatch` (strip `*` glob + `trimEnd('/', ' ')` + retry `://.`→`://` karena `java.net.URI` menolak host ber-titik-awal dari pattern `https://*.site.org/*`), `baseUrlFromMatches`, `effectiveMediaHeaders(url, scriptHeaders, scriptBaseUrl)` — header skrip menang, `Referer` (case-insensitive dinormalisasi) diisi otomatis dari base URL metadata `@match` (fallback origin URL media). Bridge Rhino: `argHeaders(args,i)` (objek JS via `NativeJSON.stringify` atau string JSON, import `kotlinx.serialization.json.contentOrNull`); `RoCatCoreWrapper` render descriptor image/video/audio meneruskan atribut `headers`. **(24.2) Native layer** — `ScriptCanvasViewModel.resolveHeaders`/`scriptBaseUrl`; `addGrid` resolve **per item** (`item.copy(headers = resolveHeaders(headers, item.imageUrl))` — bukan per-grid, fix bug tipe `String?`); `MediaDownloader.download/downloadFromUrl/fetchBytes` + `MediaDownloadComponents.start` terima headers (OkHttp `addHeader`); `ImagePreviewCard` & `GridView` pakai **`coil3.network.NetworkHeaders` + `httpHeaders()`** (Coil 3 TIDAK punya `ImageRequest.Builder.addHeader`!); `RocatVideoPlayer`/`VideoPreviewCard`/`AudioPreviewCard` pakai Media3 `DefaultHttpDataSource.Factory().setDefaultRequestProperties(headers)`; `ScriptCanvasScreen` meneruskan headers ke kartu. **(24.3) UI modernisasi** — `ScriptsScreen`: `SwipeToDismissBox` (EndToStart → delete confirm AlertDialog, StartToEnd → edit, `confirmValueChange` return false agar snap-back), `StatusChip` animasi warna `animateColorAsState` + label `AnimatedContent`, ripple eksplisit `ripple()` + `interactionSource`; `ScriptCanvasScreen`: per-item `Modifier.animateItem()` + `animateContentSize` + hint `AnimatedVisibility(fadeIn/fadeOut)` (anti-flicker clear/redraw); kartu template `JsonLogCard/HtmlPreviewCard/AlertBannerCard/BadgeGroupCard` pakai `ScriptCanvasCard` baru (RoundedCornerShape 20dp + elevation animasi 2→8dp saat pressed). **(24.4)** `DOCS_SCRIPTING.md` §2.2/2.3/2.5 param `headers` + catatan auto-`Referer` + §2.6 descriptor `headers` + contoh boilerplate §5 (`addVideo(..., headers)` html5player). Validasi: `sh gradlew :scripting:rhino:testDebugUnitTest` **44 test hijau** (recorder `:${headers.toSortedMap()}`, assert `endsWith(":true:true:{}")`, fix `emptyMap<String,String>()` inferensi) + `:app:assembleDebug` SUCCESS.
- **Tahap 23 SELESAI** (2026-08-09): Pembaruan Dokumentasi Scripting & Perbaikan Skrip Scraper. **Dokumentasi** `rocat-app/DOCS_SCRIPTING.md` diperbarui ke gaya Tahap 22/23: contoh lifecycle §1.3 & boilerplate §5 ditulis ulang memakai `RoCat.render([...])` + `RoCat.safeParseJson(payload, {})` + `addAlert` + `addBadgeGroup` + `addJsonLog`; §6 Praktik Terbaik ditambah aturan baru (gaya baru render, wajib safeParseJson, konten `<script>` = CDATA — `text()` Jsoup kosong → baca `innerHtml` + selektor `:containsData`); Lampiran menambahkan `fixed_testscrape.js`. **Perbaikan skrip**: draf `testscrape.txt` (XVideos scraper) dirombak jadi `fixed_testscrape.js` v3.0.0 (root repo) — `JSON.parse` → `RoCat.safeParseJson(payload, {})`; status pesan → `RoCatUI.addAlert(level)`; genre detail → `RoCatUI.addBadgeGroup`; sumber stream → `RoCatUI.addJsonLog`; UI verbose → `RoCat.render([...])`. **Bug akar ditemukan & diperbaiki**: ekstraksi script html5player memakai `textOf(...)`/`script.text` yang di Jsoup selalu `""` (konten `<script>` = CDATA) → kini baca **`innerHtml`** (`extractPlayerScript` pakai `doc.find("script:containsData(html5player)...")` lalu fallback manual); judul kartu strip `[tag]` dua-spasi → normalisasi `\s+`→` `. Validasi: `FixedTestscrapeScraperTest.kt` BARU (2 test, rhino) drive `fixed_testscrape.js` asli dengan HTML canned via interceptor OkHttp — onLaunch→doSearch→openDetail (cover og:image + badge genre)→openVideo (HLS best-variant 720p `addVideo(...,true,true)` + jsonLog debug) + test payload malformed/pencarian kosong tak pernah throw. Build `sh gradlew :scripting:rhino:testDebugUnitTest` SUCCESS (**rhino 33 test hijau** = 31 lama + 2 baru).
- **Tahap 22.2 SELESAI** (2026-08-09): Simplification API + Template Cards Script Canvas. **(22.1)** `RoCatCoreWrapper.kt` baru: `RhinoScriptEngine` meng-inject global `RoCat` otomatis ke setiap scope SEBELUM kode user (`cx.evaluateString(scope, RO_CAT_CORE_WRAPPER_JS, "RoCatCore.js", ...)`). `RoCat.render(items)` — gambar seluruh kanvas dari satu descriptor (array/objek: `clear/input/button/image/video/audio/json/html/alert/badges/grid/log`, toleran `pick()`/`pickBool()`), `RoCat.safeParseJson(str,fallback)` (tak pernah throw), `RoCat.fetchJson(url,options)` (parse via `safeParseJson(res.body)` supaya JSON invalid jadi `null` bukan ParseException). Wrapper ES5 Rhino-1.7.15-safe, semua dispatch `try/catch`. Fix `fetch().json()`: Rhino `JsonParser` melempar *checked* `ParseException` yang tak bisa di-catch JS → helper `throwJsError(cx,scope,msg)` baru (`cx.newObject(scope,"Error",...)` + `JavaScriptException`) → script bisa `try/catch` JSON invalid. **(22.2)** 5 komponen UI baru di `app/rocat/ui/components/`: `JsonLogCard` (JSON pretty-print + tombol Copy + Toast, expandable), `HtmlPreviewCard` (`android.text.Html.fromHtml` → `AnnotatedString`, link pakai `LinkAnnotation.Clickable`+`TextLinkStyles`, bukan `ClickableText` deprecated — inline, tanpa WebView), `AudioPreviewCard` (Media3 ExoPlayer inline Play/Pause + seek bar + tombol download via SAF `MediaDownloader`/`folder`), `AlertBannerCard` (`AlertType.from` fallback info untuk level warning/error/success/info, ikon+warna), `BadgeGroupCard` (FlowRow chip). `ScriptUIComponent` +5 tipe: `JsonLog/HtmlPreview/Audio/Alert/BadgeGroup`. `ScriptUiBridge` +5 method default no-op (`addJsonLog/addHtmlPreview/addAudio/addAlert/addBadgeGroup`) → `ScriptCanvasViewModel` implementasi tolerant (`parseBadgeGroup`), `ScriptCanvasScreen` render. Bridge Rhino `RoCatUiBridge` +5 `put` + helper `argJson(args,i)` (normalisasi objek/array via `NativeJSON.stringify`). i18n EN/ID baru (`copyJson/jsonCopied/play/pause/downloadAudio/audioSaved`). `scrape_anichin.js` v3 + `CANVAS_EXAMPLE_SCRIPT` v2 ditulis ulang memakai `RoCat.render` + `addAlert`/`addBadgeGroup` (genre chip dari `parseDetailBadges`). Test baru `RoCatScriptTemplateTest.kt` (9 test: render list/single/toleran, safeParseJson, fetchJson, badge JS array, jsonLog objek/string, alert default & unknown, html+audio). Build `:app:assembleDebug` + `./gradlew test` SUCCESS (**rhino 31 test hijau** = 22 lama + 9 baru).
- **Tahap 20 SELESAI** (2026-08-09): Native Base64 Bridge, Custom User-Agent & Custom DNS (Network Settings). **(20.1)** Bridge `ScriptUiBridge.decodeBase64(input)` baru (default `java.util.Base64` aman JVM test & API26+, return `""` bila gagal) → `RoCatUI.decodeBase64` (Rhino `RoCatUiBridge` + helper `runSafeValue`) → `ScriptCanvasViewModel` override **native Android** (`android.util.Base64.decode(padded, Base64.DEFAULT)` → UTF-8); `scrape_anichin.js` `decodeBase64()` pakai `RoCatUI.decodeBase64(s)` (fallback JS `b64Decode` tetap ada). **(20.2)** UI Pengaturan "Jaringan" di `SettingsScreen`: TextField custom User-Agent (kosong = default "Chrome 141") + dropdown DNS DoH (`DnsMode`: SYSTEM/CLOUDFLARE/GOOGLE/QUAD9/CUSTOM via `DnsModeRow`) + field URL DoH saat CUSTOM; i18n EN/ID baru (`network/userAgent/dnsSelection/...`). **(20.3)** `core/common/network` baru: `DnsMode` + `DnsProviders.dohUrl()` (Cloudflare `cloudflare-dns.com/dns-query`, Google `dns.google/dns-query`, Quad9 `dns.quad9.net/dns-query`) + `DoHResolver` (implementasi `okhttp3.Dns` JSON GET `?name&type=A`, `Answer[].type` 1=A / 28=AAAA, fallback `Dns.SYSTEM`); `NetworkHelper` dirombak: baca `userAgentProvider()`/`dnsConfigProvider()` (dari `SettingsRepository`), client dibangun **lazily** + fingerprint `"$ua|$mode|$custom"` (tidak rebuild per keystroke), `client` diubah jadi fungsi `client()`, `DoHResolver` dipasang ke `.dns(...)`; `SettingsRepository` pref `user_agent`/`dns_mode`/`custom_dns_url`; `AppModule` buat SettingsRepository **sebelum** NetworkHelper; `ScriptManager.engine()/environment()/createEnvironment()` + refresh engine bila fingerprint jaringan berubah; `SettingsViewModel` `settingsState = combine(language, mutableState)` + setter; UI via `viewModel::setUserAgent/setDnsMode/setCustomDnsUrl`. Build `:app:assembleDebug` + `:app:assemblePreview` (R8) + `./gradlew test` SUCCESS (rhino 22 test hijau, termasuk assert `decodeBase64` lewat bridge native).
- **Tahap 19 SELESAI** (2026-08-09): Anichin Scraper End-to-End. Skrip canvas produksi `scrape_anichin.js` (repo root, 320 baris) memakai `RoCatDOM` + `RoCatUI.addGrid/addImage/addVideo`: **Home** (rilisan terbaru `.bixbox article .bsx`) → **Pencarian** (`/page/1?s=`) → **Detail** (cover `.thumb img` → `addImage`, sinopsis, episode `.eplister ul li a`) → **Episode HLS** (decode base64 `select.mirror option`, ekstrak iframe `anichin.stream`, bangun master `/hls/<id>.m3u8`, `pickBestVariant` pilih varian VALID tertinggi — buang STREAM-INF tanpa URI (1080p malformed) → `RoCatUI.addVideo(url, title, isStreamHls=true, allowDownload=true)`). Test `AnichinScraperTest.kt` (rhino) menjalankan skrip asli dengan HTML canned via **interceptor OkHttp** (karena `fetch()` skrip TIDAK lewat `DefaultScriptEnvironment.fetchImpl`). Build `cd rocat-app && sh gradlew :scripting:rhino:testDebugUnitTest` SUCCESS (22 test hijau).
- **Tahap 18 SELESAI** (2026-08-09): Media Template, Downloader (Image & Video) & HLS Streaming (ExoPlayer/Media3). **(18.1)** `ScriptUIComponent.Image(url,title,allowDownload)` menggantikan `Thumbnail`; `ImagePreviewCard` (Coil + tombol download pojok) → unduh via `MediaDownloader` (OkHttp `NetworkHelper.client` membawa cookie jar + UA browser) → `StorageManager.saveFileToScrapeFolder(folder,fileName,mime,bytes)` → tertulis nyata di `[Utama]/Scrapes/[id]`, status Idle/Downloading(progress)/Done/Failed + Toast. **(18.2)** `ScriptUIComponent.Video(url,title,isStreamHls,allowDownload)`; `VideoPreviewCard` = placeholder 16:9 + judul + tombol **Play Inline** (toggle `RocatVideoPlayer`) & **Download Video** (async `Dispatchers.IO`, progress, SAF). **(18.3)** Media3 `media3-exoplayer:1.4.1` + `media3-exoplayer-hls` + `media3-ui`; `RocatVideoPlayer` (AndroidView→`PlayerView`, `ExoPlayer.Builder`, `HlsMediaSource` untuk `.m3u8`/`hls://`, `ProgressiveMediaSource` lainnya); **Full screen**: `Dialog(usePlatformDefaultWidth=false)` + `DialogWindowProvider` + landscape + sembunyikan system bars (immersive), keluar → portrait + bars muncul, player SAMA di-share (inline di-detach); `MainActivity` + `android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"` supaya rotasi tak recreate activity. **(18.4)** Bridge `RoCatUI.addImage(url,title,allowDownload)` & `addVideo(url,title,isStreamHls,allowDownload)` (ScriptUiBridge + Rhino `RoCatUiBridge` + helper `argBoolean`); `ScriptCanvasScreen` render `Image`→ImagePreviewCard, `Video`→VideoPreviewCard; string i18n baru (`download`,`playInline`,`closePlayer`,`imageSaved`, dll). Build `./gradlew :app:assembleDebug` + `:app:assemblePreview` (R8) + `./gradlew test` (rhino 20 hijau, termasuk test `addImage`/`addVideo`) SUCCESS.
- **Tahap 17 SELESAI** (2026-08-09): First-Launch Fix, Storage Skrip Fisik & UI Kategori Collapsible. **(17.1)** Bug "stuck di StorageSetupScreen": `SettingsRepository.storageUri` → `StateFlow<Uri?>` (setter `setStorageUri()`), `StorageManager.isConfigured` → `StateFlow<Boolean>` (`map { it != null }` + `stateIn Eagerly`), `RoCatApp()` pakai `collectAsState()` → begitu folder dipilih, UI langsung pindah ke `RoCatAppNav()` tanpa restart (SettingsViewModel disesuaikan ke `.value`). **(17.2)** Skrip impor kini tersimpan sebagai file fisik: `StorageManager.createScriptFolder(id)` → `[Utama]/Scripts/[id]/`, `saveFileToScriptFolder(scriptId, fileName, content)` (DocumentFile.createFile + `openOutputStream`, mime `application/javascript`), `deleteScriptFolder(id)`; `ImportScriptViewModel` memanggil `persistToStorage()` (best-effort, tidak memblokir import) setelah import URL/paste; `ScriptsViewModel.delete()` ikut menghapus folder fisik. **(17.3)** Long-press: `Modifier.combinedClickable` (`ExperimentalFoundationApi`) di kartu skrip → `ModalBottomSheet` berisi **Edit** (→ `Screen.Detail`, route baru `onEditScript` di RoCatNav) & **Hapus** (konfirmasi `AlertDialog` → `viewModel.delete`). **(17.4)** Metadata kategori: `ScriptMetadataParser` baca `@category` (fallback `@group`) → field `category` baru di `ScriptMetadata` + `Script` (default `""` — backward-compatible decode JSON lama) + diisi `ImportScript`/`UpsertScript`; `ScriptsScreen` grup `state.scripts.groupBy { it.category }`, header kategori tebal + count + ikon ExpandLess/ExpandMore, status expand/collapse per-kategori via `remember { mutableStateMapOf<String, Boolean>() }`, kategori kosong → label i18n "Others"/"Lainnya" (`StringKey.othersCategory`, `StringKey.edit` baru). Test parser kategori baru (6/6 domain). Build `./gradlew :app:assembleDebug` + `./gradlew test` SUCCESS.
- **Tahap 16 SELESAI** (2026-08-09): Perbaikan Storage & Clear Data + In-App Browser Bebas. **(16.1)** `StorageManager.saveFileToScrapeFolder(folder, fileName, mimeType, content)` baru: `DocumentFile.createFile` + `contentResolver.openOutputStream` → file scrape benar-benar tertulis ke storage (sebelumnya hanya folder dibuat); skrip kini bisa `RoCatUI.save(fileName, content, mimeType)` via `ScriptUiBridge.saveFile` + bridge Rhino; `ScriptCanvasViewModel` menulis via StorageManager (`runBlocking`); unit test rhino baru. **(16.2)** `SettingsViewModel.deleteCookies()` + `CookieManager.removeAllCookies(null)` + `flush()` dan `clearCache()` + `WebView(context).clearCache(true)` → cookies/cache WebView (yang dipakai `AndroidCookieJar`) benar-benar bersih sampai akar. **(16.3)** Tab Playground DIHAPUS TOTAL (rute, `Screen.Playground`, `KEY_PLAYGROUND`, ikon `PlayArrow`, `PlaygroundScreen/ViewModel`, `AppViewModelFactory`, registrasi DI, kunci i18n Playground-only) digantikan `Screen.Browser` + ikon `Icons.Filled.Public` + `StringKey.browser`; komponen bersama dipindah dari package `app/rocat/ui/playground/` → **`app/rocat/ui/components/`** (`ScriptUIComponent` + `GridView` `GridComponent`). **(16.4)** `BrowserScreen.kt` baru: address bar bebas (`normalizeUrl` ala Chrome, IME Go + Enter), kontrol Back/Forward/Refresh/Stop, `AndroidView` WebView (`WebViewUtil.setDefaultSettings`, progress bar, `BackHandler` riwayat, `DisposableEffect destroy()`), cookie sesi login WebView otomatis dibagi ke OkHttp scraper via `AndroidCookieJar`. Build `./gradlew :app:assembleDebug` + `./gradlew test` + `./gradlew :app:assemblePreview` (R8) SUCCESS.
- **Tahap 15 SELESAI** (2026-08-09): Lokalisasi, Scoped Storage, Database & Pengaturan. i18n kustom (bukan `res/values`): `app/rocat/i18n/` → `StringKey` enum, `Strings` open class + `EnglishStrings`/`IndonesianStrings`, `I18nProvider` reaktif (StateFlow bahasa → `StateFlow<Strings>`), CompositionLocal `LocalStrings`/`LocalAppLanguage` + `I18nApp()` + `stringResource(key)`; seluruh hardcoded string UI dipindah ke i18n. Storage: `SettingsRepository` (SharedPreferences: language + storageUri), `StorageManager` (SAF `takePersistableUriPermission`, `DocumentFile.fromTreeUri`, `createScrapeFolder()` → `[Utama]/Scrapes/[id]/`, `clearCache()` Coil memory/disk + cacheDir), `StorageSetupScreen` first-launch (OpenDocumentTree), `rememberLauncherForActivityResult` di setup & settings; `ScriptCanvasViewModel.scrapeFolder()` dibuat saat execute. Room 2.6.1 + KSP 2.0.21-1.0.28: `AppDatabase` v1 + `CookieEntity`/`HistoryEntity` + `CookieDao`/`HistoryDao` (deleteAll), singleton di `AppModule`. `Screen.Settings` + item nav ketiga; `SettingsScreen`: pemilih bahasa, ubah direktori, clear cache/cookies/history (konfirmasi dialog + Toast). Build `:app:assembleDebug`, `test`, `assemblePreview` (R8) SUCCESS.
- **Tahap 14 SELESAI** (2026-08-09): Production Readiness (Build, Sign, Splits & Firebase). `app/build.gradle.kts`: signing config `release` dibaca dari `keystore.properties` (root `rocat-app/`), fallback aman ke `signingConfigs.debug` jika keystore tidak ada (CI/ dev tak putus); `splits { abi { isEnable=true; include("armeabi-v7a","arm64-v8a","x86","x86_64"); isUniversalApk=true } }` → 4 APK per-ABI + universal; build types `debug` (`appIdSuffix ".debug"`), `release` (minify+shrinkResources+proguard), `preview` baru (`initWith(release)` + `".preview"`); build type `preview` disalin ke 6 modul library (variant matching AGP). Firebase opsional & aman: `firebase-bom 34.17.0`, `google-services 4.5.0`, `firebase-crashlytics-gradle 3.0.7` di toml + plugin `apply false` di root; plugin & deps Firebase hanya di-apply bila `app/google-services.json` ada. `proguard-rules.pro` baru (`-dontwarn java.beans.**`, keep rhino). Build `./gradlew :app:assembleDebug` + `./gradlew test` + `:app:assemblePreview` (R8) SUCCESS.
- **Tahap 13 SELESAI** (2026-08-08): Full Script-Driven Canvas & Grid (Mihon-like). Script kini dieksekusi di layar kanvas kosong: `ScriptCanvasScreen` (baru) = TopAppBar (Back + nama metadata) + konten LazyColumn render `ScriptUIComponent`; `ScriptCanvasViewModel` (per-skrip, session token anti-stale) auto-call `onLaunch()`. Bridge `RoCatUI` di-ekspansi `addGrid(columns, itemsJson, onClickFunction)` → `ScriptUIComponent.Grid` + `GridItem(title,image,rawJsonPayload)` (parse kotlinx-serialization); klik tile memanggil fungsi JS dengan payload JSON string → alur Search→Grid→Detail via `RoCatUI.clear()`. Grid dirender `GridView` (LazyVerticalGrid tinggi terukur). `ScriptsScreen` kini menampilkan cover `@icon`/`@iconURL` via Coil (fallback ikon default); klik skrip → buka Canvas (menggantikan Playground/Detail sebagai pintu masuk). Contoh `CANVAS_EXAMPLE_SCRIPT` (Manga Scraper Mock) via tombol "Canvas demo" di Import. Playground lama ikut mendukung Grid. Build `./gradlew :app:assembleDebug` + `./gradlew test` SUCCESS (rhino 17 test hijau).
- **Tahap 12 SELESAI** (2026-08-08): Script-Driven UI & Media Previews (Mihon-Style). `PlaygroundScreen` kini sepenuhnya dikendalikan skrip JS lewat native bridge global `RoCatUI` (`ScriptUiBridge`): `addInput(id,hint)`, `addButton(label,fn)`, `thumbnailPreview(url)` (Coil AsyncImage), `videoPreview(url)` (Intent.ACTION_VIEW video/*), `log(text)`, `clear()`. `buildUI()` dipanggil otomatis saat script di-load/switch; tombol mengumpulkan semua nilai Input → `Map<id,value>` → jadi 1 objek JS argumen via `ScriptEngine.invokeNamedFunction()` (return `undefined` → string kosong). `PlaygroundViewModel` pegang `SnapshotStateList<ScriptUIComponent>` (Input/Button/Thumbnail/Video/LogText), update di-marshal main thread + session token anti-stale-render; UI = LazyColumn script-driven. Logika Function Selector + Input Arg statis Tahap 11 DIHAPUS; hasil fungsi ditampilkan di kartu "Console" yang copiable (JSON pretty-print). `ScriptManager.createEnvironment(ui)` + `ExecuteScript.invoke(..., inputs:)`. Build `./gradlew :app:assembleDebug` SUCCESS; test rhino 24 hijau (termasuk 2 RoCatUI baru).
- Sebelumnya (Tahap 12, media renderer & smart inputs, 2026-08-08): `MediaPreviewRenderer` (Coil 3 `AsyncImage`, `Intent.ACTION_VIEW` `video/*`), argumen value-only + regex fungsi public/private, Coil 3.0.4. — Digantikan oleh tahap script-driven di atas. Build `./gradlew :app:assembleDebug` + `./gradlew test` SUCCESS.
- **Tahap 11 SELESAI** (2026-08-08): UI Polish & Playground Dinamis. `PlaygroundViewModel` ganti param statis menjadi `testArgs: List<TestArg>` dinamis (tambah/hapus baris Key+Value) + `testFunction` (Function Selector editable, suggestions `search/detail/main`, nama kustom bebas) → `runFunction()` invoke fungsi target dengan arg dinamis; Playground screen pakai `ElevatedCard`, monospace, dan `CopyableResultCard` dengan tombol "Copy JSON" (pretty-print via kotlinx-serialization + `LocalClipboardManager` + Toast) dan "Copy Text"; `ResultFormatter` helper baru. Polishing: `StatusChip` Active/Inactive di `ScriptsScreen` & `ScriptDetailScreen`, `AlertDialog` konfirmasi hapus script, `ImportScriptScreen` padding konsisten + source monospace. Build `./gradlew :app:assembleDebug` + `./gradlew test` SUCCESS.
- **Tahap 10 SELESAI** (2026-08-08): Stealth Browser networking & cookie management. `AndroidCookieJar` (OkHttp `CookieJar` berbasis `WebView CookieManager`) → OkHttp + WebView berbagi cookie persisten identik (sync + `flush()`); `CloudflareInterceptor` (ala Mihon) mendeteksi 403/503 + `Server: cloudflare` + title `Just a moment...` lalu headless WebView menyelesaikan JS Turnstile di main thread (latch 30s, `ChallengeState` `@Volatile`), mengambil `cf_clearance` baru, retry request, dan selalu `destroy()` WebView (anti-leak); `StealthHeadersInterceptor` menambah `Accept-Language`, `Sec-CH-UA*`, `Sec-Fetch-*` default; `NetworkHelper` kini terima `Context`, pasang cookie jar + interceptor, `newScriptClient()` mewarisi stack penuh. Build `./gradlew :app:assembleDebug` SUCCESS, unit test rhino + domain hijau, script `fetch` API tak berubah.
- **Tahap 9 SELESAI** (2026-08-08): Fix bug fatal Rhino "can't load this type of class file". `ScriptContextFactory.makeContext()` diset `optimizationLevel = -1` → Rhino murni mode interpretasi (Android classloader tak bisa muat `.class` Java dinamis); blok try-catch engine & ViewModel diperlebar (termasuk `Throwable`) agar tidak force-close; Playground tak lagi mencetak teks merah melayang — error/exception masuk ke area Result / log JSON (monospace abu-abu); helper `valueToString()` menormalkan `42.0`→`42` (efek mode interpretasi). Build `./gradlew :app:assembleDebug` SUCCESS, rhino 13/13 + domain test hijau.
- Sebelumnya (Tahap 8, 2026-08-08): Script Execution API + Native DOM Bridge + Testing UI. `JsoupBridge` (baru) diekspos ke Rhino sbg global `RoCatDOM` (parse/select/selectText/selectAttr/selectHtml/has + elemen wrapper text/html/attrs/find/textOf/attrOf/nextElement dll) → skrip tanpa cheerio; `ScriptEngine.invokeFunction()` (+ `ExecuteScript.invoke`) untuk panggil fungsi spesifik (`search`/`detail`) dan stringify hasilnya ke JSON via `NativeJSON`; Playground punya section "Test Execution" (param + tombol Run Search/Run Detail + log area); contoh MangaUpdates ditulis ulang murni sync (fetch sync + RoCatDOM) utk "Load example". Fix bug `BridgeFetch`: `as? String` gagal utk `ConsString` → pakai `Context.toString`. Build `./gradlew :app:assembleDebug` SUCCESS, 22 unit test hijau.
- Sebelumnya (Tahap 7, 2026-08-08): Network & SSL + script loader. `network_security_config.xml` (cleartext + trust-anchor system/user) direferensikan di manifest → fix `CertPathValidatorException`; `NetworkHelper` ala Mihon (UA browser-grade, `MODERN_TLS`/`COMPATIBLE_TLS`, follow redirects, timeout 30s); `ScriptSourceFetcher.normalizeUrl()` (inject `https://`, rewrite GitHub blob→raw, `Dispatchers.IO`); `ImportScriptViewModel` friendly error mapping + example script Rhino-compatible; `WebViewUtil` baru (JS/DOM/database enabled, UA sync). Unit test baru `ScriptSourceFetcherTest`. Build `./gradlew :app:assembleDebug` SUCCESS, semua unit test hijau.
- Sebelumnya (Tahap 6, 2026-08-08): Fix crash Compose `SaveableStateRegistry`. Penyebab (dari crash log): `rememberSaveable { mutableStateListOf(...) }` di `RoCatNav.kt` menampung `SnapshotStateList<String>` yang tidak bisa disimpan ke Bundle. Diperbaiki dengan `saver = listSaver(save = { it.toList() }, restore = { it.toMutableStateList() })`. Build: `./gradlew :app:assembleDebug` SUCCESS.
- Sebelumnya (Tahap 5, 2026-08-08): Global Crash Handler. `CrashHandler` (default uncaught exception handler) menulis report ke `Android/data/app.rocat/files/crash_logs/` via `CrashLogStore`, lalu meluncurkan `CrashActivity` (Activity Compose terpisah, bukan di RoCatNav) yang menampilkan stack trace scrollable + tombol Copy to Clipboard + info path, kemudian `Process.killProcess`. Terdaftar di manifest. Build: `./gradlew :app:assembleDebug` SUCCESS.
- Build: `./gradlew :app:assembleDebug` SUCCESS. Unit tests domain + rhino SUCCESS.
- Sebelumnya (Tahap 4, 2026-08-08): Stabilisasi crash ViewModel factory (`AppViewModelFactory` + Injekt), I/O `Dispatchers.IO`, validasi import URL, error handling Rhino engine.
- Sebelumnya (Tahap 2 & 3, 2026-08-07): metadata parser, fetch bridge Rhino, storage file-based JSON + CRUD, UI Mihon-style (list/detail/import/playground).
- Baseline Tahap 1: modular gradle, NetworkHelper OkHttp, Injekt DI, RhinoScriptEngine dasar.

## Riwayat Log
> Legenda: 10 log tertua (Tahap 2-11.5) sudah diarsipkan ke `archive_phase_1.md`; di bawah hanya log aktif.
| # | Tanggal | File | Ringkasan |
|---|---------|------|-----------|
| 0 | 2026-08-18 | `task_20260818_tahap35_settings_system.md` | #Fase35-SettingsSystem: metadata `@settings` (8 tipe, default ternormalisasi), `RoCat.settings` ter-typed (get/getAll/set/setTemp/getTemp) + `onSettingsChanged`/`saveHistory`/`clearHistory`/`openSettings`, Room v2 (script_settings + script_input_history) + `ScriptSettingsManager` (validasi clamp/export/import/reset + `ScriptDefaultSettings` fallback), halaman pengaturan per skrip (kontrol per tipe + Reset/Export/Import JSON + i18n), komponen canvas baru (text/divider/checkbox/toggle/dropdown/number/colorpicker/textarea/autocomplete/group/layout + `flex` weight + `parseComponents`), canvas VM rekursif `replaceInTree`/`collectFields` + history autocomplete, cleanup `ScriptsViewModel.delete`. Fix: parser regex, `argStringArray` id Int, `MenuAnchorType` top-level, hoist `MaterialTheme`. Build `:app:assembleDebug` SUCCESS; domain 11/11; rhino 67 (64 hijau, 3 gagal pre-existing fixture) + `RoCatTahap35Test` 8/8. |
| 0 | 2026-08-18 | `task_20260818_tahap34_ui_overhaul.md` | #Fase34-UIOverhaul: dynamic Material 3 theme, perbaikan layout storage, tab/paste/file import, editor kode, DownloadManager WebView dengan cookie, streaming media ke SAF, debugging opsional. |
| 1 | 2026-08-17 | `task_20260817_1428_tahap30_puppeteer_click_fix_v2.md` | Tahap 30 **v2**: Debug tap native headless yang tak berfungsi. Akar: (1) **CSS-px→view-px** — `getBoundingClientRect`/`innerWidth` = CSS-px, `MotionEvent` = px view; konversi `viewWidth/innerWidth` (poll stabil, clamp 0.1..10) di `viewportScale`; (2) **fokus/window-state** view detached → `prepareForInteraction` (focusable+requestFocus+onWindowFocusChanged(true)); (3) **sinkron renderer** pasca-`measure` 1366×768 → kick compositor `draw` scratch + `TAP_SETTLE_MS=120` + `TAP_GAP_MS=80`. `capcut_test.js` v9 (helper `findAndTagButton`/`clickButtonByText`/`setSelectValue` — buang `:has-text()`, alur `createAccount`), `CapCutNativeClickTest` invoke `createAccount` + FakeBrowser branch `data-rocat-click`/`offsetParent`/`otp`; Docs §7.2a diperbarui. Build `:app:assembleDebug` SUCCESS, rhino 59 test 54 hijau (5 gagal pre-existing data script hilang). |
| 1 | 2026-08-17 | `task_20260817_1145_tahap30_puppeteer_click_fix.md` | Tahap 30: Perbaikan Interaksi Event DOM (SPA) Headless WebView — **klik native touch**. Analisis: JS `el.click()` = event untrusted (`isTrusted=false`) diabaikan SPA/anti-bot. `HeadlessWebViewManager.click` kini `measureIfNeeded` (layout 1366×768) → `elementBounds` (`getBoundingClientRect`+`scrollIntoView`) → `dispatchNativeTap` (**MotionEvent ACTION_DOWN→60ms→ACTION_UP** via `WebView.dispatchTouchEvent`, source `SOURCE_TOUCHSCREEN`) → fallback `clickViaJs`; `RoCatBrowserWrapper` `Locator.click` prioritas `RoCatPage.click` (native) — `page.click`/`locator.click` tap native; `capcut_test.js` v4 (tandai `data-rocat-click` → `page.click('[data-rocat-click="1"]')`); `DOCS_SCRIPTING.md` §7.2a; `CapCutNativeClickTest` (2 test) + assertion TestBrowserless diperbarui; build `:app:assembleDebug` SUCCESS, rhino 59 test 54 hijau (5 gagal pre-existing data script hilang). |
| 1 | 2026-08-17 | `task_20260817_1035_tahap29_puppeteer_engine.md` | Tahap 29: Browserless/Puppeteer-like API Engine & Dokumen — `ScriptBrowserBridge` +`scrollTo/scrollBottom` (default no-op), `RoCatPageBridge`+`HeadlessWebViewManager`+`RoCatBrowserBridge` implementasi scroll (lazy-load via `window.scrollTo`/`scrollHeight`); polyfill `RoCatBrowserWrapper` +`page.type/scrollTo/scrollBottom` + global `page` facade (22 method, singleton `RoCatBrowser.getInstance().page()`, gating `typeof page`); `DOCS_SCRIPTING.md` Bab 7 lengkap + boilerplate `page.goto`+`RoCatDOM.parse`+`RoCatUI.addImage`; `test_browserless.js` baru + `TestBrowserlessScraperTest` (3 test) + `RoCatBrowserAutomationTest` +3; build `:app:assembleDebug` SUCCESS, rhino 57 test 52 hijau (5 gagal pre-existing data script hilang). |
| 1 | 2026-08-17 | `task_20260817_0952_tahap28_sweb_js_refactor.md` | Tahap 28: Refactor Mesin JS WebView Berbasis sweb-master — `WebViewUtil` +`allowUniversalAccessFromFileURLs` +`SINGLE_COLUMN` (fidelitas referensi; `setAppCacheEnabled` dihapus API di compileSdk 33+ → `LOAD_DEFAULT`), `BrowserScreen` +`onReceivedSslError` dialog Proceed/Cancel (`SSL_IDMISMATCH`) +fallback `intent://`→`browser_fallback_url`, i18n EN/ID; verifikasi emulator (Chrome-113) dengan matriks kontrol membuktikan halaman CapCut (redesign dark `arco-theme="dark"`) punya React hydration errors #418/#423/#425 + CSP refusal di SEMUA konfigurasi (termasuk browser sistem 87.6% putih) — bukan blank sistem (judul hydrate, tanpa crash), keterbatasan page/engine; `getInferredUserAgent` di-revert (tetap Chrome/141 identitas bersama); bukti di `ai_memory/evidence/`; build SUCCESS + unit test hijau. |
| 1 | 2026-08-17 | `task_20260817_0557_tahap27_emulator_js_verification.md` | Tahap 27: Otomatisasi Emulator & Verifikasi Rendering — workflow `emulator-webview-test.yml` (package **`app.rocat.debug`**, APK ABI-split `app-x86_64-debug.apk`/universal, `am start` + `EXTRA_URL` ganti monkey, `sleep 20`, logcat `WebViewJS:* AndroidRuntime:E chromium:E WebViewFactory:E` + screencap → artifact); deep-link baru `app.rocat.EXTRA_URL` (MainActivity → `RoCatApp(initialUrl)` skip gerbang storage → tab Browser → `BrowserViewModel.acceptInitialUrl` consumed-once); hardening WebView: `onRenderProcessGone` + `key(webViewEpoch)` (Compose 1.7 `AndroidView` TANPA param `key` → pakai `androidx.compose.runtime.key`), warning blank-title, `onReceivedTitle` log, `mediaPlaybackRequiresUserGesture=false`, `CookieManager.setAcceptCookie(true)`, `setWebContentsDebuggingEnabled` debug; **VERIFIKASI EMULATOR NYATA**: `Daftar - CapCut` ter-render (title di WebViewJS, screenshot 14% non-putih, tanpa error) — bukan blank; `:app:assembleDebug` SUCCESS. |
| 2 | 2026-08-17 | `task_20260817_0429_tahap26_webview_js_critical_fix.md` | Tahap 26: Resolusi Kritis JS Engine via DOCS_WEBVIEW.md — **akar masalah**: Jetpack Webkit `androidx.webkit:webkit:1.8.0` belum di dependensi (device WebView lama tak dapat kapabilitas terbaru → SPA blank) + WebChromeClient minim. Fix: +`androidx-webkit` di toml & app gradle; `BrowserScreen.kt` WebChromeClient `onShowCustomView`/`onHideCustomView` (overlay fullscreen + Close + BackHandler LIFO), `onPermissionRequest` grant, **`onConsoleMessage` → Logcat tag `WebViewJS`**; i18n `closeFullscreen`; `WebViewUtil` KDoc eksplisit kepatuhan dokumen (settings Tahap 25 sudah 100% sesuai, tak dihapus); `:app:assembleDebug` SUCCESS + `:core:common:testDebugUnitTest` hijau (app test NO-SOURCE). |
| 2 | 2026-08-17 | `task_20260817_0313_tahap25_browser_revamp_and_js_engine.md` | Tahap 25: Browser Revamp & JS Engine — `WebViewUtil` + `mixedContentMode` + `DESKTOP_USER_AGENT` + `applyDesktopMode`; pref `desktopMode` di SettingsRepository; `BrowserViewModel` baru (UiState + `BrowserCommand` SharedFlow + `loadNonce`, WebView tetap di UI layer anti-leak); pull-to-refresh `PullToRefreshBox`; address bar pil MD3 (lock SSL / clear / Go), refresh↔stop, overflow `MoreVert` → ModalBottomSheet (Desktop mode Switch, Reload, Copy Link, Open External); progress bar `onProgressChanged`; i18n EN/ID 10 key; anti double-load `lastAppliedNonce`; `:app:assembleDebug` SUCCESS (rhino 46/51, 5 gagal pre-existing data script hilang). |
| 2 | 2026-08-16 | `task_20260816_0000_tahap24_media_headers_modern_ui.md` | Tahap 24: Media HTTP Headers & Modernisasi UI — `ScriptUiBridge.addImage/addVideo/addAudio/addGrid` + `headers` param; `MediaHeaders.kt` baru (`urlOrigin`/`baseUrlFromMatch`(strip `*` + retry `://.`)`/`baseUrlFromMatches`/`effectiveMediaHeaders` — header skrip menang, `Referer` case-insensitive otomatis dari `@match` → origin URL); bridge Rhino `argHeaders` (objek/JSON string, import `contentOrNull`), `RoCatCoreWrapper` render `headers` attribute; `ScriptCanvasViewModel.resolveHeaders` + `addGrid` resolve per-item (fix bug tipe); native: Coil 3 **`NetworkHeaders`+`httpHeaders()`** (bukan `addHeader`), Media3 `setDefaultRequestProperties`, `MediaDownloader` headers, `ImagePreviewCard`/`GridView`/`RocatVideoPlayer`/`VideoPreviewCard`/`AudioPreviewCard`/`ScriptCanvasScreen`; UI modern: `ScriptsScreen` `SwipeToDismissBox` (hapus/edit) + `StatusChip` animasi + ripple, `ScriptCanvasScreen` `animateItem` + hint fade, `ScriptCanvasCard` (20dp + elevasi dinamis) untuk 4 template card; `DOCS_SCRIPTING.md` §2.2/2.3/2.5/2.6 + boilerplate headers; test recorder `:${headers.toSortedMap()}` + assert `:true:true:{}`; rhino 44 hijau + `:app:assembleDebug` SUCCESS. |
| 3 | 2026-08-09 | `task_20260809_1405_tahap23_docs_update_and_script_fix.md` | Tahap 23: Docs update & Script Fix — `DOCS_SCRIPTING.md` §1.3 & §5 boilerplate ditulis ulang gaya baru (`RoCat.render`/`safeParseJson`/`addAlert`/`addBadgeGroup`/`addJsonLog`), §6 +4 aturan (script CDATA `innerHtml`, `:containsData`), Lampiran +`fixed_testscrape.js`; `testscrape.txt` dirombak → `fixed_testscrape.js` v3.0.0 — fix akar: ekstraksi html5player via `innerHtml` (draft lama `script.text` kosong di Jsoup), safeParseJson payload, addAlert/badges/jsonLog, normalisasi spasi judul; `FixedTestscrapeScraperTest.kt` baru (2 test rhino, OkHttp interceptor canned HTML) drive onLaunch→doSearch→openDetail→openVideo (HLS 720p `:true:true`) + malformed payload; `:scripting:rhino:testDebugUnitTest` SUCCESS (33 hijau = 31+2). |
| 2 | 2026-08-09 | `task_20260809_1301_tahap22_2_script_canvas_templates.md` | Tahap 22.2: Simplification API + Template Cards — wrapper global `RoCat` auto-inject (`RoCatCoreWrapper.kt`: `render(items)` gambarkan canvas dari descriptor array/objek clear/input/button/image/video/audio/json/html/alert/badges/grid/log, `safeParseJson` tak pernah throw, `fetchJson`); fix `fetch().json()` → `throwJsError` + `JavaScriptException` agar JSON invalid bisa `try/catch` JS; 5 komponen baru `JsonLogCard`/`HtmlPreviewCard`(LinkAnnotation, bukan ClickableText deprecated)/`AudioPreviewCard`(ExoPlayer inline+download)/`AlertBannerCard`/`BadgeGroupCard` + `ScriptUIComponent` & `ScriptUiBridge` & `ScriptCanvasViewModel/Screen` & bridge Rhino `argJson`; i18n baru; `scrape_anichin.js` v3 & Manga Scraper Mock v2 pakai `RoCat.render`; `RoCatScriptTemplateTest` 9 test; build debug + test SUCCESS (rhino 31 hijau). |
| 2 | 2026-08-09 | `task_20260809_0759_tahap20_network_settings.md` | Tahap 20: Native Base64 Bridge, Custom User-Agent & Custom DNS — `ScriptUiBridge.decodeBase64` (default `java.util.Base64` aman unit test & API26+, override native `android.util.Base64` di ScriptCanvasViewModel) + `RoCatUI.decodeBase64` (helper `runSafeValue`); `scrape_anichin.js` pakai bridge native (fallback JS tetap); UI "Jaringan" di Settings (`DnsModeRow` dropdown SYSTEM/CLOUDFLARE/GOOGLE/QUAD9/CUSTOM + TextField UA + URL DoH saat CUSTOM); `DnsMode`/`DnsProviders.dohUrl()`/`DoHResolver` (okhttp3.Dns JSON, fallback `Dns.SYSTEM`); `NetworkHelper.client()` lazily rebuild by fingerprint + pref `user_agent`/`dns_mode`/`custom_dns_url`; `ScriptManager.engine()/environment()/createEnvironment()` refresh on fingerprint; build debug + preview (R8) + test SUCCESS (rhino 22 hijau). |
| 2 | 2026-08-09 | `task_20260809_0717_tahap19_anichin_scraper_end_to_end.md` | Tahap 19: Anichin Scraper End-to-End — `scrape_anichin.js` (repo root): onLaunch (home grid `.bixbox article .bsx`) → doSearch (`/page/1?s=`) → openDetail (cover `.thumb img` addImage + sinopsis + episode `.eplister ul li a` grid) → openEpisode (decode base64 `select.mirror option`, iframe `anichin.stream` → master `/hls/<id>.m3u8`, `pickBestVariant` buang STREAM-INF malformed tanpa URI & pilih varian valid tertinggi → `RoCatUI.addVideo(..., true, true)`; mirror non-HLS hanya di-log); `AnichinScraperTest.kt` (2 test, rhino) jalankan skrip asli + HTML canned lewat interceptor OkHttp (fetch skrip tak lewat `fetchImpl`); `:scripting:rhino:testDebugUnitTest` 22 test hijau. |
| 2 | 2026-08-09 | `task_20260809_0604_tahap18_media_downloader_hls_stream.md` | Tahap 18: Media Template & Downloader + HLS Streaming — `ScriptUIComponent.Image`/`Video` + `ImagePreviewCard` & `VideoPreviewCard` (tombol download → `MediaDownloader` OkHttp+SAF `saveFileToScrapeFolder`, progress + Toast), `RocatVideoPlayer` (Media3 1.4.1: ExoPlayer + `HlsMediaSource` `.m3u8` + `ProgressiveMediaSource`, full screen Dialog + landscape + immersive + `configChanges` manifest), bridge `RoCatUI.addImage`/`addVideo` (Rhino + `argBoolean`), string i18n baru; build debug + preview (R8) + test (rhino 20 hijau) SUCCESS. |
| 3 | 2026-08-09 | `task_20260809_0456_tahap17_ui_and_storage_fixes.md` | Tahap 17: First-Launch Fix (`SettingsRepository.storageUri` → `StateFlow`, `StorageManager.isConfigured` → `StateFlow<Boolean>`, `RoCatApp` `collectAsState`), Storage Skrip Fisik (`createScriptFolder` `[Utama]/Scripts/[id]` + `saveFileToScriptFolder` + `deleteScriptFolder`, dipanggil `ImportScriptViewModel`/`ScriptsViewModel`), Long-press (`combinedClickable` + `ModalBottomSheet` Edit→Detail & Hapus→AlertDialog), Kategori Collapsible (`@category`/`@group` parsing → field `category` di `ScriptMetadata`+`Script`, `groupBy` + `mutableStateMapOf` header lipat, label i18n "Others"); build + test (6/6 domain) SUCCESS. |
| 3 | 2026-08-09 | `task_20260809_0331_tahap16_bugfixes_and_browser.md` | Tahap 16: Fix Storage (`StorageManager.saveFileToScrapeFolder` via DocumentFile.createFile + `contentResolver.openOutputStream`, `RoCatUI.save()` → `ScriptUiBridge.saveFile` → skrip menulis nyata ke storage), Fix Clear Data (`CookieManager.removeAllCookies` + flush; `WebView.clearCache(true)` di samping Room DAO + `createCache`), Tab Playground DIHAPUS → tab Browser (`Screen.Browser`, `Icons.Filled.Public`, kunci i18n Playground-only dibuang, komponen dipindah → `app/rocat/ui/components/`), `BrowserScreen` baru: free address bar, Back/Forward/Stop, `WebView` via `AndroidView` (cookie WebView terbagi ke OkHttp scraper via `AndroidCookieJar`); build + test + R8 SUCCESS. |
| 4 | 2026-08-09 | `task_20260809_0213_tahap15_localization_storage_db.md` | Tahap 15: i18n kustom (`app/rocat/i18n/`: StringKey, Strings open class, English/Indonesian, I18nProvider + CompositionLocal + stringResource), SAF storage (`SettingsRepository` SharedPreferences, `StorageManager` DocumentFile + `createScrapeFolder` `[Utama]/Scrapes/[id]/`, `StorageSetupScreen` first-launch, OpenDocumentTree launcher), Room 2.6.1 + KSP (AppDatabase v1, CookieEntity/HistoryEntity, CookieDao/HistoryDao deleteAll, singleton Injekt), `SettingsScreen` (bahasa, ubah direktori, clear cache/cookies/history); `assembleDebug`, `test`, `assemblePreview` (R8) SUCCESS. |
| 5 | 2026-08-09 | `task_20260809_0005_tahap14_production_readiness.md` | Tahap 14: Production Readiness - signing `release` dari `keystore.properties` (fallback debug), ABI splits (4 arsitektur + universal), build types debug/release/preview (preview disalin ke 6 modul library), ProGuard rules Rhino (`-dontwarn java.beans.**`), Firebase BOM 34.17.0 + plugin google-services/crashlytics kondisional (hanya bila `google-services.json` ada); `assembleDebug`, `test`, `assemblePreview` (R8) semua SUCCESS. |
| 6 | 2026-08-08 | `task_20260808_1620_tahap12_media_renderer_and_smart_inputs.md` | Tahap 12: Media Renderer & Smart Inputs - argumen value-only (`List<String>`, Key dihapus, default 1 baris kosong), regex public/private (negative lookahead `_`), `MediaPreviewRenderer` (Coil AsyncImage utk image + tombol Play Video `Intent.ACTION_VIEW` utk video); build & test hijau. |
| 7 | 2026-08-08 | `task_20260808_2223_tahap12_script_driven_ui.md` | Tahap 12 (revisi): Script-Driven UI - native bridge `RoCatUI` (`ScriptUiBridge`: addInput/addButton/thumbnailPreview/videoPreview/clear/log) di Rhino engine; `buildUI()` auto-load; tombol kumpulkan Input -> `Map<id,value>` -> objek JS via `invokeNamedFunction`; `PlaygroundViewModel` pakai `SnapshotStateList<ScriptUIComponent>` + session token; `ScriptUIComponent` LazyColumn; test rhino 24 hijau. |
| 8 | 2026-08-08 | `task_20260808_2333_tahap13_full_script_canvas_and_grid.md` | Tahap 13: Script Canvas & Grid - `ScriptCanvasScreen` kanvas per-skrip (auto `onLaunch()`), `ScriptCanvasViewModel` (session token), bridge `RoCatUI.addGrid` -> `ScriptUIComponent.Grid`/`GridItem` (parse kotlinx JSON), `GridView` (LazyVerticalGrid tinggi terukur), cover `@icon`/`@iconURL` Coil di ScriptsScreen (fallback Extension), klik skrip -> `Screen.Canvas`, contoh `Manga Scraper Mock` (Search->Grid->Detail) + 2 test baru; build & test hijau. |
| - | 2026-08-08 | `archive_phase_1.md` | Arsip phase 1: 10 log Tahap 2-11.5 dirangkum jadi satu file; file individual dihapus. |

## Catatan Teknis Penting
- Tahap 32 (2026-08-18): audit `sweb-master` memperbaiki `setAcceptThirdPartyCookies(webView, true)` (sebelumnya getter saja), mengaktifkan `javaScriptCanOpenWindowsAutomatically`, membiarkan UA mobile bawaan WebView seperti sweb-master, dan menambah `DownloadListener`; ad-blocker sengaja tidak diimpor karena berisiko memblokir bundle SPA. Detail: `task_20260818_1200_tahap32_webview_sweb_integration.md`.
- Native touch click (Tahap 30): **JS `el.click()`/`dispatchEvent` menghasilkan event untrusted (`isTrusted=false`) yang diabaikan SPA & anti-bot** — `page.click(selector)`/`locator.click()` harus memakai **tap native**: `MotionEvent.obtain(downTime, downTime, ACTION_DOWN, x, y, 0)` + `ACTION_UP` via `WebView.dispatchTouchEvent` dengan `.source = InputDevice.SOURCE_TOUCHSCREEN`, di koordinat pusat elemen (`getBoundingClientRect` + `scrollIntoView({block:'center'})`). **KRITIS (v2): `getBoundingClientRect`/`window.innerWidth` = CSS pixel, `MotionEvent` = pixel view WebView** → koordinat wajib dikali rasio `scaleX = viewWidth / window.innerWidth` (dan tinggi) — poll `innerWidth/Height` 3x sampai stabil (renderer menyerap ukuran 1366×768 pasca-`measure`; 2 baca identik = stabil) dan clamp 0.1..10; tanpa ini tap meleset di layar hi-dpi/`device-width`/halaman tanpa viewport-meta (980px). View headless tak berukuran → **wajib `measure`/`layout` dulu ke viewport default (1366×768, sama dengan screenshot)** + **kick compositor** (`Bitmap` scratch + `wv.draw`) karena view detached tak punya choreographer. View detached juga tak punya **fokus/window-focus** → `prepareForInteraction`: `isEnabled/isFocusable/isFocusableInTouchMode/isClickable=true`, `setLongClickable(false)`, `requestFocus(FOCUS_DOWN)`, `requestFocusFromTouch()`, `onWindowFocusChanged(true)`. `TAP_SETTLE_MS=120` sebelum DOWN (settle `scrollIntoView`) + `TAP_GAP_MS=80` antar down/up; sleep hanya di thread skrip, UI thread bebas. Overload `MotionEvent.obtain(6 arg)` = `(long,long,int,float,float,int)` (metaState) → argumen terakhir `0` (Int), bukan `0f`. **Polyfill `RoCatBrowserWrapper` `Locator.click` harus memprioritaskan `RoCatPage.click(sel)`** (bridge native) bila ada — tanpa ini `page.click` jatuh ke JS sintetis lagi. `page.waitForSelector` di polyfill = polling `locator.exists()` via `evaluate` (BUKAN panggil `RoCatPage.waitForSelector`) — test jangan berharap call `wait:`. **Skrip jangan pakai pseudo-selector Playwright `:has-text()`** — `document.querySelector` tak bisa mem-parsingnya; pakai helper yang menemukan elemen by text + set `data-rocat-click` lalu `page.click('[data-rocat-click="1"]')`.
- Refactor mesin JS sweb-master (Tahap 28): **`WebSettings.setAppCacheEnabled` TIDAK ADA lagi** (compileSdk 33+; error "Unresolved reference 'appCacheEnabled'") — niat sweb-master diwarisi lewat `cacheMode = LOAD_DEFAULT`. **`SslError.SSL_HOSTMISMATCH` TIDAK ada** di SDK 35; konstanta yang benar = **`SSL_IDMISMATCH`**. **Placeholder `%1$s`/`%2$s` di strings `Strings.kt` (Kotlin) WAJIB di-escape `%1\$s`** — tanpa escape jadi "Unresolved reference 's'". `StringKey.cancel` sudah ada → jangan tambah duplikat (ambiguity). **Halaman yang "blank putih" TIDAK selalu salah konfigurasi WebView**: di engine lama (mis. WebView Chrome-113 pada image API 34 google_apis) page React modern bisa log hydration errors (#418/#423/#425) + penolakan CSP inline-script pada SETIAP konfigurasi termasuk browser sistem — buktikan dulu dengan **matriks kontrol + probe DOM** (`evaluateJavascript`: `innerHTML.length`, `scrollHeight`, `getComputedStyle(...).backgroundColor`, `document.styleSheets.length`, `arco-theme`) sebelum mengubah settings; verifikasi "bukan blank sistem" = judul ter-hydrate (`page title:` non-kosong) + tanpa `AndroidRuntime` FATAL + tanpa crash renderer. Identitas UA bersama = `NetworkHelper.DEFAULT_USER_AGENT` (Chrome/141); `getInferredUserAgent` tidak terbukti menolong di emulator dan memecah konsistensi OkHttp/WebView — jangan ganti tanpa bukti. Bukti render disimpan di `ai_memory/evidence/` (screencap + log).
- Emulator & verifikasi render (Tahap 27): **package di workflow harus `app.rocat.debug`** — build type debug memakai `applicationIdSuffix = ".debug"` (release = `app.rocat`); activity FQCN = `app.rocat.ui.main.MainActivity` (relative ke namespace, BUKAN applicationId). **ABI splits aktif** → output debug = `app-x86_64-debug.apk`/`app-universal-debug.apk` (tidak ada `app-debug.apk` polos); workflow memilih per-ABI lalu fallback universal. **Deep-link `app.rocat.EXTRA_URL`** dibaca MainActivity → `RoCatApp(initialUrl)` MEN-SKIP gerbang first-launch storage (browser tak butuh storage, jadi test am start langsung masuk browser), back stack diinisialisasi `[KEY_BROWSER]`, `BrowserScreen(initialUrl)` memanggil `viewModel.acceptInitialUrl(url)` (guard `initialUrlConsumed` sekali per ViewModel — pindah tab tak me-reload). **BUKTI render dari logcat `WebViewJS`**: `I/... page title: ...` non-kosong = SPA hydrate (positif); `W/... onPageFinished with blank title` = curiga blank (negatif). **Screenshot bisa dianalisis komputasional** (Pillow/ImageMagick) saat tak bisa dilihat: fraksi non-putih + palet warna — blank murni ≈99-100% putih (CapCut terverifikasi 14% non-putih + lavender `(243,237,247)` + teks `(73,69,79)` = TERENDER). **`onRenderProcessGone` recovery**: gejala blank-putih klasik = renderer crash → `view.destroy()` + `webViewEpoch += 1`; **Compose UI 1.7 (BOM 2024.12.01) `AndroidView` TIDAK punya parameter `key`** (compile error "No parameter with name 'key'") → bungkus dengan **`androidx.compose.runtime.key(webViewEpoch) { AndroidView(...) }`** (kunci slot Compose memaksa factory ulang). `onRenderProcessGone` = API 26+ (minSdk 26 aman). Runner: emulator perlu KVM (`sudo chmod 666 /dev/kvm` bila user bukan grup kvm); `sdkmanager "emulator" "system-images;android-34;google_apis;x86_64"`. Perbaikan pencegahan web modern: `mediaPlaybackRequiresUserGesture=false`, `CookieManager.setAcceptCookie(true)`, `WebView.setWebContentsDebuggingEnabled(BuildConfig.DEBUG)`. Kebijakan Tahap 26 dipertahankan: `setSupportMultipleWindows(true)` TANPA `onCreateWindow`.
- WebView critical JS fix (Tahap 26, sumber = DOCS_WEBVIEW.md): **Jetpack Webkit `androidx.webkit:webkit:1.8.0` WAJIB di dependensi** (dokumen §"Work with WebView on earlier versions") — device dengan System WebView lama tidak bisa memakai kapabilitas WebView terbaru tanpa library ini, halaman SPA/heavy-JS rawan blank. **`setSupportMultipleWindows(true)` TANPA override `WebChromeClient.onCreateWindow` = perilaku paling aman** yang direkomendasikan dokumen (§"Manage windows"): popup `target="_blank"` diblokir — JANGAN tambah override `onCreateWindow` tanpa alasan keamanan. **WebChromeClient minimal = `onProgressChanged`**; agar situs modern penuh, implementasikan `onShowCustomView`/`onHideCustomView` (view HTML5 video di-`re-parent` lewat `AndroidView(factory = { video })` dalam overlay `Box` full-screen; callback `CustomViewCallback.onCustomViewHidden()` harus dipanggil saat tutup; guard anti-tumpuk bila view lama masih tampil), `onPermissionRequest(request) = request.grant(request.resources)` (default WebChromeClient = deny → situs minta izin geolocation/media menggantung), dan **`onConsoleMessage(message)`** untuk meneruskan seluruh console JS ke Logcat (tag `WebViewJS`, level ERROR/WARNING/DEBUG/INFO via `android.util.Log`, format `sourceId:lineNumber: message` → SyntaxError/Cross-Origin/CSP terdiagnosa dari `adb logcat -s WebViewJS`). **BackHandler = LIFO**: handler fullscreen harus dikomposisi SETELAH handler riwayat agar Back saat fullscreen menutup video dulu. Jangan pernah panggil `loadUrl`/`reload` dari dalam `shouldOverrideUrlLoading` (dokumen §"Handle page navigation"). Akar masalah JS blank bukan mixed-content saja — verifikasi juga dependensi Jetpack Webkit & kelengkapan WebChromeClient.
- Browser revamp & JS engine (Tahap 25): **WebView TIDAK boleh dimiliki ViewModel** — instansinya hidup di layer Compose (`remember { mutableStateOf<WebView?>(null) }`), ViewModel mengeluarkan perintah lewat `BrowserCommand` (sealed `Reload`/`SetDesktopMode`) pada `MutableSharedFlow<BrowserCommand>(extraBufferCapacity = 4)`; UI koleksi di `LaunchedEffect(Unit)`, `DisposableEffect` tetap `destroy()` anti-leak. **Anti double-load start page**: `AndroidView(factory)` jalan SEBELUM `LaunchedEffect` → factory mengeksekusi `loadUrl` + menandai `lastAppliedNonce = state.loadNonce`, lalu `LaunchedEffect(state.loadNonce)` melewati nonce yang sama (`if (lastAppliedNonce != state.loadNonce)`). **Pull-to-refresh**: `androidx.compose.material3.pulltorefresh.PullToRefreshBox(isRefreshing, onRefresh, state = rememberPullToRefreshState())` tersedia sejak material3 1.3.0 (BOM 2024.12.01 → 1.3.1), wajib `@OptIn(ExperimentalMaterial3Api::class)`; `isRefreshing` ditutup dari `onPageFinished`/`onProgressChanged(>=100)`/`onReceivedError`. **Mode Desktop**: `WebViewUtil.applyDesktopMode(view, desktop, mobileUA)` swap `userAgentString` (desktop = `WebViewUtil.DESKTOP_USER_AGENT` Chrome 141 Win x64) + `useWideViewPort`/`loadWithOverviewMode` selalu `true`, lalu UI `reload()`; status di-persist ke pref `desktopMode` (SettingsRepository) dan di-restore saat factory membuat WebView. **`mixedContentMode = MIXED_CONTENT_COMPATIBILITY_MODE`** wajib di `setDefaultSettings` untuk situs campuran http/https. `stringResource()` tidak boleh dipanggil dalam callback onClick → tangkap string (mis. `linkCopiedMessage`) di body composable. `onReceivedError(view, request, error)` = overload API 23+ (minSdk 26 aman). Normalisasi URL (inject `https://`, prefix `www.`, fallback `https://www.google.com/search?q=`) dipindah ke `BrowserViewModel.normalizeUrl` (fungsi private file).
- Media headers (Tahap 24): **Coil 3 TIDAK punya `ImageRequest.Builder.addHeader/addHeaders`** — header HTTP dimuat lewat `coil3.network.NetworkHeaders` + extension `coil3.network.httpHeaders` (`import coil3.network.NetworkHeaders; import coil3.network.httpHeaders`): `ImageRequest.Builder(context).data(url).httpHeaders(NetworkHeaders.Builder().apply { set(k,v) }.build())`. Video/audio pakai Media3 `DefaultHttpDataSource.Factory().setDefaultRequestProperties(headers)` (berlaku utk request playlist + segment HLS). **`java.net.URI` menolak host ber-titik-awal** (`https://.example.org` → `getHost() == null`) — `baseUrlFromMatch` perlu retry `replace("://.", "://")` setelah strip `*` supaya `https://*.example.org/*` → `https://example.org`. **`emptyMap()` tanpa argumen di `assertEquals(Object,Object)` gagal infer tipe** → gunakan `emptyMap<String, String>()`. Test recorder menyisipkan `:${headers.toSortedMap()}`; assertion `endsWith(":true:true")` yang lama otomatis menjadi `endsWith(":true:true:{}")`. `contentOrNull` adalah extension `kotlinx.serialization.json` → wajib `import kotlinx.serialization.json.contentOrNull`. UI: `SwipeToDismissBox` (`material3.rememberSwipeToDismissBoxState`) dengan `confirmValueChange` return `false` membuat item **snap-back** (tidak benar-benar ter-dismiss) — cocok utk aksi yang butuh konfirmasi (delete) atau navigasi (edit); `dismissState.dismissDirection` untuk warna/ikon background. `Modifier.animateItem()` (`foundation.lazy.LazyItemScope`, `@OptIn(ExperimentalFoundationApi)`) = anti-flicker idiomatis untuk clear/redraw LazyColumn (bukan Crossfade ber-state shared yang tak bisa menampilkan konten lama).
- Simplification API & Template Cards (Tahap 22.2): **`RoCatCoreWrapper.js` di-inject di `RhinoScriptEngine.createScope`** dengan `cx.evaluateString(scope, RO_CAT_CORE_WRAPPER_JS, "RoCatCore.js", 1, null)` SETELAH `__rocat_sandbox__`/`RoCatUI`/`RoCatDOM`/`document` dan SEBELUM kode user — wrapper harus ditulis ES5 Rhino-1.7.15-safe (tanpa `async`/`class`/spread/optional-chaining) dan tiap dispatch `renderOne` dibungkus `try/catch` + guard `hasUI()` (`typeof RoCatUI !== "undefined"`). `RoCat.render` memetakan `type` → panggilan `RoCatUI.add*` dengan `pick()`/`pickBool()`; `type:"json"` men-serialize objek non-string via `JSON.stringify`; `type:"badges"` menerima array JS ATAU string JSON. **`fetch().json()` TIDAK boleh langsung `JsonParser.parseValue`**: Rhino melempar *checked* `ParseException` yang tak bisa ditangkap `try/catch` JS → selalu lewat `throwJsError(cx, scope, msg)` (`cx.newObject(scope, "Error", arrayOf(message))` + `throw JavaScriptException(error, null, 0)`). `RoCat.fetchJson` memilih `safeParseJson(res.body)` (pakai `JSON.parse` JS yang SyntaxError-nya catchable) bukan `res.json()`. `ScriptUiBridge` method baru wajib ber-**default no-op** (bukan abstract) supaya `RecordingUiBridge` lama & implementasi test lain tetap valid. `argJson(args, i)` di `RoCatUiBridge`: argumen `Scriptable` (objek/array JS) → `NativeJSON.stringify`, `ConsString`/String → `Context.toString`, `Undefined` → default "". UI card `AlertBannerCard` normalisasi level via `AlertType.from(...)` fallback info (bridge meneruskan apa adanya, tak pernah throw).
- Native Base64 & network settings (Tahap 20): **default method `ScriptUiBridge.decodeBase64` TIDAK boleh pakai `android.util.Base64`** — unit test JVM rhino yang memanggilnya kena "Method not mocked"; gunakan `java.util.Base64` (safe di JVM + Android API26+), override native `android.util.Base64` hanya di app (`ScriptCanvasViewModel`). Fungsi bridge yang mengembalikan nilai (decodeBase64) wajib pakai `runSafeValue(default)` (bukan `runSafe` yang mengembalikan `Unit`) agar JS menerima string. `DnsMode` (SYSTEM/CLOUDFLARE/GOOGLE/QUAD9/CUSTOM) + `DnsProviders.dohUrl()` + `DoHResolver` (impl `okhttp3.Dns`, klien DoH sendiri `.dns(Dns.SYSTEM)` anti-rekursi, fallback `Dns.SYSTEM` bila DoH gagal — tanpa dependensi `okhttp3-dnsoverhttps`). **`NetworkHelper.client` kini fungsi `client()`** yang dibangun **lazily** hanya saat fingerprint `"$ua|$mode|$customUrl"` berubah (pemanggil lama: MediaDownloader, ScriptSourceFetcher, ScriptManager di-update). `SettingsRepository` simpan `user_agent`/`dns_mode`/`custom_dns_url` ke pref; `AppModule` buat SettingsRepository **sebelum** NetworkHelper (seed config awal dari pref); `ScriptManager.engine()/environment()/createEnvironment()` kini FUNGSI yang memanggil `refresh()` + membandingkan `NetworkHelper.fingerprint()` → engine/env dibangun ulang saat config berubah.
- Anichin scraper end-to-end (Tahap 19): **`fetch()` skrip TIDAK membaca `DefaultScriptEnvironment.fetchImpl`** — bridge Rhino selalu lewat OkHttp client. Untuk test hermetic pakai `okhttp3.Interceptor` yang me-intercept host target dan membalas HTML canned (`Response.Builder` + `toResponseBody("text/html; charset=utf-8".toMediaType())`; okhttp 4.12). `scrape_anichin.js`: `pickBestVariant` buang baris `#EXT-X-STREAM-INF` yang tidak diikuti URI (master anichin.stream berisi varian 1080p malformed) → kirim URL varian valid ke `RoCatUI.addVideo(url, title, true, true)`; `decodeBase64` coba `android.util.Base64` lalu fallback decoder murni JS (pad otomatis); `readInput` dukung map polos & `{ get }`; skrip memakai hanya sintaks Rhino 1.7.15 aman (sync, tanpa async/spread/optional-chaining/class).
- Media Template, Downloader & HLS (Tahap 18): **Media3 1.4.1** (`media3-exoplayer`, `media3-exoplayer-hls`, `media3-ui`) aman untuk R8 preview tanpa rule tambahan. `RocatVideoPlayer` memakai `ExoPlayer.Builder` + `DefaultHttpDataSource.Factory().setAllowCrossProtocolRedirects(true).setUserAgent(DEFAULT_USER_AGENT)`; HLS dideteksi dari flag `isStreamHls` ATAU URL `.m3u8`/`hls://` → `HlsMediaSource.Factory`, selainnya `ProgressiveMediaSource.Factory`. **Full screen WAJIB `Dialog(usePlatformDefaultWidth=false)`** untuk menembus Scaffold/SystemBar; window dialog diambil via `(LocalView.current.parent as? DialogWindowProvider)?.window` (sudah STABIL di Compose 1.7 — `ExperimentalComposeUiApi` justru unresolved, jangan pakai `@OptIn`). Orientasi `SCREEN_ORIENTATION_SENSOR_LANDSCAPE` masuk / `PORTRAIT` keluar + `WindowInsetsControllerCompat` hide/show system bars; **`MainActivity` butuh `android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"`** agar rotasi tidak recreate activity (kalau tidak, Dialog & state full screen hilang). Player SAMA di-share inline↔fullscreen; inline `PlayerView` di-detach (`update { player = null }`) saat dialog aktif. Download: `MediaDownloader` (OkHttp `NetworkHelper.client` + `StorageManager.saveFileToScrapeFolder`) — `CloudflareInterceptor` aman dari thread IO; status card via `DownloadStatus` (Idle/Downloading/Done/Failed) + `CircularProgressIndicator(progress = { fraction })` (overload lambda di material3 1.3). Bridge baru `RoCatUI.addImage`/`addVideo` (ScriptUiBridge) + helper Rhino `argBoolean(args,index,default)`; `ScriptUIComponent.Thumbnail` diganti `Image` (backward-compat `thumbnailPreview` tetap jalan).
- First-launch, storage fisik skrip & kategori (Tahap 17): **Gate first-launch harus reaktif** — `SettingsRepository.storageUri` sekarang `StateFlow<Uri?>` (setter `setStorageUri()`), `StorageManager.isConfigured` = `StateFlow<Boolean>` (`.map { it != null }` + `.stateIn(scope, SharingStarted.Eagerly, ...)`); akses nilai via `.value` (SettingsViewModel) atau `collectAsState()` (RoCatApp). Skrip impor ditulis sebagai file fisik `[Utama]/Scripts/[id]/<id>.js` via `StorageManager.saveFileToScriptFolder` (DocumentFile.createFile + `openOutputStream`, mime `application/javascript`); `ImportScriptViewModel.persistToStorage()` best-effort (wrap `runCatching`) supaya import tak gagal walau storage belum siap; `ScriptsViewModel.delete()` panggil `deleteScriptFolder`. Long-press pakai `Modifier.combinedClickable` (butuh `@OptIn(ExperimentalFoundationApi::class)`), menu `ModalBottomSheet` (Edit → `Screen.Detail`, Hapus → `AlertDialog` + `viewModel.delete`). Kategori: parser baca `@category` (fallback `@group`); field `category` baru di `ScriptMetadata` + `Script` (default `""`) AMAN untuk decode `scripts.json` lama (kotlinx pakai default saat field hilang); `ScriptsScreen` grup via `groupBy { it.category }`, header kategori `item(key = "category:$name")` + expand state `remember { mutableStateMapOf<String, Boolean>() }` (missing = expanded), label kategori kosong = i18n `StringKey.othersCategory`. `stringResource` hanya boleh dipanggil di dalam blok `item {}` composable, bukan di level builder `forEach` LazyListScope.
- Browser Bug Storage & In-App Browser (Tahap 16): **`RoCatUI.save()`** → `ScriptUiBridge.saveFile(fileName, content, mimeType)` → `StorageManager.saveFileToScrapeFolder()` (DocumentFile.createFile + `contentResolver.openOutputStream`, overload String/byte, sanitize nama). **Jangan pakai `WebView(...).apply { }` di dalam `AndroidView(factory)` saat ada variabel local bernama sama** (`webViewClient`, `webView`, `currentUrl`) → `'val' cannot be reassigned`; gunakan `also { view -> view.webViewClient = ... }`. Interface `ScriptUiBridge` berubah → semua implementasi (termasuk unit test `RecordingUiBridge`) wajib menambah `saveFile`. Clear cookies tuntas = Room `cookieDao.deleteAll()` + `CookieManager.removeAllCookies(null)` + `flush()`; clear cache = Coil + `cacheDir` + `WebView(context).clearCache(true)` (main thread). `BrowserScreen`: WebView via `AndroidView` dipakai `WebViewUtil.setDefaultSettings` (JS/DOM/UA), `Back/Forward/Refresh/Stop` langsung ke riwayat WebView, `BackHandler(enabled=canGoBack)`, `DisposableEffect` `destroy()`; cookie login di browser otomatis dipakai mesin scraper via `AndroidCookieJar`.
- Lokalisasi & storage (Tahap 15): i18n pakai `StringKey` enum + `Strings` **open class** (bukan data class — harus bisa diturunkan oleh `EnglishStrings`/`IndonesianStrings`); `staticCompositionLocalOf<Strings>` butuh tipe eksplisit; `stringResource()` **tidak boleh** dipanggil di callback non-composable (onClick/onFailure) — tangkap string di body composable. Storage: `takePersistableUriPermission` + simpan tree URI di SharedPreferences (`SettingsRepository`); folder scrape `[Utama]/Scrapes/[id]/` via `StorageManager.createScrapeFolder()` (DocumentFile); `clearCache()` = Coil memory+`diskCache.clear()` (suspend → `Dispatchers.IO`) + `context.cacheDir`; first-launch `StorageSetupScreen` memblokir UI sampai folder dipilih. Room 2.6.1 + KSP `2.0.21-1.0.28` (cocok Kotlin 2.0.21), DAO/DB singleton via Injekt; R8 preview jalan tanpa rule tambahan.
- Production Readiness (Tahap 14): build type `preview` (`initWith(release)`, suffix `.preview`) WAJIB juga didefinisikan di setiap modul library (`core:common`, `core:viewmodel`, `domain`, `data`, `scripting:api`, `scripting:rhino`) dengan `initWith(getByName("release"))`; tanpa ini variant matching AGP gagal (app preview tak bisa konsumsi varian library). Signing: `signingConfigs.release` hanya terisi bila `keystore.properties` + keystore ada (`hasReleaseSigning`), jika tidak `release`/`preview` fallback ke `signingConfigs.debug` → build tak pernah gagal. ABI splits aktif: 4 APK per-ABI (`armeabi-v7a`/`arm64-v8a`/`x86`/`x86_64`) + `isUniversalApk = true`. ProGuard Wajib Rhino: `-dontwarn java.beans.**` + `-dontwarn org.mozilla.javascript.**` (R8 error tanpa ini karena `JavaToJSONConverters` mereferensikan `java.beans.*` yang tidak ada di Android). Firebase: plugin `google-services` & `firebase.crashlytics` dideklarasikan `apply false` di root, dan hanya di-apply + deps Firebase ditambahkan bila `app/google-services.json` ada.
- Full Script Canvas & Grid (Tahap 13): `RoCatUI` ekspansi `addGrid(columns, itemsJson, onClickFunction)`; `ScriptCanvasScreen`/`ScriptCanvasViewModel` memakai entry point `onLaunch()` (bukan `buildUI`), auto-run + session token, tombol Button mengumpulkan Input -> `Map<id,value>` via `invokeNamedFunction`; klik item Grid -> `invoke(script, fn, args = listOf(item.rawJsonPayload))` (payload JSON string yang di-`JSON.parse` lagi oleh JS); komponen `Grid`/`GridItem(title,imageUrl,rawJsonPayload)` di-parse dari JSON array via kotlinx-serialization; `GridView` = `LazyVerticalGrid` (`userScrollEnabled=false`) pakai tinggi terukur (tile 180dp) supaya bisa dimuat di dalam LazyColumn; `NavigationBar` hanya untuk Scripts/Playground (kanvas full-screen); `ScriptsScreen` menampilkan cover `script.icon` (`@icon`/`@iconURL`) via Coil `AsyncImage`, fallback ikon Extension. Contoh `CANVAS_EXAMPLE_SCRIPT` = demo Search->Grid->Detail ('Manga Scraper Mock') tersedia via tombol 'Canvas demo' di Import.
- Script-Driven UI (Tahap 12 revisi): global `RoCatUI` tersedia di Rhino scope saat `environment.ui != null` (`ScriptUiBridge`: `addInput(id,hint)`, `addButton(label,functionName)`, `thumbnailPreview(url)`, `videoPreview(url)`, `clear()`, `log(text)`); `buildUI()` dipanggil Kotlin otomatis; tombol mengumpulkan semua nilai `ScriptUIComponent.Input` → `Map<id,value>` → `ScriptEngine.invokeNamedFunction()` mengubahnya jadi 1 argumen objek JS (`inputs.video_url`); return `undefined` diflatkan jadi `""`; UI update dimarshal `Dispatchers.Main` + `uiSession` token membuang render lama. Kontrak lama `{"media_type":"image"/"video","media_url":...}` masih dirender di `CopyableResultCard`.
- Media Renderer & Smart Inputs (Tahap 12 awal): argumen Playground value-only (`testArgs: List<String>`, default `List(1){""}`, tombol "+ Add Input"); regex Public vs Private = `function\s+(?!_)([a-zA-Z$][\w$]*)\s*\(`; Coil 3.0.4 (`coil-compose` + `coil-network-okhttp`). — Digantikan oleh Playground script-driven (testArgs dihapus).
- Playground hotfix (Tahap 11.5): tidak ada lagi card "Run main"/Target URL; `testFunctionSuggestions` diisi otomatis dari `extractFunctionNames(script.source)` (Regex `function\s+([a-zA-Z_$][\w$]*)\s*\(`) saat skrip dipilih/dimuat, `testFunction` default = fungsi pertama; `testArgs` default `emptyList()` → input hanya muncul setelah tombol "+ Add Input"; display & copy di `CopyableResultCard` memakai `ResultFormatter.prettyJson` (JSON pretty-print, non-JSON fallback raw) dan teks dibungkus `SelectionContainer` agar bisa diseleksi. 
- Playground dinamis (Tahap 11): `testArgs: List<TestArg(label,value)>` (baris input Key+Value bisa ditambah/dihapus, nilai blank dilewati saat pemanggilan); `testFunction` editable + suggestion (`search/detail/main`) → `ExecuteScript.invoke(script, fn, args)`; hasil bisa disalin via `CopyableResultCard` (Copy JSON = pretty-print `Json { prettyPrint=true }` fallback ke teks asli jika bukan JSON; Copy Text = raw) dengan `LocalClipboardManager` + `Toast`.
- Stealth network (Tahap 10): `AndroidCookieJar` = OkHttp `CookieJar` ber-basis `WebView CookieManager` → cookie OkHttp & WebView identik (persisten, sync + `flush()`); `CloudflareInterceptor` bypass CF challenge via headless WebView di main thread (`Handler(mainLooper)`, latch 30s, cek cookie `cf_clearance` baru, retry, lalu `destroy()` WebView); `StealthHeadersInterceptor` default header Chromium (`Sec-CH-UA*`, `Sec-Fetch-*`, `Accept-Language`) hanya bila header belum ada; `NetworkHelper(context)` pasang cookie jar + UA + stealth headers + CF interceptor, `newScriptClient()` mewarisi semua.
- Network (Tahap 7): `network_security_config.xml` aktif (cleartext + trust system/user); `NetworkHelper.DEFAULT_USER_AGENT` = `Chrome/141.0.0.0`; ConnectionSpec `MODERN_TLS/COMPATIBLE_TLS/CLEARTEXT`; `ScriptSourceFetcher.normalizeUrl()` otomatis inject `https://` & rewrite GitHub blob→raw; `WebViewUtil.setDefaultSettings()` utk engine fallback.
- Rhino 1.7.15: support `let/const`, arrow fn, template literal, Promise, generator; TIDAK support `async/await`, spread, optional-chaining, class. Script sample harus ditulis sync (`fetch()` mengembalikan objek Response sync dengan `.text()`/`.json()`).
- Rhino di Android WAJIB mode interpretasi (`optimizationLevel = -1` di `ScriptContextFactory.makeContext()`): tanpa ini Rhino mengompilasi JS ke `.class` Java standar yang tidak bisa dimuat classloader Android → error "can't load this type of class file". Efek samping: bilangan bulat dikembalikan sbg `Double` (`42.0`) → dinormalisasi via `valueToString()`.
- `fetch()` bridge: sync, melewati `scriptFetch` (OkHttp client app), balikkan `{status,statusText,ok,url,body,headers,error,text(),json()}`. Arg URL harus dikonversi `Context.toString` (Rhino `ConsString` bukan `java.lang.String`).
- `RoCatDOM` (Tahap 8): global DOM bridge berbasis Jsoup; `parse/select/selectText/selectAttr/selectHtml/has`; elemen wrapper punya `text,html,innerHtml,attrs,attr(),has(),contains(),find(),textOf(),attrOf(),textsOf(),nextElement()` — semua balik nilai JS native.
- `ScriptEngine.invokeFunction()` (Tahap 8): evaluasi source → panggil fungsi bernama (`search`/`detail`) → hasil `NativeJSON.stringify`. Dipakai Playground "Test Execution" (Run Search/Run Detail).
- Watchdog: `ScriptContextFactory` + instruction budget 10M; timeout network di `NetworkHelper.newScriptClient()`.
- Parser metadata: blok `==UserScript==` atau fallback `// @tag`; tag `@name/@version/@description/@author/@match/@include/@icon(/iconURL)`.
- Tahap 32.2 (2026-08-18): isolasi rendering WebView. `BrowserScreen` tidak memiliki
  `shouldInterceptRequest` dan tidak memasang OkHttp/interceptor custom pada WebView;
  jalur resource dibiarkan native Chromium. `AndroidView` dan instance WebView kini
  memakai `MATCH_PARENT` eksplisit, WebView dibuat dengan Activity context, dan manifest
  mengaktifkan `android:hardwareAccelerated="true"`. `WebView.setWebContentsDebuggingEnabled(true)`
  aktif statis; `WebChromeClient.onConsoleMessage` mencatat level, source, baris, dan pesan
  ke Logcat tag `WebViewJS-Console`. Log console aktual belum dapat dikumpulkan tanpa
  menjalankan APK pada emulator/device; gunakan `adb logcat -s WebViewJS-Console` untuk
  membedakan React hydration/CSP error dari kegagalan renderer/layout.
# Tahap 37 (2026-08-19): Coil image bounding diperbaiki dengan `fillMaxWidth` + `ContentScale.FillWidth`, tinggi minimum/loading-error fallback, dan header `Referer` tetap diteruskan melalui `NetworkHeaders`. `RoCatUI.addImage(..., seamless)` backward-compatible (default `false`) untuk mode webtoon edge-to-edge tanpa card/padding/elevasi.
# Tahap 38 (2026-08-24): Grid katalog komik memakai `GridCells.Adaptive` bergaya Mihon, tombol canvas tidak lagi raksasa/full-width, dan reader mempertahankan mode seamless/full-screen dengan Coil precision `INEXACT` + `FIT`. Build debug berhasil; task `spotlessApply` tidak tersedia di project.
# Fase 41: Aspect Ratio Video & Immersive Full Screen Player

- **Status:** Selesai
- **Ringkasan Perubahan:** `VideoPreviewCard` menerima `videoAspectRatio` (default 16:9), meneruskannya ke placeholder dan `RocatVideoPlayer`. Player memakai rasio dinamis; fullscreen memilih orientasi sensor portrait untuk video vertikal dan landscape untuk video horizontal. `FullScreenVideoDialog` memanggil `WindowCompat.setDecorFitsSystemWindows(window, false)` sebelum menyembunyikan system bars. ExoPlayer tetap diingat dan dibagi antara inline/fullscreen agar playback tidak restart.
- **Verifikasi:** `spotlessApply` tidak tersedia di project (task tidak ditemukan). `bash ./gradlew assembleDebug` sukses; hanya ada warning deprecated yang sudah ada di `BrowserScreen` dan `ScriptDetailScreen`.
- **Tugas Selanjutnya:** Validasi runtime pada video portrait dan landscape di perangkat/emulator untuk memastikan transisi fullscreen serta immersive bars berjalan mulus.

exec
/bin/bash -lc "sed -n '1,180p' app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt; sed -n '180,560p' app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt; sed -n '1,220p' scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt; sed -n '1,130p' app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt; sed -n '35,95p' scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt" in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:
package app.rocat.scripting

import android.annotation.SuppressLint
import android.content.Context
import android.graphics.Bitmap
import android.graphics.Canvas
import android.graphics.Color
import android.os.Handler
import android.os.Looper
import android.os.SystemClock
import android.view.InputDevice
import android.view.MotionEvent
import android.view.View
import android.webkit.CookieManager
import android.webkit.WebView
import android.webkit.WebViewClient
import app.rocat.core.common.util.WebViewUtil
import org.json.JSONArray
import org.json.JSONObject
import org.json.JSONTokener
import java.io.File
import java.io.FileOutputStream
import java.util.concurrent.CountDownLatch
import java.util.concurrent.TimeUnit
import java.util.concurrent.atomic.AtomicBoolean
import java.util.concurrent.atomic.AtomicReference
import kotlin.math.roundToInt

/**
 * Manages a single **headless** [WebView] (Tahap 23: dual-mode scraping engine) that
 * backs the script-facing `RoCatPage` global.
 *
 * WebView is Android main-thread bound, while the Rhino engine runs scripts on a
 * background thread. Every public method therefore marshals its work onto the main
 * looper via [Handler] and blocks the calling thread with a [CountDownLatch] until a
 * result is ready. The main UI thread is never blocked — only the script thread parks.
 *
 * The WebView is created lazily on first use and torn down by [close]; the instance is
 * never attached to a view hierarchy, so nothing leaks into the UI.
 */
class HeadlessWebViewManager(private val appContext: Context) {

    private val mainHandler = Handler(Looper.getMainLooper())

    @Volatile
    private var webView: WebView? = null

    private fun onMain(block: () -> Unit) {
        if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
    }

    /** Creates the hidden WebView on the main thread if it does not exist yet. */
    @SuppressLint("SetJavaScriptEnabled")
    private fun ensureWebView(): WebView? {
        webView?.let { return it }
        val latch = CountDownLatch(1)
        val ref = AtomicReference<WebView?>()
        onMain {
            try {
                enableWholeDocumentDrawing()
                val wv = WebView(appContext)
                WebViewUtil.setDefaultSettings(wv)
                wv.setBackgroundColor(Color.TRANSPARENT)
                wv.webViewClient = WebViewClient()
                webView = wv
                ref.set(wv)
            } catch (_: Throwable) {
                ref.set(null)
            }
            latch.countDown()
        }
        if (!latch.await(5, TimeUnit.SECONDS)) return null
        return ref.get()
    }

    /**
     * Opens [url] and blocks until `onPageFinished` (or a load error) fires or
     * [timeoutMs] elapses.
     */
    fun open(url: String, timeoutMs: Long): Boolean {
        val wv = ensureWebView() ?: return false
        val latch = CountDownLatch(1)
        onMain {
            try {
                wv.webViewClient = object : WebViewClient() {
                    override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                }
                wv.loadUrl(url)
            } catch (_: Throwable) {
                latch.countDown()
            }
        }
        return latch.await(timeoutMs, TimeUnit.MILLISECONDS)
    }

    /** Fills the element matching [selector] with [text] (React/Vue-friendly events). */
    fun type(selector: String, text: String): Boolean {
        val js = """
            (function() {
                var el = document.querySelector(${jsQuote(selector)});
                if (!el) return false;
                try { el.focus(); } catch (e) {}
                el.value = ${jsQuote(text)};
                el.dispatchEvent(new Event('input', { bubbles: true }));
                el.dispatchEvent(new Event('change', { bubbles: true }));
                try { el.dispatchEvent(new Event('blur', { bubbles: true })); } catch (e) {}
                return true;
            })()
        """.trimIndent()
        return evaluateJs(js, DEFAULT_EVAL_TIMEOUT_MS) == "true"
    }

    /**
     * Clicks [selector] with a **native touch sequence** (Tahap 30).
     *
     * A plain JS `element.click()` / `dispatchEvent` produces *untrusted* DOM events
     * (`isTrusted === false`) that modern SPA frameworks (React/Vue) and anti-bot
     * gateways (CapCut signup, hCaptcha, Cloudflare Turnstile, ...) routinely ignore,
     * so the page never reacts. To be treated as a genuine screen interaction the click
     * is dispatched as a real [MotionEvent] pair (`ACTION_DOWN` → `ACTION_UP`) at the
     * element's on-screen center through `WebView.dispatchTouchEvent`, which makes the
     * page observe trusted `touchstart/touchend → pointerdown/pointerup →
     * mousedown/mouseup → click` events exactly like a human tap.
     *
     * The WebView is first laid out to the default viewport (same as [screenshot]) so
     * the element's `getBoundingClientRect` coordinates map onto a real coordinate
     * space, and the element is scrolled into view when it sits outside the viewport.
     * DOM coordinates are **CSS pixels** while a [MotionEvent] lives in the view's
     * physical-pixel space, so the bounding box center is multiplied by the live
     * layout-viewport ratio (`viewWidth / window.innerWidth`) before dispatch —
     * without it a tap misses on any page whose density / viewport differs from 1:1.
     * When the element cannot be located (or the native dispatch is rejected) the
     * previous JS event-sequence fallback is used, so the method never crashes and
     * never reports a false negative.
     */
    fun click(selector: String): Boolean {
        val wv = webView ?: ensureWebView() ?: return false
        measureIfNeeded(wv)
        prepareForInteraction(wv)
        val bounds = elementBounds(wv, selector)
        if (bounds != null) {
            val scale = viewportScale(wv) ?: return clickViaJs(selector)
            val viewportLeft = bounds[0] - bounds[4]
            val viewportTop = bounds[1] - bounds[5]
            val cx = ((viewportLeft + bounds[2] / 2f) * scale[0]).roundToInt()
                .coerceIn(0, (wv.width - 1).coerceAtLeast(0))
            val cy = ((viewportTop + bounds[3] / 2f) * scale[1]).roundToInt()
                .coerceIn(0, (wv.height - 1).coerceAtLeast(0))
            if (dispatchNativeTap(wv, cx, cy)) return true
        }
        return clickViaJs(selector)
    }

    /**
     * Ensures the (never-attached) WebView has a real width/height so element
     * coordinates and touch dispatch work in a genuine coordinate space. Uses the
     * same default viewport as [screenshot]; no-op once the view is laid out.
     */
    private fun measureIfNeeded(wv: WebView) {
        if (wv.width > 0 && wv.height > 0) return
        val latch = CountDownLatch(1)
        onMain {
            try {
                wv.measure(
                    View.MeasureSpec.makeMeasureSpec(DEFAULT_VIEWPORT_WIDTH, View.MeasureSpec.EXACTLY),
                    View.MeasureSpec.makeMeasureSpec(DEFAULT_VIEWPORT_HEIGHT, View.MeasureSpec.EXACTLY),
                )
                wv.layout(0, 0, wv.measuredWidth, wv.measuredHeight)
                // Kick the compositor: a detached WebView has no window/choreographer to
                // schedule frames on its own, so force one real frame here (same trick as
                // screenshot()). This makes the renderer apply the fresh 1366×768 size and
                // produce up-to-date hit-test geometry before any touch is dispatched.
                drawCompositorFrame(wv)
            } catch (_: Throwable) {
                // Layout is best-effort; touch fallback still applies below.
            }
            latch.countDown()
        }
        try {
            latch.await(1, TimeUnit.SECONDS)
            latch.await(1, TimeUnit.SECONDS)
        } catch (_: InterruptedException) {
            Thread.currentThread().interrupt()
        }
    }

    /**
     * Gives the (never-attached) WebView a real "active view" state. A detached WebView
     * has no window / attach info, so Android treats it as non-focusable and without
     * window focus; some WebView input paths (focus steering, touch-mode handling,
     * selection) behave differently for such views and can swallow or reject a synthetic
     * [MotionEvent]. We explicitly enable, focus and "window-focus" the view before a tap.
     * Every step is best-effort and wrapped — a headless edge case must never crash.
     */
    private fun prepareForInteraction(wv: WebView) {
        val latch = CountDownLatch(1)
        onMain {
            try {
                if (!wv.isEnabled) wv.isEnabled = true
                wv.isFocusable = true
                wv.isFocusableInTouchMode = true
                wv.isClickable = true
                wv.setLongClickable(false)
                wv.onWindowFocusChanged(true)
                wv.requestFocus(View.FOCUS_DOWN)
                wv.requestFocusFromTouch()
            } catch (_: Throwable) {
                // Best-effort focus hints — native dispatch still applies below.
            } finally {
                latch.countDown()
            }
        }
        try {
            latch.await(DEFAULT_EVAL_TIMEOUT_MS, TimeUnit.MILLISECONDS)
        } catch (_: InterruptedException) {
            Thread.currentThread().interrupt()
        }
    }

    /**
     * Returns the CSS-pixel → view-pixel scale factors `[scaleX, scaleY]` for the live
     * layout viewport, or `null` when the page reports no viewport yet.
     *
     * `getBoundingClientRect()` / `window.innerWidth` are **CSS pixels**, but a
     * [MotionEvent] dispatched to the WebView is in the view's own coordinate space
     * (physical pixels). The WebView maps its layout viewport onto its measured box, so
     * the conversion is exactly `viewWidth / window.innerWidth` (and likewise height).
     * Tahap 30 v1 ignored this ratio and tapped raw CSS coordinates — that only lands on
     * the element when the viewport happens to be 1:1, and misses on every hi-dpi
     * device (`device-width` < view px), pages without a viewport meta (980px default)
     * or any page that zooms. This also polls `window.innerWidth` until it stabilises so
     * the scale is computed after the renderer picks up the freshly-measured viewport
     * size, not from a stale one.
     */
    private fun viewportScale(wv: WebView): FloatArray? {
        var innerW = 0f
        var innerH = 0f
        for (attempt in 0 until VIEWPORT_SETTLE_TRIES) {
            val raw = evaluateJs(
                "JSON.stringify([window.innerWidth || 0, window.innerHeight || 0])",
                DEFAULT_EVAL_TIMEOUT_MS,
            )
            val dims = parseFloatArray(raw)
            if (dims != null && dims[0] > 0f && dims[1] > 0f) {
                if (innerW > 0f && dims[0] == innerW && dims[1] == innerH) {
                    innerW = dims[0]
                    innerH = dims[1]
                    break
                }
                innerW = dims[0]
                innerH = dims[1]
            }
            if (attempt < VIEWPORT_SETTLE_TRIES - 1) sleep(POLL_INTERVAL_MS)
        }
        if (innerW <= 0f || innerH <= 0f) return null
        return floatArrayOf(
            (wv.width.toFloat() / innerW).coerceIn(MIN_TAP_SCALE, MAX_TAP_SCALE),
            (wv.height.toFloat() / innerH).coerceIn(MIN_TAP_SCALE, MAX_TAP_SCALE),
        )
    }

    /** Parses a JSON float array string (`[a, b, c, ...]`) or null on any failure. */
    private fun parseFloatArray(raw: String?): FloatArray? {
        if (raw == null) return null
        val trimmed = raw.trim()
        if (trimmed.isEmpty() || trimmed == "null" || trimmed == "undefined") return null
        return try {
            val arr = JSONArray(trimmed)
            FloatArray(arr.length()) { i -> arr.getDouble(i).toFloat() }
        } catch (_: Exception) {
            null
        }
    }

    /**
     * Returns document-space bounds as `[left, top, width, height, scrollX, scrollY]`.
     * The page is physically scrolled first, then the geometry is read again after the
     * renderer settles. Keeping document and viewport coordinates separate prevents a
     * scrolled element's Y coordinate from being tapped twice-offset or off-screen.
     */
    private fun elementBounds(wv: WebView, selector: String): FloatArray? {
        val scrollJs = """
            (function() {
                var el = document.querySelector(${jsQuote(selector)});
                if (!el) return false;
                try { el.scrollIntoView({ block: 'center', inline: 'center' }); }
                catch (e) { try { el.scrollIntoView(); } catch (e2) {} }
                return true;
            })()
        """.trimIndent()
        if (evaluateJs(scrollJs, DEFAULT_EVAL_TIMEOUT_MS) != "true") return null
        sleep(TAP_SETTLE_MS)
        forceCompositorFrame(wv)

        val boundsJs = """
            (function() {
                var el = document.querySelector(${jsQuote(selector)});
                if (!el) return null;
                var r = el.getBoundingClientRect();
                if (!r || r.width <= 0 || r.height <= 0) return null;
                var sx = window.scrollX || window.pageXOffset || 0;
                var sy = window.scrollY || window.pageYOffset || 0;
                return [r.left + sx, r.top + sy, r.width, r.height, sx, sy];
            })()
        """.trimIndent()
        val values = parseFloatArray(evaluateJs(boundsJs, DEFAULT_EVAL_TIMEOUT_MS)) ?: return null
        return values.takeIf { it.size >= 6 }
    }

    /**
     * Dispatches a trusted screen tap at `(x, y)` through the Android input pipeline:
     * `ACTION_DOWN` → short gap → `ACTION_UP`, each as a touchscreen-sourced
     * [MotionEvent]. This is what makes the page see `isTrusted: true` events instead
     * of the synthetic (untrusted) DOM events a JS `el.click()` produces.
     */
    private fun dispatchNativeTap(wv: WebView, x: Int, y: Int): Boolean {
        val downTime = SystemClock.uptimeMillis()

        val downLatch = CountDownLatch(1)
        val downRef = AtomicReference(false)
        onMain {
            try {
                val down = MotionEvent.obtain(
                    downTime, downTime, MotionEvent.ACTION_DOWN, x.toFloat(), y.toFloat(), 0,
                ).apply { source = InputDevice.SOURCE_TOUCHSCREEN }
                val handled = wv.dispatchTouchEvent(down)
                down.recycle()
                downRef.set(handled)
            } catch (_: Throwable) {
                downRef.set(false)
            } finally {
                downLatch.countDown()
            }
        }
        if (!awaitLatch(downLatch)) return false

        // A realistic tap has a few ms between finger-down and finger-up; this also
        // lets the page start its press/ripple handling before the release arrives.
        try {
            Thread.sleep(TAP_GAP_MS)
        } catch (_: InterruptedException) {
            Thread.currentThread().interrupt()
            return false
        }

        val upLatch = CountDownLatch(1)
        val upRef = AtomicReference(false)
        onMain {
            try {
                val up = MotionEvent.obtain(
                    downTime, SystemClock.uptimeMillis(), MotionEvent.ACTION_UP, x.toFloat(), y.toFloat(), 0,
                ).apply { source = InputDevice.SOURCE_TOUCHSCREEN }
                val handled = wv.dispatchTouchEvent(up)
                up.recycle()
                upRef.set(handled)
            } catch (_: Throwable) {
                upRef.set(false)
            } finally {
                upLatch.countDown()
            }
        }
        if (!awaitLatch(upLatch)) return downRef.get()
        val handled = downRef.get() || upRef.get()
        if (handled) {
            forceCompositorFrame(wv)
            sleep(POST_TAP_SETTLE_MS)
            forceCompositorFrame(wv)
        }
        return handled
    }

    /** Blocks the calling (script) thread on [latch] until it opens or the timeout hits. */
    private fun awaitLatch(latch: CountDownLatch): Boolean = try {
        latch.await(DEFAULT_EVAL_TIMEOUT_MS, TimeUnit.MILLISECONDS)
    } catch (_: InterruptedException) {
        Thread.currentThread().interrupt()
        false
    }

    /** Fallback: the classic synthetic pointer/mouse click sequence, straight to JS. */
    private fun clickViaJs(selector: String): Boolean {
        val js = """
            (function() {
                var el = document.querySelector(${jsQuote(selector)});
                if (!el) return false;
                var opts = { bubbles: true, cancelable: true, view: window };
                try {
                    el.dispatchEvent(new MouseEvent('pointerdown', opts));
                    el.dispatchEvent(new MouseEvent('mousedown', opts));
                    el.dispatchEvent(new MouseEvent('pointerup', opts));
                    el.dispatchEvent(new MouseEvent('mouseup', opts));
                    el.dispatchEvent(new MouseEvent('click', opts));
                } catch (e) {
                    el.click();
                }
                return true;
            })()
        """.trimIndent()
        return evaluateJs(js, DEFAULT_EVAL_TIMEOUT_MS) == "true"
    }

    /** Polls the live DOM until [selector] exists or [timeoutMs] elapses. */
    fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
        val probe = "(document.querySelector(${jsQuote(selector)}) !== null)"
        val deadline = System.currentTimeMillis() + timeoutMs
        while (true) {
            if (evaluateJs(probe, DEFAULT_EVAL_TIMEOUT_MS) == "true") return true
            if (System.currentTimeMillis() >= deadline) return false
            try {
                Thread.sleep(POLL_INTERVAL_MS)
            } catch (_: InterruptedException) {
                return false
            }
        }
    }

    /** Runs [script] in the live page and returns the raw JSON-encoded result. */
    fun evaluate(script: String): String =
        evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"

    /** Returns the current fully-rendered HTML (JSON-decoded). */
    fun getHtml(): String {
        val raw = evaluateJs("document.documentElement.outerHTML", DEFAULT_EVAL_TIMEOUT_MS) ?: return ""
        return unquoteJson(raw)
    }

    /** Releases the hidden WebView and frees its memory (safe to call repeatedly). */
    fun close() {
        onMain {
            val wv = webView
            webView = null
            wv?.let { view ->
                try {
                    view.stopLoading()
                    view.removeAllViews()
                    view.destroy()
                } catch (_: Throwable) {
                    // Already destroyed — nothing else to free.
                }
            }
        }
    }

    // =====================================================================
    // Tahap 25 — General-purpose automation commands (Playwright-like subset)
    // =====================================================================

    /** Pauses the calling (script) thread for [ms] ms — no main-thread hop needed. */
    fun sleep(ms: Long): Boolean = try {
        Thread.sleep(ms.coerceAtLeast(0))
        true
    } catch (_: InterruptedException) {
        false
    }

    /** Current page URL (via the live DOM), or `""` when no page is open. */
    fun url(): String {
        val raw = evaluateJs("location.href", DEFAULT_EVAL_TIMEOUT_MS) ?: return ""
        return unquoteJson(raw)
    }

    /** Current page title (via the live DOM), or `""` when no page is open. */
    fun title(): String {
        val raw = evaluateJs("document.title", DEFAULT_EVAL_TIMEOUT_MS) ?: return ""
        return unquoteJson(raw)
    }

    /** Navigates the WebView back one history entry. */
    fun goBack(): Boolean = navigate { it.goBack() }

    /** Navigates the WebView forward one history entry. */
    fun goForward(): Boolean = navigate { it.goForward() }

    /** Reloads the current page. */
    fun reload(): Boolean = navigate { it.reload() }

    /** Stops the current page load. */
    fun stop(): Boolean = navigate { it.stopLoading() }

    /**
     * Polls `document.readyState` until it reaches the target for [state] or
     * [timeoutMs] elapses. `"load"`/`"complete"` target `complete`;
     * `"domcontentloaded"`/`"interactive"` target `interactive`.
     */
    fun waitForLoad(state: String, timeoutMs: Long): Boolean {
        val target = when (state.trim().lowercase()) {
            "domcontentloaded", "interactive" -> "interactive"
            else -> "complete"
        }
        val probe = "(document.readyState === ${jsQuote(target)})"
        val deadline = System.currentTimeMillis() + timeoutMs
        while (true) {
            if (evaluateJs(probe, DEFAULT_EVAL_TIMEOUT_MS) == "true") return true
            if (System.currentTimeMillis() >= deadline) return false
            try {
                Thread.sleep(POLL_INTERVAL_MS)
            } catch (_: InterruptedException) {
                return false
            }
        }
    }

    /**
     * Draws the rendered page into a bitmap and writes it as a PNG to [path] (when
     * absolute / non-blank) or a timestamped file under the app cache. Returns the
     * absolute path of the written file, or `""` on any failure.
     */
    fun screenshot(path: String, quality: Int): String {
        val wv = webView ?: return ""
        measureIfNeeded(wv)
        val metrics = parseFloatArray(
            evaluateJs(
                """
                    (function() {
                        var d = document.documentElement, b = document.body;
                        var h = Math.max(d ? d.scrollHeight : 0, d ? d.offsetHeight : 0,
                            b ? b.scrollHeight : 0, b ? b.offsetHeight : 0,
                            window.innerHeight || 0);
                        return [h, window.innerWidth || 0, window.scrollX || 0, window.scrollY || 0];
                    })()
                """.trimIndent(),
                DEFAULT_EVAL_TIMEOUT_MS,
            ),
        )
        val cssHeight = metrics?.getOrNull(0)?.takeIf { it > 0f } ?: DEFAULT_VIEWPORT_HEIGHT.toFloat()
        val cssWidth = metrics?.getOrNull(1)?.takeIf { it > 0f } ?: DEFAULT_VIEWPORT_WIDTH.toFloat()
        val documentScrollX = metrics?.getOrNull(2)?.roundToInt() ?: 0
        val documentScrollY = metrics?.getOrNull(3)?.roundToInt() ?: 0
        val captureWidth = wv.width.takeIf { it > 0 } ?: DEFAULT_VIEWPORT_WIDTH
        val captureHeight = (cssHeight * captureWidth / cssWidth).roundToInt()
            .coerceIn(DEFAULT_VIEWPORT_HEIGHT, MAX_SCREENSHOT_HEIGHT)

        evaluateJs("window.scrollTo(0, 0); true", DEFAULT_EVAL_TIMEOUT_MS)
        val latch = CountDownLatch(1)
        val ref = AtomicReference<Bitmap?>()
        onMain {
            val oldWidth = wv.width.takeIf { it > 0 } ?: DEFAULT_VIEWPORT_WIDTH
            val oldHeight = wv.height.takeIf { it > 0 } ?: DEFAULT_VIEWPORT_HEIGHT
            val oldViewScrollX = wv.scrollX
            val oldViewScrollY = wv.scrollY
            try {
                wv.measure(
                    View.MeasureSpec.makeMeasureSpec(captureWidth, View.MeasureSpec.EXACTLY),
                    View.MeasureSpec.makeMeasureSpec(captureHeight, View.MeasureSpec.EXACTLY),
                )
                wv.layout(0, 0, captureWidth, captureHeight)
                wv.scrollTo(0, 0)
                wv.invalidate()
                val bitmap = Bitmap.createBitmap(captureWidth, captureHeight, Bitmap.Config.ARGB_8888)
                wv.draw(Canvas(bitmap))
                ref.set(bitmap)
            } catch (_: Throwable) {
                ref.set(null)
            } finally {
                try {
                    wv.measure(
                        View.MeasureSpec.makeMeasureSpec(oldWidth, View.MeasureSpec.EXACTLY),
                        View.MeasureSpec.makeMeasureSpec(oldHeight, View.MeasureSpec.EXACTLY),
                    )
                    wv.layout(0, 0, oldWidth, oldHeight)
                    wv.scrollTo(oldViewScrollX, oldViewScrollY)
package app.rocat.scripting.api

/**
 * Headless-browser bridge exposed to user scripts as the global `RoCatPage` object
 * (Tahap 23: dual-mode scraping engine). It mirrors the Puppeteer subset RoCat scripts
 * need to break form logins, anti-bot challenges and JS-generated player iframes that a
 * plain `fetch()` + Jsoup parse cannot reach.
 *
 * **Threading contract:** the Rhino engine evaluates scripts on a background coroutine,
 * so every method here is **synchronous and blocking**. Implementations marshal their
 * work onto the Android main thread (a WebView is main-thread bound) and park the
 * calling thread until a result is ready. Only the background script thread is blocked —
 * the main UI thread stays responsive.
 *
 * **Dual-mode guidance:** prefer `fetch()` + `RoCatDOM` (Mode Statis) for plain HTML
 * scraping — it is cheap, fast and battery friendly. Reach for [open]/[type]/[click]/
 * [waitForSelector]/[evaluate] only when the target genuinely needs a live browser
 * (JS-rendered DOM, form submission, anti-bot walls), because rendering a real page is
 * far heavier than an HTTP request.
 *
 * All failures are reported through return values (e.g. `false`, empty string), never
 * thrown, so a misbehaving page cannot crash the script.
 */
interface ScriptBrowserBridge {

    /**
     * Opens [url] in the hidden WebView and blocks until the page finished loading or
     * [timeoutMs] elapses.
     *
     * @return `true` when the page reached `onPageFinished` (or reported a load error)
     *   within the timeout; `false` on timeout / no browser available.
     */
    fun open(url: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean

    /**
     * Fills the first element matching [selector] with [text], dispatching real
     * `focus`/`input`/`change`/`blur` events so React/Vue-style frameworks pick the
     * value up.
     *
     * @return `true` when the element existed and was filled.
     */
    fun type(selector: String, text: String): Boolean

    /**
     * Clicks the first element matching [selector]. The app-side implementation
     * (Tahap 30) first tries a **native touch tap** (a real `ACTION_DOWN`/`ACTION_UP`
     * `MotionEvent` pair at the element's on-screen center) so SPA / anti-bot pages
     * see trusted events instead of the synthetic (untrusted) ones a JS `el.click()`
     * produces; a JS pointer/mouse sequence remains the fallback.
     *
     * @return `true` when the element existed and was clicked.
     */
    fun click(selector: String): Boolean

    /**
     * Polls the live DOM until an element matching [selector] exists or [timeoutMs]
     * elapses. Useful after a click that triggers an async navigation/re-render.
     *
     * @return `true` when the selector appeared within the timeout.
     */
    fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean

    /**
     * Runs [script] inside the page's own JavaScript context and returns the value the
     * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
     * contract). The Rhino bridge parses that string back into a native JS value; when
     * it is not JSON (e.g. the literal `undefined`) the raw string is returned instead.
     *
     * Returns `"null"` when no page is open or the script could not run.
     */
    fun evaluate(script: String): String

    /** Returns the fully-rendered current HTML (`document.documentElement.outerHTML`). */
    fun getHtml(): String

    /** Releases the hidden WebView and frees its memory. No-op when no page is open. */
    fun close()

    // =====================================================================
    // Tahap 25 — General-purpose automation commands (Playwright-like subset)
    // ---------------------------------------------------------------------
    // Every new method below has a **default no-op implementation** so existing
    // bridges / test doubles written before Tahap 25 stay valid. The app-side
    // RoCatBrowserBridge overrides the ones the hidden WebView can really do.
    // =====================================================================

    /**
     * Pauses the calling (script) thread for [ms] milliseconds. Unlike every other
     * method this does **not** hop to the main thread — the script thread itself
     * sleeps, which is cheap and lets scripts implement `waitForTimeout`.
     *
     * @return `true` when the sleep ran to completion; `false` when interrupted.
     */
    fun sleep(ms: Long): Boolean = false

    /** Current page URL, or `""` when no page is open. */
    fun url(): String = ""

    /** Current page title, or `""` when no page is open. */
    fun title(): String = ""

    /** Navigates the WebView back one history entry. */
    fun goBack(): Boolean = false

    /** Navigates the WebView forward one history entry. */
    fun goForward(): Boolean = false

    /** Reloads the current page. */
    fun reload(): Boolean = false

    /** Stops the current page load. */
    fun stop(): Boolean = false

    /**
     * Polls `document.readyState` until it reaches the target state for [state]
     * (`"load"`/`"complete"` → `complete`, `"domcontentloaded"`/`"interactive"` →
     * `interactive`) or [timeoutMs] elapses.
     *
     * @return `true` when the state was reached within the timeout.
     */
    fun waitForLoad(state: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean = false

    /**
     * Captures a screenshot of the rendered page. The headless WebView is drawn into
     * a bitmap; the PNG is written to [path] when non-blank (a directory-absolute
     * path) or to a timestamped file inside the app cache otherwise.
     *
     * @return the absolute path of the written PNG file, or `""` on failure.
     */
    fun screenshot(path: String = "", quality: Int = 80): String = ""

    /**
     * Scrolls the live page to absolute viewport coordinates `(x, y)`. Used to move the
     * page around (or back to top) without triggering lazy-load; see [scrollBottom].
     *
     * @return `true` when the scroll command reached the page.
     */
    fun scrollTo(x: Int, y: Int): Boolean = false

    /**
     * Scrolls the live page to the bottom of the document — the standard way to trigger
     * *lazy-load* / infinite-scroll sites (newsfeeds, galleries, search results) so
     * content that only renders near the viewport gets injected into the DOM.
     *
     * @return `true` when the scroll command reached the page.
     */
    fun scrollBottom(): Boolean = false

    /**
     * Returns the cookies of the current page as a **JSON array string** — each
     * entry `{ "name", "value", "domain", "path", "url" }`. The WebView
     * `CookieManager` is the exact store shared with the OkHttp scraper via
     * `AndroidCookieJar`, so this is automatically synced with `fetch()`.
     */
    fun getCookies(): String = "[]"

    /**
     * Sets a cookie from its JSON representation: `{ "name", "value", "url"?, "domain"?,
     * "path"? }`. When [cookieJson] has no `url` the current page URL is used (a blank
     * page fails). Persists immediately (`CookieManager.flush()`).
     *
     * @return `true` when the cookie was accepted.
     */
    fun setCookie(cookieJson: String): Boolean = false

    /** Clears every WebView cookie (also wiped from the OkHttp jar it shares). */
    fun clearCookies(): Boolean = false

    companion object {
        /** Default per-call timeout used by `open` / `waitForSelector`. */
        const val DEFAULT_TIMEOUT_MS: Long = 15_000L
    }
}
package app.rocat.scripting

import app.rocat.scripting.api.ScriptBrowserBridge

/**
 * App-side [ScriptBrowserBridge] **general-purpose** implementation (Tahap 25). It wires
 * the low-level script globals (`RoCatPage` and, through the JS polyfill, `RoCatBrowser`)
 * to the [HeadlessWebViewManager], making every call **synchronous for the script** while
 * the hidden WebView itself runs on the Android main thread.
 *
 * It exposes the full Puppeteer/Playwright-like command set:
 *
 *  - **Page basics** — `open`, `type`, `click`, `waitForSelector`, `evaluate`, `getHtml`, `close`
 *  - **Time control** — `sleep`, `waitForLoad`
 *  - **Page info** — `url`, `title`
 *  - **Navigation** — `goBack`, `goForward`, `reload`, `stop`
 *  - **Capture** — `screenshot`
 *  - **Cookies** — `getCookies`, `setCookie`, `clearCookies`
 *
 * Cookie commands go through the WebView `CookieManager`, which is exactly the store the
 * OkHttp scraper shares via `AndroidCookieJar` — so a login performed here is immediately
 * usable by `fetch()` and vice versa.
 *
 * Every method is wrapped in `runCatching` so a WebView failure (process death, missing
 * renderer, etc.) surfaces as `false`/`""`/`"[]"` instead of an uncaught exception that
 * would kill the script run.
 */
class RoCatBrowserBridge(private val manager: HeadlessWebViewManager) : ScriptBrowserBridge {

    override fun open(url: String, timeoutMs: Long): Boolean =
        runCatching { manager.open(url, timeoutMs) }.getOrDefault(false)

    override fun type(selector: String, text: String): Boolean =
        runCatching { manager.type(selector, text) }.getOrDefault(false)

    override fun click(selector: String): Boolean =
        runCatching { manager.click(selector) }.getOrDefault(false)

    override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
        runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)

    override fun evaluate(script: String): String =
        runCatching { manager.evaluate(script) }.getOrElse { "null" }

    override fun getHtml(): String =
        runCatching { manager.getHtml() }.getOrDefault("")

    override fun close() {
        runCatching { manager.close() }
    }

    // --- Tahap 25: general-purpose commands ---

    override fun sleep(ms: Long): Boolean =
        runCatching { manager.sleep(ms) }.getOrDefault(false)

    override fun url(): String =
        runCatching { manager.url() }.getOrDefault("")

    override fun title(): String =
        runCatching { manager.title() }.getOrDefault("")

    override fun goBack(): Boolean =
        runCatching { manager.goBack() }.getOrDefault(false)

    override fun goForward(): Boolean =
        runCatching { manager.goForward() }.getOrDefault(false)

    override fun reload(): Boolean =
        runCatching { manager.reload() }.getOrDefault(false)

    override fun stop(): Boolean =
        runCatching { manager.stop() }.getOrDefault(false)

    override fun waitForLoad(state: String, timeoutMs: Long): Boolean =
        runCatching { manager.waitForLoad(state, timeoutMs) }.getOrDefault(false)

    override fun screenshot(path: String, quality: Int): String =
        runCatching { manager.screenshot(path, quality) }.getOrDefault("")

    // --- Tahap 29: scrolling (Puppeteer-like) ---

    override fun scrollTo(x: Int, y: Int): Boolean =
        runCatching { manager.scrollTo(x, y) }.getOrDefault(false)

    override fun scrollBottom(): Boolean =
        runCatching { manager.scrollBottom() }.getOrDefault(false)

    override fun getCookies(): String =
        runCatching { manager.getCookies() }.getOrDefault("[]")

    override fun setCookie(cookieJson: String): Boolean =
        runCatching { manager.setCookie(cookieJson) }.getOrDefault(false)

    override fun clearCookies(): Boolean =
        runCatching { manager.clearCookies() }.getOrDefault(false)
} * the main-thread WebView answers (the main UI thread is never blocked).
 */
internal class RoCatPageBridge(
    private val cx: Context,
    private val scope: Scriptable,
    private val browser: ScriptBrowserBridge,
) : ScriptableObject() {

    init {
        put("open", this, PageFn { args ->
            browser.open(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
        })
        put("type", this, PageFn { args ->
            browser.type(pageArgString(args, 0), pageArgString(args, 1))
        })
        put("click", this, PageFn { args ->
            browser.click(pageArgString(args, 0))
        })
        put("waitForSelector", this, PageFn { args ->
            browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
        })
        put("evaluate", this, PageFn { args ->
            evaluateResult(browser.evaluate(pageArgString(args, 0)))
        })
        put("getHtml", this, PageFn { browser.getHtml() })
        put("close", this, PageFn { browser.close() })

        // --- Tahap 25: general-purpose automation commands (Playwright-like subset) ---
        // These are the low-level primitives the high-level `RoCatBrowser` JS polyfill
        // (RoCatBrowserWrapper.kt) is built on top of.
        put("sleep", this, PageFn { args ->
            browser.sleep(pageArgLong(args, 0, 0))
        })
        put("url", this, PageFn { browser.url() })
        put("title", this, PageFn { browser.title() })
        put("goBack", this, PageFn { browser.goBack() })
        put("goForward", this, PageFn { browser.goForward() })
        put("reload", this, PageFn { browser.reload() })
        put("stop", this, PageFn { browser.stop() })
        put("waitForLoad", this, PageFn { args ->
            browser.waitForLoad(pageArgString(args, 0, "load"), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
        })
        put("screenshot", this, PageFn { args ->
            browser.screenshot(pageArgString(args, 0), pageArgInt(args, 1, 80))
        })
        // Tahap 29: scroll commands (scrollBottom triggers lazy-load / infinite scroll).
        put("scrollTo", this, PageFn { args ->
            browser.scrollTo(pageArgInt(args, 0, 0), pageArgInt(args, 1, 0))
        })
        put("scrollBottom", this, PageFn { browser.scrollBottom() })
        put("getCookies", this, PageFn { browser.getCookies() })
        put("setCookie", this, PageFn { args ->
            browser.setCookie(pageArgString(args, 0))
        })
        put("clearCookies", this, PageFn { browser.clearCookies() })
    }

    override fun getClassName(): String = "RoCatPageBridge"

    /**
     * The app bridge returns WebView's raw `evaluateJavascript` value — a JSON-encoded

codex
Desain yang cocok dengan arsitektur saat ini adalah bridge native baru pada `ScriptBrowserBridge`, lalu `page.waitForResponse()` melakukan polling sinkron seperti API tunggu lainnya. Interceptor akan diinjeksikan setiap navigasi, menjaga referensi fungsi asli, mengkloning respons `fetch`, dan membatasi payload 1 MiB sebelum masuk ke map thread-safe.
exec
/bin/bash -lc "sed -n '230,340p' scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt; sed -n '420,465p' scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt; sed -n '1,170p' app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt; tail -n 100 app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt; sed -n '1,180p' scripting/rhino/src/test/java/app/rocat/scripting/rhino/RoCatBrowserAutomationTest.kt" in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:

    function Page() {
        this._url = "";
        this._title = "";
    }

    Page.prototype.goto = function (url, options) {
        options = options || {};
        var timeout = options.timeout || _defaultTimeout;
        if (!hasPage()) throw new Error("RoCatBrowser: no browser bridge available");
        var opened = RoCatPage.open(url, timeout);
        if (!opened) throw new Error("RoCatBrowser: failed to open " + url);
        this._url = RoCatPage.url() || url;
        this._title = RoCatPage.title() || "";
        var waitUntil = options.waitUntil;
        if (waitUntil === "domcontentloaded" || waitUntil === "interactive") {
            this.waitForLoad("domcontentloaded", timeout);
        } else if (waitUntil === "load" || waitUntil === "complete") {
            this.waitForLoad("load", timeout);
        }
        return this;
    };

    Page.prototype.type = function (selector, text, delay) {
        return this.locator(selector).type(text, delay);
    };

    Page.prototype.scrollTo = function (x, y) {
        if (!hasPage() || !RoCatPage.scrollTo) return false;
        return RoCatPage.scrollTo(
            (typeof x === "number") ? x : 0,
            (typeof y === "number") ? y : 0
        );
    };

    Page.prototype.scrollBottom = function () {
        if (!hasPage() || !RoCatPage.scrollBottom) return false;
        return RoCatPage.scrollBottom();
    };

    Page.prototype.waitForLoad = function (state, timeout) {
        state = state || "load";
        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
        var target = (state === "load" || state === "complete") ? "complete" : "interactive";
        var deadline = Date.now() + timeout;
        while (true) {
            var ready = hasPage() ? RoCatPage.evaluate("document.readyState") : "";
            if (ready === target || (target === "interactive" && ready === "complete")) return true;
            if (Date.now() >= deadline) throw new Error("Timeout waiting for load state: " + state);
            busyWait(100);
        }
    };

    Page.prototype.waitForTimeout = function (ms) {
        busyWait((typeof ms === "number") ? ms : 0);
        return this;
    };

    Page.prototype.content = function () {
        return hasPage() ? (RoCatPage.getHtml() || "") : "";
    };

    Page.prototype.evaluate = function (fn, args) {
        return evaluateInPage(fn, args, null);
    };

    Page.prototype.locator = function (selector) {
        return new Locator(selector, this);
    };

    Page.prototype.click = function (selector) {
        return this.locator(selector).click();
    };

    Page.prototype.fill = function (selector, text) {
        return this.locator(selector).fill(text);
    };

    Page.prototype.text = function (selector) {
        return this.locator(selector).text();
    };

    Page.prototype.getAttribute = function (selector, attr) {
        return this.locator(selector).getAttribute(attr);
    };

    Page.prototype.waitForSelector = function (selector, timeout) {
        return this.locator(selector).waitFor(timeout);
    };

    Page.prototype.url = function () {
        var live = hasPage() ? RoCatPage.url() : "";
        return live || this._url;
    };

    Page.prototype.title = function () {
        var live = hasPage() ? RoCatPage.title() : "";
        return live || this._title;
    };

    Page.prototype.goBack = function () {
        return hasPage() ? RoCatPage.goBack() : false;
    };

    Page.prototype.goForward = function () {
        return hasPage() ? RoCatPage.goForward() : false;
    };

    Page.prototype.reload = function () {
        return hasPage() ? RoCatPage.reload() : false;
    };
            return this.getInstance();
        },
        version: function () {
            return "RoCatBrowser v1.0.0";
        },
        setDefaultTimeout: function (timeout) {
            _defaultTimeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
        },
        hasBrowser: hasPage
    };
})();

// Tahap 29 — a Puppeteer-like convenience **global `page`** facade. Scripts that only
// need one live tab can drive it directly (page.goto / page.click / page.type / ...)
// without creating a Browser instance, mirroring Puppeteer's `page.*` API. It shares
// the singleton Browser from RoCatBrowser.getInstance() so `page` and `RoCatBrowser`
// always operate on the same underlying WebView.
var page = (function () {
    function current() {
        return RoCatBrowser.getInstance().page();
    }
    return {
        goto: function (url, options) { return current().goto(url, options); },
        waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
        waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
        click: function (selector) { return current().click(selector); },
        type: function (selector, text, delay) { return current().type(selector, text, delay); },
        fill: function (selector, text) { return current().fill(selector, text); },
        scrollTo: function (x, y) { return current().scrollTo(x, y); },
        scrollBottom: function () { return current().scrollBottom(); },
        evaluate: function (fn, args) { return current().evaluate(fn, args); },
        content: function () { return current().content(); },
        url: function () { return current().url(); },
        title: function () { return current().title(); },
        screenshot: function (options) { return current().screenshot(options); },
        cookies: function () { return current().cookies(); },
        setCookie: function (cookie) { return current().setCookie(cookie); },
        clearCookies: function () { return current().clearCookies(); },
        locator: function (selector) { return current().locator(selector); },
        goBack: function () { return current().goBack(); },
        goForward: function () { return current().goForward(); },
        reload: function () { return current().reload(); },
        stop: function () { return current().stop(); },
        close: function () { return current().close(); }
    };
})();
package app.rocat.scripting

import app.rocat.scripting.api.ScriptBrowserBridge

/**
 * App-side [ScriptBrowserBridge] (Tahap 23: dual-mode scraping engine). It wires the
 * script-facing `RoCatPage` global to the [HeadlessWebViewManager], making every call
 * synchronous for the script while the WebView itself runs on the Android main thread.
 *
 * Every method is wrapped in `runCatching` so a WebView failure (process death,
 * missing renderer, etc.) surfaces as `false`/`""` instead of an uncaught exception
 * that would kill the script run.
 */
class AppScriptBrowserBridge(private val manager: HeadlessWebViewManager) : ScriptBrowserBridge {

    override fun open(url: String, timeoutMs: Long): Boolean =
        runCatching { manager.open(url, timeoutMs) }.getOrDefault(false)

    override fun type(selector: String, text: String): Boolean =
        runCatching { manager.type(selector, text) }.getOrDefault(false)

    override fun click(selector: String): Boolean =
        runCatching { manager.click(selector) }.getOrDefault(false)

    override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
        runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)

    override fun evaluate(script: String): String =
        runCatching { manager.evaluate(script) }.getOrElse { "null" }

    override fun getHtml(): String =
        runCatching { manager.getHtml() }.getOrDefault("")

    override fun close() {
        runCatching { manager.close() }
    }
}
            try {
                action(wv)
                ref.set(true)
            } catch (_: Throwable) {
                ref.set(false)
            }
            latch.countDown()
        }
        if (!latch.await(DEFAULT_EVAL_TIMEOUT_MS, TimeUnit.MILLISECONDS)) return false
        return ref.get()
    }

    /** Forces a frame for the detached WebView so scroll/SPA state reaches Chromium. */
    private fun forceCompositorFrame(wv: WebView) {
        val latch = CountDownLatch(1)
        onMain {
            try {
                wv.invalidate()
                drawCompositorFrame(wv)
            } finally {
                latch.countDown()
            }
        }
        awaitLatch(latch)
    }

    private fun drawCompositorFrame(wv: WebView) {
        if (wv.width <= 0 || wv.height <= 0) return
        var scratch: Bitmap? = null
        try {
            scratch = Bitmap.createBitmap(wv.width, wv.height, Bitmap.Config.ARGB_8888)
            wv.draw(Canvas(scratch))
        } catch (_: Throwable) {
            // Best-effort: invalidation and the following DOM poll can still settle it.
        } finally {
            scratch?.recycle()
        }
    }

    /** Runs [js] on the main thread and returns the callback value, or null on failure. */
    private fun evaluateJs(js: String, timeoutMs: Long): String? {
        val wv = webView ?: ensureWebView() ?: return null
        val latch = CountDownLatch(1)
        val ref = AtomicReference<String?>()
        onMain {
            try {
                wv.evaluateJavascript(js) { value ->
                    ref.set(value)
                    latch.countDown()
                }
            } catch (_: Throwable) {
                ref.set(null)
                latch.countDown()
            }
        }
        if (!latch.await(timeoutMs, TimeUnit.MILLISECONDS)) return null
        return ref.get()
    }

    /** Decodes WebView's JSON-encoded callback string back into a plain string. */
    private fun unquoteJson(value: String): String = try {
        JSONTokener(value).nextValue() as? String ?: value
    } catch (_: Exception) {
        value
    }

    private companion object {
        val wholeDocumentDrawingEnabled = AtomicBoolean(false)
        const val DEFAULT_EVAL_TIMEOUT_MS = 5_000L
        const val POLL_INTERVAL_MS = 150L
        const val SCREENSHOT_DIR = "browser_screenshots"
        const val DEFAULT_VIEWPORT_WIDTH = 1366
        const val DEFAULT_VIEWPORT_HEIGHT = 768
        const val MAX_SCREENSHOT_HEIGHT = 5_000
        const val TAP_GAP_MS = 80L
        const val TAP_SETTLE_MS = 120L
        const val POST_TAP_SETTLE_MS = 250L
        const val VIEWPORT_SETTLE_TRIES = 3
        const val MIN_TAP_SCALE = 0.1f
        const val MAX_TAP_SCALE = 10f

        fun enableWholeDocumentDrawing() {
            if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
            try {
                WebView.enableSlowWholeDocumentDraw()
            } catch (_: Throwable) {
                // Must be called before the first WebView; older/already-started providers may reject it.
            }
        }
    }
}

/** Quotes a Kotlin string for safe injection into a JS string literal. */
private fun jsQuote(value: String): String =
    "\"" + value
        .replace("\\", "\\\\")
        .replace("\"", "\\\"")
        .replace("\n", "\\n")
        .replace("\r", "\\r")
        .replace("\t", "\\t") + "\""
package app.rocat.scripting.rhino

import app.rocat.scripting.api.FetchResult
import app.rocat.scripting.api.ScriptBrowserBridge
import app.rocat.scripting.api.ScriptResult
import app.rocat.scripting.api.model.DefaultScriptEnvironment
import app.rocat.scripting.api.model.Script
import kotlinx.coroutines.runBlocking
import okhttp3.OkHttpClient
import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Tahap 25 — verifies the general-purpose `RoCatBrowser` automation polyfill (a
 * Playwright/Puppeteer-like Browser / Page / Locator API layered on top of the
 * low-level `RoCatPage` native bridge):
 *
 *  1. `RoCatBrowser` is only registered when a [ScriptBrowserBridge] is present.
 *  2. `launch()` → `newPage()` → `goto()` round-trips to the native bridge and the
 *     page's `title()` / `url()` come back from the live DOM.
 *  3. Locator operations (`exists`, `fill`, `click`, `text`, `all`, `clickAll`,
 *     `waitFor`, `getAttribute`) drive `RoCatPage.evaluate` with serialised args.
 *  4. `evaluate(fn, args)` executes an inline function and returns native JS values.
 *  5. Cookies / screenshot / navigation / time-control commands forward correctly.
 *  6. Missing elements and timeouts surface as script-catchable errors / `{success:false}`.
 */
class RoCatBrowserAutomationTest {

    private val engine = RhinoScriptEngine(OkHttpClient.Builder().build())

    private fun script(source: String) = Script(id = "browser", name = "browser", source = source)

    /**
     * A tiny fake browser whose `evaluate` answers from canned JSON strings detected by
     * markers in the generated script — the wrapper always sends
     * `(function (…args…) {…})("…", …)` so the selector literal is searchable.
     */
    private class FakeBrowser : ScriptBrowserBridge {
        val calls = mutableListOf<String>()
        var currentUrl = "https://example.com/login"
        var readyState = "complete"
        var hasElement = true

        override fun open(url: String, timeoutMs: Long): Boolean {
            calls += "open:$url:$timeoutMs"
            currentUrl = url
            return true
        }

        override fun type(selector: String, text: String): Boolean {
            calls += "type:$selector:$text"
            return true
        }

        override fun click(selector: String): Boolean {
            calls += "click:$selector"
            return true
        }

        override fun waitForSelector(selector: String, timeoutMs: Long): Boolean {
            calls += "wait:$selector:$timeoutMs"
            return true
        }

        override fun evaluate(script: String): String {
            calls += "eval"
            return when {
                script.contains("#missing") ->
                    "{\"success\":false,\"error\":\"Element not found: #missing\"}"
                script.contains("document.title") -> "\"Fake Title\""
                script.contains("location.href") -> "\"$currentUrl\""
                script.contains("readyState") -> "\"$readyState\""
                script.contains("outerHTML") -> "\"<html><body>Rendered</body></html>\""
                script.contains("index: i") ->
                    "[{\"index\":0,\"success\":true},{\"index\":1,\"success\":true}]"
                script.contains("querySelectorAll") -> "[" +
                    "{\"text\":\"One\",\"html\":\"<li>One</li>\",\"attributes\":{}}," +
                    "{\"text\":\"Two\",\"html\":\"<li>Two</li>\",\"attributes\":{}}]"
                script.contains("getBoundingClientRect") ->
                    "{\"x\":0,\"y\":0,\"width\":100,\"height\":50,\"top\":0,\"right\":100,\"bottom\":50,\"left\":0}"
                script.contains("getAttribute") -> "\"item-1\""
                script.contains("scrollIntoView") -> "true"
                script.contains("textContent") -> "\"Hello World\""
                script.contains("sum") -> "{\"sum\":42}"
                script.contains("querySelector") -> if (hasElement) "true" else "false"
                else -> "true"
            }
        }

        override fun getHtml(): String {
            calls += "getHtml"
            return "<html><body>Rendered</body></html>"
        }

        override fun sleep(ms: Long): Boolean {
            calls += "sleep:$ms"
            return true
        }

        override fun url(): String {
            calls += "url"
            return currentUrl
        }

        override fun title(): String {
            calls += "title"
            return "Fake Title"
        }

        override fun goBack(): Boolean {
            calls += "back"
            return true
        }

        override fun goForward(): Boolean {
            calls += "forward"
            return true
        }

        override fun reload(): Boolean {
            calls += "reload"
            return true
        }

        override fun waitForLoad(state: String, timeoutMs: Long): Boolean {
            calls += "waitForLoad:$state:$timeoutMs"
            return true
        }

        override fun scrollTo(x: Int, y: Int): Boolean {
            calls += "scrollTo:$x:$y"
            return true
        }

        override fun scrollBottom(): Boolean {
            calls += "scrollBottom"
            return true
        }

        override fun screenshot(path: String, quality: Int): String {
            calls += "screenshot:$path:$quality"
            return "/cache/browser_screenshots/shot_1.png"
        }

        override fun getCookies(): String {
            calls += "getCookies"
            return "[]"
        }

        override fun setCookie(cookieJson: String): Boolean {
            calls += "setCookie:$cookieJson"
            return true
        }

        override fun clearCookies(): Boolean {
            calls += "clearCookies"
            return true
        }

        override fun close() {
            calls += "close"
        }
    }

    private fun env(browser: ScriptBrowserBridge) = DefaultScriptEnvironment(
        fetchImpl = { _, _, _, _ -> FetchResult(200, emptyMap(), "") },
        browser = browser,
    )

    @Test
    fun `rocatorbrowser global is only registered when a browser bridge is present`() = runBlocking {
        val plainSource = "function main() { return typeof RoCatBrowser; }"

        val plainEnv = DefaultScriptEnvironment(
            fetchImpl = { _, _, _, _ -> FetchResult(200, emptyMap(), "") },
        )
        val plain = engine.execute(script(plainSource), plainEnv)
        assertEquals(ScriptResult.Success("undefined"), plain)


codex
Saya akan mengubah lima lapisan yang saling terkait: manager WebView, kontrak bridge, dua implementasi bridge app, bridge Rhino, dan wrapper `page`, kemudian menambah tes wrapper serta log Fase 42. Respons dibatasi 1 MiB per body dan jumlah entri dibatasi agar sesi panjang tidak menumpuk tanpa batas.
apply patch
patch: completed
/home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
/home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
/home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
/home/runner/work/rocat-app/rocat-app/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
/home/runner/work/rocat-app/rocat-app/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
/home/runner/work/rocat-app/rocat-app/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

exec
/bin/bash -lc 'bash ./gradlew assembleDebug' in /home/runner/work/rocat-app/rocat-app
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

codex
Implementasi inti sudah masuk. Build sedang berjalan; setelah selesai saya akan memperbaiki error kompilasi bila ada, lalu menambahkan catatan `ai_memory` dan indeks sesuai kewajiban prompt.
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

 succeeded in 63997ms:
Starting a Gradle Daemon (subsequent builds will be faster)
Type-safe project accessors is an incubating feature.
> Task :data:preBuild UP-TO-DATE
> Task :domain:preBuild UP-TO-DATE
> Task :app:preBuild UP-TO-DATE
> Task :core:common:preBuild UP-TO-DATE
> Task :app:preDebugBuild UP-TO-DATE
> Task :data:preDebugBuild UP-TO-DATE
> Task :core:common:preDebugBuild UP-TO-DATE
> Task :domain:preDebugBuild UP-TO-DATE
> Task :app:mergeDebugNativeDebugMetadata NO-SOURCE
> Task :app:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :domain:generateDebugResValues FROM-CACHE
> Task :data:generateDebugResValues FROM-CACHE
> Task :core:common:generateDebugResValues FROM-CACHE
> Task :domain:generateDebugResources FROM-CACHE
> Task :core:common:generateDebugResources FROM-CACHE
> Task :data:generateDebugResources FROM-CACHE
> Task :data:packageDebugResources FROM-CACHE
> Task :core:viewmodel:preBuild UP-TO-DATE
> Task :core:viewmodel:preDebugBuild UP-TO-DATE
> Task :core:viewmodel:generateDebugResValues FROM-CACHE
> Task :domain:packageDebugResources FROM-CACHE
> Task :core:common:packageDebugResources FROM-CACHE
> Task :scripting:rhino:preBuild UP-TO-DATE
> Task :scripting:api:preBuild UP-TO-DATE
> Task :scripting:rhino:preDebugBuild UP-TO-DATE
> Task :scripting:api:preDebugBuild UP-TO-DATE
> Task :core:viewmodel:generateDebugResources FROM-CACHE
> Task :scripting:api:generateDebugResValues FROM-CACHE
> Task :scripting:rhino:generateDebugResValues FROM-CACHE
> Task :core:viewmodel:packageDebugResources FROM-CACHE
> Task :scripting:rhino:generateDebugResources FROM-CACHE
> Task :scripting:api:generateDebugResources FROM-CACHE
> Task :scripting:rhino:packageDebugResources FROM-CACHE
> Task :scripting:api:packageDebugResources FROM-CACHE
> Task :data:writeDebugAarMetadata
> Task :core:common:writeDebugAarMetadata
> Task :domain:writeDebugAarMetadata
> Task :scripting:api:writeDebugAarMetadata
> Task :core:viewmodel:writeDebugAarMetadata
> Task :scripting:rhino:writeDebugAarMetadata
> Task :core:common:extractDeepLinksDebug FROM-CACHE
> Task :data:extractDeepLinksDebug FROM-CACHE
> Task :domain:extractDeepLinksDebug FROM-CACHE
> Task :domain:processDebugManifest FROM-CACHE
> Task :data:processDebugManifest FROM-CACHE
> Task :core:common:processDebugManifest FROM-CACHE
> Task :scripting:rhino:extractDeepLinksDebug FROM-CACHE
> Task :scripting:api:extractDeepLinksDebug FROM-CACHE
> Task :core:viewmodel:extractDeepLinksDebug FROM-CACHE
> Task :core:viewmodel:processDebugManifest FROM-CACHE
> Task :scripting:rhino:processDebugManifest FROM-CACHE
> Task :scripting:api:processDebugManifest FROM-CACHE
> Task :data:compileDebugLibraryResources FROM-CACHE
> Task :core:common:parseDebugLocalResources FROM-CACHE
> Task :domain:parseDebugLocalResources FROM-CACHE
> Task :data:parseDebugLocalResources FROM-CACHE
> Task :scripting:rhino:parseDebugLocalResources FROM-CACHE
> Task :scripting:api:parseDebugLocalResources FROM-CACHE
> Task :domain:compileDebugLibraryResources FROM-CACHE
> Task :core:viewmodel:compileDebugLibraryResources FROM-CACHE
> Task :scripting:api:compileDebugLibraryResources FROM-CACHE
> Task :scripting:rhino:compileDebugLibraryResources FROM-CACHE
> Task :data:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :domain:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :scripting:api:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :core:viewmodel:parseDebugLocalResources FROM-CACHE
> Task :core:common:generateDebugRFile FROM-CACHE
> Task :core:common:compileDebugLibraryResources FROM-CACHE
> Task :core:common:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :scripting:api:javaPreCompileDebug FROM-CACHE
> Task :scripting:api:generateDebugRFile FROM-CACHE
> Task :domain:generateDebugRFile FROM-CACHE
> Task :scripting:rhino:generateDebugRFile FROM-CACHE
> Task :core:viewmodel:generateDebugRFile FROM-CACHE
> Task :domain:javaPreCompileDebug FROM-CACHE
> Task :scripting:rhino:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :data:generateDebugRFile FROM-CACHE
> Task :data:javaPreCompileDebug FROM-CACHE
> Task :core:viewmodel:checkKotlinGradlePluginConfigurationErrors SKIPPED
> Task :scripting:rhino:javaPreCompileDebug FROM-CACHE
> Task :app:dataBindingMergeDependencyArtifactsDebug
> Task :app:generateDebugResValues FROM-CACHE
> Task :app:generateDebugResources FROM-CACHE
> Task :core:viewmodel:compileDebugKotlin FROM-CACHE
> Task :data:mergeDebugShaders
> Task :data:compileDebugShaders NO-SOURCE
> Task :data:generateDebugAssets UP-TO-DATE
> Task :core:viewmodel:javaPreCompileDebug FROM-CACHE
> Task :core:viewmodel:compileDebugJavaWithJavac NO-SOURCE
> Task :data:packageDebugAssets
> Task :domain:mergeDebugShaders
> Task :domain:compileDebugShaders NO-SOURCE
> Task :domain:generateDebugAssets UP-TO-DATE
> Task :core:common:compileDebugKotlin FROM-CACHE
> Task :core:common:javaPreCompileDebug FROM-CACHE
> Task :core:common:compileDebugJavaWithJavac NO-SOURCE
> Task :domain:packageDebugAssets
> Task :core:viewmodel:mergeDebugShaders
> Task :core:common:mergeDebugShaders
> Task :core:common:compileDebugShaders NO-SOURCE
> Task :core:common:generateDebugAssets UP-TO-DATE
> Task :core:common:packageDebugAssets
> Task :core:viewmodel:compileDebugShaders NO-SOURCE
> Task :core:viewmodel:generateDebugAssets UP-TO-DATE
> Task :core:viewmodel:packageDebugAssets
> Task :scripting:api:mergeDebugShaders
> Task :scripting:api:compileDebugShaders NO-SOURCE
> Task :scripting:api:generateDebugAssets UP-TO-DATE
> Task :scripting:api:packageDebugAssets
> Task :core:viewmodel:bundleLibCompileToJarDebug
> Task :scripting:rhino:mergeDebugShaders
> Task :core:common:bundleLibCompileToJarDebug
> Task :scripting:rhino:compileDebugShaders NO-SOURCE
> Task :scripting:rhino:generateDebugAssets UP-TO-DATE
> Task :core:viewmodel:bundleLibRuntimeToJarDebug
> Task :core:common:bundleLibRuntimeToJarDebug
> Task :scripting:rhino:packageDebugAssets
> Task :app:mergeDebugResources FROM-CACHE
> Task :core:common:processDebugJavaRes
> Task :core:viewmodel:processDebugJavaRes
> Task :app:dataBindingGenBaseClassesDebug FROM-CACHE
> Task :core:viewmodel:bundleLibRuntimeToDirDebug
> Task :app:generateDebugBuildConfig FROM-CACHE
> Task :core:common:bundleLibRuntimeToDirDebug
> Task :data:mergeDebugJniLibFolders
> Task :domain:mergeDebugJniLibFolders
> Task :domain:mergeDebugNativeLibs NO-SOURCE
> Task :data:mergeDebugNativeLibs NO-SOURCE
> Task :data:copyDebugJniLibsProjectOnly
> Task :domain:copyDebugJniLibsProjectOnly
> Task :core:common:mergeDebugJniLibFolders
> Task :core:viewmodel:mergeDebugJniLibFolders
> Task :core:common:mergeDebugNativeLibs NO-SOURCE
> Task :core:common:copyDebugJniLibsProjectOnly
> Task :core:viewmodel:mergeDebugNativeLibs NO-SOURCE
> Task :app:checkDebugAarMetadata
> Task :core:viewmodel:copyDebugJniLibsProjectOnly
> Task :data:stripDebugDebugSymbols NO-SOURCE
> Task :scripting:rhino:mergeDebugJniLibFolders
> Task :data:copyDebugJniLibsProjectAndLocalJars
> Task :scripting:rhino:mergeDebugNativeLibs NO-SOURCE
> Task :app:mapDebugSourceSetPaths
> Task :data:extractDeepLinksForAarDebug FROM-CACHE
> Task :scripting:rhino:copyDebugJniLibsProjectOnly
> Task :domain:stripDebugDebugSymbols NO-SOURCE
> Task :data:prepareDebugArtProfile
> Task :app:createDebugCompatibleScreenManifests
> Task :domain:copyDebugJniLibsProjectAndLocalJars
> Task :domain:extractDeepLinksForAarDebug FROM-CACHE
> Task :app:extractDeepLinksDebug FROM-CACHE
> Task :domain:prepareDebugArtProfile
> Task :data:prepareLintJarForPublish
> Task :core:common:stripDebugDebugSymbols NO-SOURCE
> Task :domain:prepareLintJarForPublish
> Task :core:viewmodel:stripDebugDebugSymbols NO-SOURCE
> Task :core:common:copyDebugJniLibsProjectAndLocalJars
> Task :core:viewmodel:copyDebugJniLibsProjectAndLocalJars
> Task :app:processDebugMainManifest FROM-CACHE
> Task :app:processDebugManifest FROM-CACHE
> Task :app:processDebugManifestForPackage FROM-CACHE
> Task :app:processDebugResources FROM-CACHE
> Task :app:javaPreCompileDebug FROM-CACHE
> Task :app:mergeDebugShaders
> Task :app:compileDebugShaders NO-SOURCE
> Task :app:generateDebugAssets UP-TO-DATE
> Task :app:mergeDebugAssets
> Task :app:compressDebugAssets FROM-CACHE
> Task :app:desugarDebugFileDependencies FROM-CACHE
> Task :app:checkDebugDuplicateClasses
> Task :core:common:extractDebugAnnotations FROM-CACHE
> Task :scripting:api:mergeDebugJniLibFolders
> Task :scripting:api:mergeDebugNativeLibs NO-SOURCE
> Task :scripting:api:copyDebugJniLibsProjectOnly
> Task :core:common:extractDeepLinksForAarDebug FROM-CACHE
> Task :core:common:mergeDebugGeneratedProguardFiles
> Task :core:common:mergeDebugConsumerProguardFiles
> Task :core:common:prepareDebugArtProfile
> Task :core:common:prepareLintJarForPublish
> Task :core:viewmodel:extractDebugAnnotations FROM-CACHE
> Task :core:viewmodel:extractDeepLinksForAarDebug FROM-CACHE
> Task :core:viewmodel:mergeDebugGeneratedProguardFiles
> Task :core:viewmodel:mergeDebugConsumerProguardFiles
> Task :core:viewmodel:prepareDebugArtProfile
> Task :core:viewmodel:prepareLintJarForPublish
> Task :core:common:mergeDebugJavaResource
> Task :core:viewmodel:mergeDebugJavaResource
> Task :core:common:syncDebugLibJars FROM-CACHE
> Task :core:viewmodel:syncDebugLibJars FROM-CACHE
> Task :core:viewmodel:bundleDebugAar
> Task :core:viewmodel:assembleDebug
> Task :scripting:api:stripDebugDebugSymbols NO-SOURCE
> Task :core:common:bundleDebugAar
> Task :core:common:assembleDebug
> Task :scripting:api:copyDebugJniLibsProjectAndLocalJars
> Task :scripting:api:extractDeepLinksForAarDebug FROM-CACHE
> Task :scripting:rhino:stripDebugDebugSymbols NO-SOURCE
> Task :scripting:api:prepareDebugArtProfile
> Task :scripting:rhino:copyDebugJniLibsProjectAndLocalJars
> Task :scripting:rhino:extractDeepLinksForAarDebug FROM-CACHE
> Task :scripting:api:prepareLintJarForPublish
> Task :scripting:rhino:prepareDebugArtProfile
> Task :scripting:rhino:prepareLintJarForPublish
> Task :app:mergeExtDexDebug FROM-CACHE
> Task :app:mergeDebugJniLibFolders
> Task :app:mergeDebugNativeLibs
> Task :app:validateSigningDebug
> Task :app:writeDebugAppMetadata
> Task :app:writeDebugSigningConfigVersions

> Task :app:stripDebugDebugSymbols
Unable to strip the following libraries, packaging them as they are: libandroidx.graphics.path.so.

> Task :scripting:api:compileDebugKotlin
> Task :scripting:api:compileDebugJavaWithJavac NO-SOURCE
> Task :scripting:api:bundleLibCompileToJarDebug
> Task :scripting:api:bundleLibRuntimeToJarDebug
> Task :scripting:api:processDebugJavaRes
> Task :scripting:api:bundleLibRuntimeToDirDebug
> Task :scripting:api:extractDebugAnnotations
> Task :scripting:api:mergeDebugGeneratedProguardFiles
> Task :scripting:api:mergeDebugConsumerProguardFiles
> Task :scripting:api:mergeDebugJavaResource
> Task :scripting:api:syncDebugLibJars
> Task :scripting:api:bundleDebugAar
> Task :scripting:api:assembleDebug
> Task :domain:compileDebugKotlin
> Task :domain:compileDebugJavaWithJavac NO-SOURCE
> Task :domain:bundleLibCompileToJarDebug
> Task :domain:bundleLibRuntimeToJarDebug
> Task :domain:processDebugJavaRes
> Task :domain:bundleLibRuntimeToDirDebug
> Task :domain:extractDebugAnnotations
> Task :domain:mergeDebugGeneratedProguardFiles
> Task :domain:mergeDebugConsumerProguardFiles
> Task :domain:mergeDebugJavaResource
> Task :domain:syncDebugLibJars FROM-CACHE
> Task :domain:bundleDebugAar
> Task :domain:assembleDebug

> Task :scripting:rhino:compileDebugKotlin
w: file:///home/runner/work/rocat-app/rocat-app/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RhinoScriptEngine.kt:917:41 Comparison with '==' might not be intended because 'kotlin.Float' and 'kotlin.Double' are incompatible types.

> Task :scripting:rhino:compileDebugJavaWithJavac NO-SOURCE
> Task :scripting:rhino:bundleLibCompileToJarDebug
> Task :scripting:rhino:bundleLibRuntimeToJarDebug
> Task :scripting:rhino:processDebugJavaRes
> Task :scripting:rhino:bundleLibRuntimeToDirDebug
> Task :scripting:rhino:extractDebugAnnotations
> Task :scripting:rhino:mergeDebugGeneratedProguardFiles
> Task :scripting:rhino:mergeDebugConsumerProguardFiles
> Task :scripting:rhino:mergeDebugJavaResource
> Task :scripting:rhino:syncDebugLibJars
> Task :scripting:rhino:bundleDebugAar
> Task :scripting:rhino:assembleDebug
> Task :data:compileDebugKotlin
> Task :data:compileDebugJavaWithJavac NO-SOURCE
> Task :data:bundleLibCompileToJarDebug
> Task :data:bundleLibRuntimeToJarDebug
> Task :data:processDebugJavaRes
> Task :data:bundleLibRuntimeToDirDebug
> Task :data:extractDebugAnnotations
> Task :data:mergeDebugGeneratedProguardFiles
> Task :data:mergeDebugConsumerProguardFiles
> Task :data:mergeDebugJavaResource
> Task :data:syncDebugLibJars FROM-CACHE
> Task :data:bundleDebugAar
> Task :data:assembleDebug
> Task :app:mergeLibDexDebug
> Task :app:kspDebugKotlin

> Task :app:compileDebugKotlin
w: file:///home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/ui/browser/BrowserScreen.kt:663:54 'val Icons.Filled.OpenInNew: ImageVector' is deprecated. Use the AutoMirrored version at Icons.AutoMirrored.Filled.OpenInNew.
w: file:///home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/ui/detail/ScriptDetailScreen.kt:230:43 'val Icons.Filled.Undo: ImageVector' is deprecated. Use the AutoMirrored version at Icons.AutoMirrored.Filled.Undo.
w: file:///home/runner/work/rocat-app/rocat-app/app/src/main/java/app/rocat/ui/detail/ScriptDetailScreen.kt:233:43 'val Icons.Filled.Redo: ImageVector' is deprecated. Use the AutoMirrored version at Icons.AutoMirrored.Filled.Redo.

> Task :app:compileDebugJavaWithJavac
> Task :app:dexBuilderDebug
> Task :app:mergeDebugGlobalSynthetics FROM-CACHE
> Task :app:processDebugJavaRes
> Task :app:mergeProjectDexDebug
> Task :app:mergeDebugJavaResource
> Task :app:packageDebug
> Task :app:createDebugApkListingFileRedirect
> Task :app:assembleDebug
gradle/actions: Writing build results to /home/runner/work/_temp/.gradle-actions/build-results/__run_4-1787730874548.json

BUILD SUCCESSFUL in 1m 3s
212 actionable tasks: 129 executed, 83 from cache

diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

codex
Build debug berhasil tanpa error. Saya sedang menutup pekerjaan dengan dokumentasi Fase 42 dan satu pembaruan indeks; catatan ini akan mencantumkan API, batas memori, serta hasil verifikasi build.
exec
/bin/bash -lc 'head -n 30 ai_memory/00_INDEX.md' in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:
# MEMORY INDEX — rocat-app

- **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.

- **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
- **Fase 39 (2026-08-24)**: [Sinkronisasi Coil3 dan loading preview](task_20260824_1615_Fase_39.md) — Coil memakai `NetworkHelper.client()` global secara lazy; cache key grid memasukkan headers; indikator loading/error preview aktif.
- **Tahap 35 (2026-08-18)**: [Sistem pengaturan skrip & canvas UI](task_20260818_tahap35_settings_system.md) — metadata `@settings`, `RoCat.settings`, Room v2, halaman pengaturan per skrip, komponen canvas baru (text/divider/checkbox/toggle/dropdown/number/colorpicker/textarea/autocomplete/group/layout + flex).
- **Tahap 31 (2026-08-18)**: [UI/media fixes](task_20260818_0000_tahap31_ui_media_fixes.md) — isolasi loading tombol, fullscreen media, SAF writer hardening, template copy.

Proyek: `rocat-app` — Android app modular ala Mihon untuk mengelola & menjalankan custom userscript (Rhino engine). Workspace: `rocat-app/` di root repo ini.

## Status Proyek Terkini
- **Fase 41 SELESAI** (2026-08-26): Video preview/player memakai aspect ratio dinamis; fullscreen adaptif portrait/landscape dan dialog immersive. `assembleDebug` sukses; Spotless tidak tersedia.
- **Fase 40 SELESAI** (2026-08-25): Reader memakai software bitmap dan decode berbasis constraint untuk menghindari limit tekstur OpenGL pada halaman webtoon panjang; memory cache Coil dibatasi 25% RAM dan disk cache 100 MiB. Build debug berhasil; Spotless tidak tersedia.
- **Fase 39 SELESAI** (2026-08-24): Sinkronisasi jaringan Coil3 dengan `NetworkHelper.client()` dan perbaikan indikator loading preview. Build debug berhasil; formatter Spotless tidak tersedia.
- **Tahap 35 SELESAI** (2026-08-18): Sistem Pengaturan Skrip & Canvas UI. **(35.1) Metadata `@settings`** — `ScriptMetadataParser.parseSetting` membaca `// @settings key: type: default=..., label=..., min=..., max=..., step=..., options=a,b,c, maxLength=..., rows=...` (regex `settingLine`/`parameterTokens`); tipe `string|password|boolean|number|select|multiline|color|email` (`ScriptSettingType`, fallback string); `ScriptSetting` (domain) + `ScriptMetadata.settings` + `normalizedDefault`. **(35.2) `RoCat.settings` API** — `ScriptSettingsBridge` (default no-op di `scripting:api`), `ScriptEnvironment.settings`; Rhino `createScope` meng-inject `__rocatSettingsJson__` (snapshot ter-typed boolean/number/string via `typedSettingsJson`/`coerceToJsonValue`) + `__rocatSettingsBridge__` + `RO_CAT_SETTINGS_WRAPPER_JS` → `RoCat.settings` (get/getAll/set/setTemp/getTemp), `RoCat.onSettingsChanged(fn)`, `RoCat.saveHistory/clearHistory(key,value)`, `RoCat.openSettings()`; gating `typeof RoCat.settings === "undefined"` tanpa bridge. **(35.3) Persistence** — Room v2 (`MIGRATION_1_2`): tabel `script_settings` (PK script_id,key) + `script_input_history` (indeks, `GROUP BY value ORDER BY MAX(timestamp) DESC`); entity/DAO baru + `ScriptSettingsManager` (load/setValue validasi clamp/export/import/history/deleteAll + `bridgeFor` + fallback `ScriptDefaultSettings` autoRun/timeout/downloadPath dst di belakang deklarasi skrip). **(35.4) Halaman Pengaturan** — `ScriptSettingsScreen`/`ScriptSettingsViewModel` (route `Screen.ScriptSettings(scriptId)`, ikon settings di `ScriptCanvasScreen`, navigasi dari `RoCat.openSettings()` via `settingsOpenRequests`); kontrol per tipe (Switch/dropdown/color picker/number min-max-step/password/email/textarea); Reset/Export/Import JSON; i18n EN/ID baru. **(35.5) Komponen Canvas** — `ScriptUIComponent` +`Text/Divider/Checkbox/Toggle/Dropdown/Number/ColorPicker/TextArea/Autocomplete/Group/Layout` + properti `flex` (abstract, `flexWeight`); `ScriptUiBridge` +11 method no-op; Rhino `RoCatUiBridge` +`argStringArray`/`argDouble`; `RoCatCoreWrapper.renderOne` dispatch tipe baru; `parseComponents`/`parseComponent` descriptor JSON rekursif; canvas VM `replaceInTree`/`collectFields` rekursif + `loadHistory`/`saveAutocompleteHistory`; `ScriptsViewModel.delete` + `deleteAll`. Fix: regex parser di luar `companion object` dalam `object`, `argStringArray` id array JS = `Int`, `MenuAnchorType` top-level material3, `MaterialTheme.colorScheme` di luar `remember`. Build `:app:assembleDebug` SUCCESS; test domain 11/11, rhino 67 (64 hijau; 3 gagal pre-existing `FixedTestscrapeScraperTest` fixture hilang); `RoCatTahap35Test` baru 8/8.
- **Tahap 33 SELESAI** (2026-08-18): Sinkronisasi klik SPA & screenshot full-page headless. `HeadlessWebViewManager.screenshot()` membaca `scrollHeight`, me-layout WebView sementara ke tinggi konten dinamis (maks. **5000 px**, anti-OOM), mengaktifkan `enableSlowWholeDocumentDraw()` best-effort untuk tile di luar viewport, lalu memulihkan viewport/scroll. Klik native kini memakai koordinat dokumen (`rect + window.scrollX/Y`) → scroll fisik → konversi kembali ke viewport → CSS-px ke view-px; compositor dipaksa redraw sebelum/sesudah tap + settle state SPA. `capcut_test.js` v10 membersihkan marker lama dan menunggu form via `waitForSelector`; docs §7.2b ditambahkan. API `RoCatPage` tidak berubah.
- **Tahap 30 SELESAI** (2026-08-17): Perbaikan Interaksi Event DOM (SPA) pada Headless WebView — **klik native touch**. Analisis akar: `el.click()`/`dispatchEvent` menghasilkan event **untrusted** (`isTrusted=false`) yang diabaikan SPA (React/Vue event delegation) & anti-bot (CapCut/hCaptcha/Cloudflare). **(30.1)** `HeadlessWebViewManager.click(selector)` di-upgrade: `measureIfNeeded` (layout WebView headless ke viewport default 1366×768), `elementBounds` (`getBoundingClientRect` + `scrollIntoView`), `dispatchNativeTap` (**`MotionEvent.ACTION_DOWN`→gap→`ACTION_UP`** via `WebView.dispatchTouchEvent`, source `SOURCE_TOUCHSCREEN`, di pusat elemen → halaman melihat `isTrusted=true` touch/pointer/mouse/click), fallback `clickViaJs` (urutan pointer/mouse lama) bila elemen tak ditemukan / tap ditolak; semua helper anti-crash (`catch Throwable`, `runCatching`). **(30.2)** `RoCatBrowserWrapper` (JS polyfill): `Locator.prototype.click` kini memprioritaskan **`RoCatPage.click(selector)`** (bridge native) bila tersedia → `page.click` & `locator.click` memakai tap native, fallback JS hanya tanpa bridge. **(30.3)** `capcut_test.js` v4→v9 ditulis ulang: hapus rantai klik `evaluate` rumit + pseudo-selector `:has-text()` → tandai elemen `data-rocat-click="1"` lalu **`page.click('[data-rocat-click="1"]')`** langsung; verifikasi email/password + screenshot sebelum/sesudah tetap. **(30.4)** `DOCS_SCRIPTING.md` Bab 7 + **§7.2a Klik Native Touch (Tahap 30)** (kenapa JS click gagal + alur MotionEvent). **(30.5)** `CapCutNativeClickTest.kt` (2 test rhino: onLaunch tak sentuh browser; createAccount alur sinkron penuh → bridge `click:[data-rocat-click="1"]` → verifikasi email/password); assertion `TestBrowserlessScraperTest` diperbarui (click = bridge call). Build `:app:assembleDebug` **SUCCESS**; rhino **59 test, 54 hijau** (5 gagal = pre-existing data script hilang); fetch/RoCatDOM tak tersentuh. **(30.6) [v2 — Debug tap tidak berfungsi]**: akar = (1) koordinat DOM **CSS-px ≠ view-px** — `getBoundingClientRect` dalam CSS-px, `MotionEvent` dalam px view → wajib `scaleX=viewWidth/window.innerWidth` (poll innerWidth sampai stabil, clamp 0.1..10); tanpa ini tap meleset di layar hi-dpi/`device-width`/halaman tanpa viewport-meta (980px); (2) view headless tidak punya **fokus/window-focus** → `prepareForInteraction` (`isFocusable`+`requestFocus`+`requestFocusFromTouch`+`onWindowFocusChanged(true)`); (3) renderer belum menyerap ukuran 1366×768 pasca-`measure` → kick compositor (`draw` scratch bitmap) + `TAP_SETTLE_MS=120` sebelum DOWN + `TAP_GAP_MS=80`. `capcut_test.js` v9 + `CapCutNativeClickTest` dirombak (invoke `createAccount`, FakeBrowser branch `data-rocat-click`/`offsetParent`/`otp`). Docs §7.2a diperbarui. Build `:app:assembleDebug` SUCCESS; rhino 59 test 54 hijau (5 gagal pre-existing).
- **Tahap 29 SELESAI** (2026-08-17): Browserless/Puppeteer-like API Engine & Dokumentasi. **(29.1)** Arsitektur headless WebView diverifikasi lengkap: `HeadlessWebViewManager` (lazy `ensureWebView`, main-thread `Handler`+`CountDownLatch` block script thread, teardown aman di `ScriptCanvasViewModel.onCleared`, `WebViewUtil.setDefaultSettings` full-JS). **(29.2)** `ScriptBrowserBridge` +`scrollTo(x,y)`/`scrollBottom()` (default no-op backward-compat); `RoCatPageBridge` +`put scrollTo/scrollBottom`; `HeadlessWebViewManager` implementasi via `window.scrollTo` / `documentElement.scrollHeight`; `RoCatBrowserBridge` override `runCatching`. **(29.2 polyfill)** `RoCatBrowserWrapper` +`Page.prototype.type(sel,text,delay)` +`scrollTo`/`scrollBottom` + **global `page`** baru (facade Puppeteer-like 22 method → singleton `RoCatBrowser.getInstance().page()`; hanya aktif bila browser bridge ada — `typeof page==="undefined"` di eksekusi polos). **(29.4)** `DOCS_SCRIPTING.md` Bab **7. Browserless / Headless WebView API**: dual-mode statis/interaktif, eksekusi sinkron anti-crash, tabel method `page`, `goto`/`locator`/`evaluate`/`screenshot`, pola lazy-load `scrollBottom`, boilerplate `page.goto`+`RoCatDOM.parse`+`RoCatUI.addImage`; globals & Lampiran diperbarui. **(29.5)** `test_browserless.js` baru (runDemo browserless + runStatic fetch/RoCatDOM) + `TestBrowserlessScraperTest.kt` (3 test rhino drive skrip asli: onLaunch tak sentuh browser, runDemo sinkron penuh + parse `Budi` via RoCatDOM + screenshot + close, runStatic tanpa WebView) + `RoCatBrowserAutomationTest` +3 test (`page` global gating, facade, scroll forward). Build `:app:assembleDebug` SUCCESS; rhino **57 test, 52 hijau** (5 gagal = pre-existing data script `scrape_anichin.js`/`fixed_testscrape.js` hilang).
- **Tahap 28 SELESAI** (2026-08-17): Refactor Mesin JavaScript WebView Berbasis Referensi sweb-master. **(28.1)** Analisis `sweb-master/app/src/main/java/landau/sweb/MainActivity.java` — konfigurasi `createWebView()` referensi: `SINGLE_COLUMN`, `allowUniversalAccessFromFileURLs`, `appCacheEnabled`/`LOAD_DEFAULT`, `domStorageEnabled`, zoom, `onReceivedSslError` Proceed/Cancel, `shouldOverrideUrlLoading` `intent://`→`browser_fallback_url`. **(28.2)** `WebViewUtil.setDefaultSettings` +`allowUniversalAccessFromFileURLs=true` +`layoutAlgorithm=SINGLE_COLUMN` (fidelitas sweb; `setAppCacheEnabled` TIDAK bisa disalin — API dihapus di compileSdk 33+, diganti `LOAD_DEFAULT`); `BrowserScreen.kt` +`onReceivedSslError` (AlertDialog `SslErrorHandler.proceed/cancel`, konstanta **`SSL_IDMISMATCH`** — `SSL_HOSTMISMATCH` tak ada), +`shouldOverrideUrlLoading` fallback `intent://`; i18n EN/ID +`insecureConnectionTitle/insecureConnectionMessage/proceed` (`StringKey.cancel` sudah ada; placeholder `%1\$s`/`%2\$s` wajib di-escape di Strings.kt). **(28.3)** Verifikasi emulator (Chrome-113 WebView, API 34): matriks kontrol (sweb penuh / tanpa SINGLE_COLUMN / bare / UA inferred / browser sistem) — halaman CapCut yang sedang berjalan = **redesign dark** (`arco-theme="dark"`, SSR `signup.html` terbukti via curl) dengan **error hydration React #418/#423/#425 + CSP inline-script ditolak** di SETIAP konfigurasi (termasuk browser sistem, yang juga tak paint penuh: 87.6% putih); DOM probe: `scrollHeight:0`, stylesheets 27→59 ter-load, tema/layout = hasil React mount yang gagal; tanpa AndroidRuntime FATAL & tanpa crash renderer → **bukan blank sistem, tapi keterbatasan page/engine-version**; `getInferredUserAgent` di-revert (tak membantu, pecah konsistensi identitas OkHttp/WebView → tetap Chrome/141 bersama). Konfigurasi final = mirror sweb-master + UA bersama. Bukti: `ai_memory/evidence/capcut_signup_tahap28.png` + `webview_js_tahap28.log`. Build SUCCESS + unit test hijau. **Catatan penting bagi tahap berikutnya**: untuk page modern yang gagal paint di engine WebView lama (Chrome 113), langkah efektif berikutnya = upaya memperbarui System WebView emulator (sangat terbatas di CI) ATAU mengejar upgrade API level/WebView provider; jangan habiskan siklus pada settings WebView yang sudah terbukti bukan penyebab.
- **Tahap 27 SELESAI** (2026-08-17): Otomatisasi Emulator & Verifikasi Rendering Web Modern (CapCut). **(27.1) CI/CD** — `.github/workflows/emulator-webview-test.yml` (reactivecircus/android-emulator-runner v2, API 34 x86_64 Pixel 6 headless `swiftshader_indirect`): identitas package disesuaikan ke **`app.rocat.debug`** (debug pakai suffix `.debug`; diverifikasi `aapt dump badging` → launchable `app.rocat.ui.main.MainActivity`), path APK disesuaikan ke output ABI splits `app/build/outputs/apk/debug/app-x86_64-debug.apk` (fallback universal). **(27.2) Monkey→Intent** — `adb shell monkey` acak dihapus; `am start -n app.rocat.debug/app.rocat.ui.main.MainActivity --es app.rocat.EXTRA_URL "https://www.capcut.com/id-id/signup"` + `sleep 20`; **deep-link baru**: `MainActivity` baca `EXTRA_URL`, `RoCatApp(initialUrl)` skip gerbang first-launch storage (browser tak butuh storage), `RoCatAppNav` start di `KEY_BROWSER`, `BrowserViewModel.acceptInitialUrl` (consumed-once) + `navigateTo` (nonce+1), `BrowserScreen(initialUrl)`. **(27.3) Log & screencap** — `adb logcat -d -v time -s WebViewJS:* AndroidRuntime:E chromium:E WebViewFactory:E` + `exec-out screencap -p` → artifact `webview-render-evidence` (`if: always()`). **(27.4) Evaluasi nyata di emulator** — TERVERIFIKASI TERENDER (bukan blank): logcat `WebViewJS: page title: Daftar - CapCut` (SPA hydrate, hanya warning preload benign, tanpa error JS/AndroidRuntime/chromium); screenshot dianalisis Pillow = **14.0% non-putih** + palet lavender `(243,237,247)` & teks gelap `(73,69,79)` (blank murni ≈100% putih). Perbaikan pencegahan: **`onRenderProcessGone`** recovery (destroy WebView + `webViewEpoch += 1` + **`androidx.compose.runtime.key(webViewEpoch)`** — AndroidView Compose 1.7 TIDAK punya param `key`!), warning `onPageFinished with blank title` (deteksi blank via logcat), `onReceivedTitle` → `page title:` bukti positif, `WebViewUtil` +`mediaPlaybackRequiresUserGesture=false` +`CookieManager.setAcceptCookie(true)`, `RoApp` +`WebView.setWebContentsDebuggingEnabled(BuildConfig.DEBUG)`. **(27.5)** `:app:assembleDebug` SUCCESS (152 task), app & core:common unit test NO-SOURCE; KVM butuh `chmod 666 /dev/kvm` di runner.
- **Tahap 26 SELESAI** (2026-08-17): Resolusi Kritis JavaScript Engine via Dokumentasi Internal. **(26.1) Analisis DOCS_WEBVIEW.md** — instruksi kunci: tambah **Jetpack Webkit** `androidx.webkit:webkit:1.8.0` (BELUM ada di proyek = akar masalah blank page di device WebView lama), JS enabled, `setSupportMultipleWindows(true)` TANPA override `onCreateWindow` (paling aman, blok popup), WebChromeClient fullscreen/permission/console. **(26.2) Fix JS engine** — `libs.versions.toml` + `app/build.gradle.kts`: +`androidx-webkit 1.8.0`; `BrowserScreen.kt` WebChromeClient: `onShowCustomView`/`onHideCustomView` (overlay fullscreen hitam + tombol Close + `BackHandler` LIFO), `onPermissionRequest` grant resources (situs modern tak menggantung), `onConsoleMessage` → **Logcat tag `WebViewJS`** (ERROR/WARNING/DEBUG/INFO + `sourceId:lineNumber: message` — SyntaxError/Cross-Origin/CSP terlihat); `closeFullscreen()` + `Box` root (Column di-re-indent); i18n `closeFullscreen`. **(26.3) Pembersihan** — semua settings Tahap 25 sudah 100% sesuai dokumen, tak ada yang dihapus; `WebViewUtil` KDoc dicatat eksplisit kepatuhan dokumen. **(26.4) Testing** — build + compile SUCCESS; verifikasi render & isi Logcat butuh device. **(26.5)** `:app:assembleDebug` SUCCESS (152 task; kegagalan `packageDebug` pertama transien), `:core:common:testDebugUnitTest` hijau, `:app:testDebugUnitTest` NO-SOURCE; formatter tetap manual (tak ada plugin spotless/ktlint).
- **Tahap 25 SELESAI** (2026-08-17): Perombakan UI/UX In-App Browser, Mode Desktop & Optimalisasi JS Engine. **(25.1) Engine WebView** — `WebViewUtil.setDefaultSettings` + `mixedContentMode = MIXED_CONTENT_COMPATIBILITY_MODE` (JS/DOM storage/database tetap aktif → SPA React/Vue/Angular tak blank); konstanta `DESKTOP_USER_AGENT` (Chrome 141 Win x64); fungsi baru `applyDesktopMode(webView, desktop, mobileUserAgent)`: swap `userAgentString` + `useWideViewPort`/`loadWithOverviewMode` selalu true. **(25.2) Mode Desktop & fitur** — pref `desktopMode` (Boolean) di `SettingsRepository` (persisten); `BrowserViewModel` BARU (pola mihon `StateViewModel` + Injekt): `UiState` (urlInput/currentUrl/canGoBack/Forward/progress/desktopMode/isLoading/`loadNonce`), callback WebViewClient/ChromeClient push nav-state (`NavigationState`), perintah ke WebView lewat `BrowserCommand` sealed (`Reload`/`SetDesktopMode`) via `SharedFlow(extraBufferCapacity=4)` — **WebView tetap milik UI layer** (anti-leak, `DisposableEffect destroy()` tetap); `submitUrl` menormalkan URL (logic `normalizeUrl` dipindah ke VM); pull-to-refresh via **`material3.pulltorefresh.PullToRefreshBox`** + `rememberPullToRefreshState` (material3 1.3.1). **(25.3) UI modern MD3** — address bar **pil** (`RoundedCornerShape(50)` + `Surface(surfaceContainerHigh)` + `BasicTextField`): ikon gembok `Lock`/`LockOpen` (indikator SSL dari scheme https), tombol **clear** (Close), tombol **Go**, Enter fisik + IME; tombol **refresh↔stop**; **overflow 3 titik** `MoreVert` → `ModalBottomSheet` (`ListItem`+`Switch`): Desktop mode, Muat Ulang, Salin Tautan (clipboard+Toast), Buka di Browser Eksternal (`ACTION_VIEW`); **`LinearProgressIndicator` tipis** di bawah address bar tersambung `onProgressChanged`. i18n EN/ID baru: `moreOptions/desktopMode/copyLink/openInBrowser/linkCopied/clearText/reload/secureSite/insecureSite`. Anti double-load start page: factory AndroidView `loadUrl` + set `lastAppliedNonce`, `LaunchedEffect(loadNonce)` skip nonce sama. Build `:app:compileDebugKotlin` + `:app:assembleDebug` SUCCESS (152 task); rhino 46/51 hijau (5 gagal **pre-existing** — data `scrape_anichin.js`/`fixed_testscrape.js` tak ada di checkout, bukan file git-tracked); `gradlew` wrapper jar tidak ada → build pakai Gradle 8.11.1 manual (`/tmp/gradle-8.11.1/`).
- **Tahap 24 SELESAI** (2026-08-16): Media HTTP Headers & Modernisasi UI. **(24.1) Bridge header media** — `ScriptUiBridge.addImage/addVideo/addAudio` + `addGrid` kini menerima `headers: Map<String,String>` (default `{}`); `ScriptUIComponent` Image/Video/Audio/Grid/GridItem bawa `headers`; helper baru `scripting/api/MediaHeaders.kt`: `urlOrigin` (scheme+host+port, hanya http/https, host di-trimStart `.`), `baseUrlFromMatch` (strip `*` glob + `trimEnd('/', ' ')` + retry `://.`→`://` karena `java.net.URI` menolak host ber-titik-awal dari pattern `https://*.site.org/*`), `baseUrlFromMatches`, `effectiveMediaHeaders(url, scriptHeaders, scriptBaseUrl)` — header skrip menang, `Referer` (case-insensitive dinormalisasi) diisi otomatis dari base URL metadata `@match` (fallback origin URL media). Bridge Rhino: `argHeaders(args,i)` (objek JS via `NativeJSON.stringify` atau string JSON, import `kotlinx.serialization.json.contentOrNull`); `RoCatCoreWrapper` render descriptor image/video/audio meneruskan atribut `headers`. **(24.2) Native layer** — `ScriptCanvasViewModel.resolveHeaders`/`scriptBaseUrl`; `addGrid` resolve **per item** (`item.copy(headers = resolveHeaders(headers, item.imageUrl))` — bukan per-grid, fix bug tipe `String?`); `MediaDownloader.download/downloadFromUrl/fetchBytes` + `MediaDownloadComponents.start` terima headers (OkHttp `addHeader`); `ImagePreviewCard` & `GridView` pakai **`coil3.network.NetworkHeaders` + `httpHeaders()`** (Coil 3 TIDAK punya `ImageRequest.Builder.addHeader`!); `RocatVideoPlayer`/`VideoPreviewCard`/`AudioPreviewCard` pakai Media3 `DefaultHttpDataSource.Factory().setDefaultRequestProperties(headers)`; `ScriptCanvasScreen` meneruskan headers ke kartu. **(24.3) UI modernisasi** — `ScriptsScreen`: `SwipeToDismissBox` (EndToStart → delete confirm AlertDialog, StartToEnd → edit, `confirmValueChange` return false agar snap-back), `StatusChip` animasi warna `animateColorAsState` + label `AnimatedContent`, ripple eksplisit `ripple()` + `interactionSource`; `ScriptCanvasScreen`: per-item `Modifier.animateItem()` + `animateContentSize` + hint `AnimatedVisibility(fadeIn/fadeOut)` (anti-flicker clear/redraw); kartu template `JsonLogCard/HtmlPreviewCard/AlertBannerCard/BadgeGroupCard` pakai `ScriptCanvasCard` baru (RoundedCornerShape 20dp + elevation animasi 2→8dp saat pressed). **(24.4)** `DOCS_SCRIPTING.md` §2.2/2.3/2.5 param `headers` + catatan auto-`Referer` + §2.6 descriptor `headers` + contoh boilerplate §5 (`addVideo(..., headers)` html5player). Validasi: `sh gradlew :scripting:rhino:testDebugUnitTest` **44 test hijau** (recorder `:${headers.toSortedMap()}`, assert `endsWith(":true:true:{}")`, fix `emptyMap<String,String>()` inferensi) + `:app:assembleDebug` SUCCESS.
- **Tahap 23 SELESAI** (2026-08-09): Pembaruan Dokumentasi Scripting & Perbaikan Skrip Scraper. **Dokumentasi** `rocat-app/DOCS_SCRIPTING.md` diperbarui ke gaya Tahap 22/23: contoh lifecycle §1.3 & boilerplate §5 ditulis ulang memakai `RoCat.render([...])` + `RoCat.safeParseJson(payload, {})` + `addAlert` + `addBadgeGroup` + `addJsonLog`; §6 Praktik Terbaik ditambah aturan baru (gaya baru render, wajib safeParseJson, konten `<script>` = CDATA — `text()` Jsoup kosong → baca `innerHtml` + selektor `:containsData`); Lampiran menambahkan `fixed_testscrape.js`. **Perbaikan skrip**: draf `testscrape.txt` (XVideos scraper) dirombak jadi `fixed_testscrape.js` v3.0.0 (root repo) — `JSON.parse` → `RoCat.safeParseJson(payload, {})`; status pesan → `RoCatUI.addAlert(level)`; genre detail → `RoCatUI.addBadgeGroup`; sumber stream → `RoCatUI.addJsonLog`; UI verbose → `RoCat.render([...])`. **Bug akar ditemukan & diperbaiki**: ekstraksi script html5player memakai `textOf(...)`/`script.text` yang di Jsoup selalu `""` (konten `<script>` = CDATA) → kini baca **`innerHtml`** (`extractPlayerScript` pakai `doc.find("script:containsData(html5player)...")` lalu fallback manual); judul kartu strip `[tag]` dua-spasi → normalisasi `\s+`→` `. Validasi: `FixedTestscrapeScraperTest.kt` BARU (2 test, rhino) drive `fixed_testscrape.js` asli dengan HTML canned via interceptor OkHttp — onLaunch→doSearch→openDetail (cover og:image + badge genre)→openVideo (HLS best-variant 720p `addVideo(...,true,true)` + jsonLog debug) + test payload malformed/pencarian kosong tak pernah throw. Build `sh gradlew :scripting:rhino:testDebugUnitTest` SUCCESS (**rhino 33 test hijau** = 31 lama + 2 baru).
- **Tahap 22.2 SELESAI** (2026-08-09): Simplification API + Template Cards Script Canvas. **(22.1)** `RoCatCoreWrapper.kt` baru: `RhinoScriptEngine` meng-inject global `RoCat` otomatis ke setiap scope SEBELUM kode user (`cx.evaluateString(scope, RO_CAT_CORE_WRAPPER_JS, "RoCatCore.js", ...)`). `RoCat.render(items)` — gambar seluruh kanvas dari satu descriptor (array/objek: `clear/input/button/image/video/audio/json/html/alert/badges/grid/log`, toleran `pick()`/`pickBool()`), `RoCat.safeParseJson(str,fallback)` (tak pernah throw), `RoCat.fetchJson(url,options)` (parse via `safeParseJson(res.body)` supaya JSON invalid jadi `null` bukan ParseException). Wrapper ES5 Rhino-1.7.15-safe, semua dispatch `try/catch`. Fix `fetch().json()`: Rhino `JsonParser` melempar *checked* `ParseException` yang tak bisa di-catch JS → helper `throwJsError(cx,scope,msg)` baru (`cx.newObject(scope,"Error",...)` + `JavaScriptException`) → script bisa `try/catch` JSON invalid. **(22.2)** 5 komponen UI baru di `app/rocat/ui/components/`: `JsonLogCard` (JSON pretty-print + tombol Copy + Toast, expandable), `HtmlPreviewCard` (`android.text.Html.fromHtml` → `AnnotatedString`, link pakai `LinkAnnotation.Clickable`+`TextLinkStyles`, bukan `ClickableText` deprecated — inline, tanpa WebView), `AudioPreviewCard` (Media3 ExoPlayer inline Play/Pause + seek bar + tombol download via SAF `MediaDownloader`/`folder`), `AlertBannerCard` (`AlertType.from` fallback info untuk level warning/error/success/info, ikon+warna), `BadgeGroupCard` (FlowRow chip). `ScriptUIComponent` +5 tipe: `JsonLog/HtmlPreview/Audio/Alert/BadgeGroup`. `ScriptUiBridge` +5 method default no-op (`addJsonLog/addHtmlPreview/addAudio/addAlert/addBadgeGroup`) → `ScriptCanvasViewModel` implementasi tolerant (`parseBadgeGroup`), `ScriptCanvasScreen` render. Bridge Rhino `RoCatUiBridge` +5 `put` + helper `argJson(args,i)` (normalisasi objek/array via `NativeJSON.stringify`). i18n EN/ID baru (`copyJson/jsonCopied/play/pause/downloadAudio/audioSaved`). `scrape_anichin.js` v3 + `CANVAS_EXAMPLE_SCRIPT` v2 ditulis ulang memakai `RoCat.render` + `addAlert`/`addBadgeGroup` (genre chip dari `parseDetailBadges`). Test baru `RoCatScriptTemplateTest.kt` (9 test: render list/single/toleran, safeParseJson, fetchJson, badge JS array, jsonLog objek/string, alert default & unknown, html+audio). Build `:app:assembleDebug` + `./gradlew test` SUCCESS (**rhino 31 test hijau** = 22 lama + 9 baru).
- **Tahap 20 SELESAI** (2026-08-09): Native Base64 Bridge, Custom User-Agent & Custom DNS (Network Settings). **(20.1)** Bridge `ScriptUiBridge.decodeBase64(input)` baru (default `java.util.Base64` aman JVM test & API26+, return `""` bila gagal) → `RoCatUI.decodeBase64` (Rhino `RoCatUiBridge` + helper `runSafeValue`) → `ScriptCanvasViewModel` override **native Android** (`android.util.Base64.decode(padded, Base64.DEFAULT)` → UTF-8); `scrape_anichin.js` `decodeBase64()` pakai `RoCatUI.decodeBase64(s)` (fallback JS `b64Decode` tetap ada). **(20.2)** UI Pengaturan "Jaringan" di `SettingsScreen`: TextField custom User-Agent (kosong = default "Chrome 141") + dropdown DNS DoH (`DnsMode`: SYSTEM/CLOUDFLARE/GOOGLE/QUAD9/CUSTOM via `DnsModeRow`) + field URL DoH saat CUSTOM; i18n EN/ID baru (`network/userAgent/dnsSelection/...`). **(20.3)** `core/common/network` baru: `DnsMode` + `DnsProviders.dohUrl()` (Cloudflare `cloudflare-dns.com/dns-query`, Google `dns.google/dns-query`, Quad9 `dns.quad9.net/dns-query`) + `DoHResolver` (implementasi `okhttp3.Dns` JSON GET `?name&type=A`, `Answer[].type` 1=A / 28=AAAA, fallback `Dns.SYSTEM`); `NetworkHelper` dirombak: baca `userAgentProvider()`/`dnsConfigProvider()` (dari `SettingsRepository`), client dibangun **lazily** + fingerprint `"$ua|$mode|$custom"` (tidak rebuild per keystroke), `client` diubah jadi fungsi `client()`, `DoHResolver` dipasang ke `.dns(...)`; `SettingsRepository` pref `user_agent`/`dns_mode`/`custom_dns_url`; `AppModule` buat SettingsRepository **sebelum** NetworkHelper; `ScriptManager.engine()/environment()/createEnvironment()` + refresh engine bila fingerprint jaringan berubah; `SettingsViewModel` `settingsState = combine(language, mutableState)` + setter; UI via `viewModel::setUserAgent/setDnsMode/setCustomDnsUrl`. Build `:app:assembleDebug` + `:app:assemblePreview` (R8) + `./gradlew test` SUCCESS (rhino 22 test hijau, termasuk assert `decodeBase64` lewat bridge native).
- **Tahap 19 SELESAI** (2026-08-09): Anichin Scraper End-to-End. Skrip canvas produksi `scrape_anichin.js` (repo root, 320 baris) memakai `RoCatDOM` + `RoCatUI.addGrid/addImage/addVideo`: **Home** (rilisan terbaru `.bixbox article .bsx`) → **Pencarian** (`/page/1?s=`) → **Detail** (cover `.thumb img` → `addImage`, sinopsis, episode `.eplister ul li a`) → **Episode HLS** (decode base64 `select.mirror option`, ekstrak iframe `anichin.stream`, bangun master `/hls/<id>.m3u8`, `pickBestVariant` pilih varian VALID tertinggi — buang STREAM-INF tanpa URI (1080p malformed) → `RoCatUI.addVideo(url, title, isStreamHls=true, allowDownload=true)`). Test `AnichinScraperTest.kt` (rhino) menjalankan skrip asli dengan HTML canned via **interceptor OkHttp** (karena `fetch()` skrip TIDAK lewat `DefaultScriptEnvironment.fetchImpl`). Build `cd rocat-app && sh gradlew :scripting:rhino:testDebugUnitTest` SUCCESS (22 test hijau).
- **Tahap 18 SELESAI** (2026-08-09): Media Template, Downloader (Image & Video) & HLS Streaming (ExoPlayer/Media3). **(18.1)** `ScriptUIComponent.Image(url,title,allowDownload)` menggantikan `Thumbnail`; `ImagePreviewCard` (Coil + tombol download pojok) → unduh via `MediaDownloader` (OkHttp `NetworkHelper.client` membawa cookie jar + UA browser) → `StorageManager.saveFileToScrapeFolder(folder,fileName,mime,bytes)` → tertulis nyata di `[Utama]/Scrapes/[id]`, status Idle/Downloading(progress)/Done/Failed + Toast. **(18.2)** `ScriptUIComponent.Video(url,title,isStreamHls,allowDownload)`; `VideoPreviewCard` = placeholder 16:9 + judul + tombol **Play Inline** (toggle `RocatVideoPlayer`) & **Download Video** (async `Dispatchers.IO`, progress, SAF). **(18.3)** Media3 `media3-exoplayer:1.4.1` + `media3-exoplayer-hls` + `media3-ui`; `RocatVideoPlayer` (AndroidView→`PlayerView`, `ExoPlayer.Builder`, `HlsMediaSource` untuk `.m3u8`/`hls://`, `ProgressiveMediaSource` lainnya); **Full screen**: `Dialog(usePlatformDefaultWidth=false)` + `DialogWindowProvider` + landscape + sembunyikan system bars (immersive), keluar → portrait + bars muncul, player SAMA di-share (inline di-detach); `MainActivity` + `android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"` supaya rotasi tak recreate activity. **(18.4)** Bridge `RoCatUI.addImage(url,title,allowDownload)` & `addVideo(url,title,isStreamHls,allowDownload)` (ScriptUiBridge + Rhino `RoCatUiBridge` + helper `argBoolean`); `ScriptCanvasScreen` render `Image`→ImagePreviewCard, `Video`→VideoPreviewCard; string i18n baru (`download`,`playInline`,`closePlayer`,`imageSaved`, dll). Build `./gradlew :app:assembleDebug` + `:app:assemblePreview` (R8) + `./gradlew test` (rhino 20 hijau, termasuk test `addImage`/`addVideo`) SUCCESS.
- **Tahap 17 SELESAI** (2026-08-09): First-Launch Fix, Storage Skrip Fisik & UI Kategori Collapsible. **(17.1)** Bug "stuck di StorageSetupScreen": `SettingsRepository.storageUri` → `StateFlow<Uri?>` (setter `setStorageUri()`), `StorageManager.isConfigured` → `StateFlow<Boolean>` (`map { it != null }` + `stateIn Eagerly`), `RoCatApp()` pakai `collectAsState()` → begitu folder dipilih, UI langsung pindah ke `RoCatAppNav()` tanpa restart (SettingsViewModel disesuaikan ke `.value`). **(17.2)** Skrip impor kini tersimpan sebagai file fisik: `StorageManager.createScriptFolder(id)` → `[Utama]/Scripts/[id]/`, `saveFileToScriptFolder(scriptId, fileName, content)` (DocumentFile.createFile + `openOutputStream`, mime `application/javascript`), `deleteScriptFolder(id)`; `ImportScriptViewModel` memanggil `persistToStorage()` (best-effort, tidak memblokir import) setelah import URL/paste; `ScriptsViewModel.delete()` ikut menghapus folder fisik. **(17.3)** Long-press: `Modifier.combinedClickable` (`ExperimentalFoundationApi`) di kartu skrip → `ModalBottomSheet` berisi **Edit** (→ `Screen.Detail`, route baru `onEditScript` di RoCatNav) & **Hapus** (konfirmasi `AlertDialog` → `viewModel.delete`). **(17.4)** Metadata kategori: `ScriptMetadataParser` baca `@category` (fallback `@group`) → field `category` baru di `ScriptMetadata` + `Script` (default `""` — backward-compatible decode JSON lama) + diisi `ImportScript`/`UpsertScript`; `ScriptsScreen` grup `state.scripts.groupBy { it.category }`, header kategori tebal + count + ikon ExpandLess/ExpandMore, status expand/collapse per-kategori via `remember { mutableStateMapOf<String, Boolean>() }`, kategori kosong → label i18n "Others"/"Lainnya" (`StringKey.othersCategory`, `StringKey.edit` baru). Test parser kategori baru (6/6 domain). Build `./gradlew :app:assembleDebug` + `./gradlew test` SUCCESS.

diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

apply patch
patch: completed
/home/runner/work/rocat-app/rocat-app/ai_memory/00_INDEX.md
/home/runner/work/rocat-app/rocat-app/ai_memory/task_20260826_1200_Fase_42.md
diff --git a/ai_memory/00_INDEX.md b/ai_memory/00_INDEX.md
index e4c67921d20e1bd36f0d1b81b24fd8a6cc733c81..a38e2a42ac850d2274ad89280efc98bd359233ed
--- a/ai_memory/00_INDEX.md
+++ b/ai_memory/00_INDEX.md
@@ -1,5 +1,7 @@
 # MEMORY INDEX — rocat-app
 
+- **Fase 42 (2026-08-26)**: [Network interception & XHR capture](task_20260826_1200_Fase_42.md) — monkey-patch fetch/XHR pada headless WebView, bridge response thread-safe, API `page.waitForResponse`, dan pembatasan payload/memori; build debug sukses.
+
 - **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.
 
 - **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
diff --git a/ai_memory/task_20260826_1200_Fase_42.md b/ai_memory/task_20260826_1200_Fase_42.md
new file mode 100644
index 0000000000000000000000000000000000000000..289198747bb4680977b92562289e112082abe08c
--- /dev/null
+++ b/ai_memory/task_20260826_1200_Fase_42.md
@@ -0,0 +1,8 @@
+# Fase 42 (2026-08-26): Network interception & XHR capture
+
+- Menambahkan monkey-patch JavaScript untuk `window.fetch` dan `XMLHttpRequest` pada headless WebView.
+- Response body dikirim melalui `RoCatBrowserBridge.onNetworkResponse(url, body)` dan disimpan thread-safe di `HeadlessWebViewManager`.
+- Payload dibatasi 1 MiB dan koleksi dibatasi 64 URL untuk mencegah pertumbuhan memori tanpa batas; hanya response text/JSON yang diteruskan.
+- Menambahkan primitive bridge `interceptedResponse()` dan API browser `page.waitForResponse(urlPattern, timeout)` dengan polling sinkron dan timeout.
+- Patch menjaga fungsi asli fetch/XHR (fetch mengembalikan Promise asli; XHR open/send didelegasikan).
+- Verifikasi: `bash ./gradlew assembleDebug` — BUILD SUCCESSFUL.
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

diff --git a/ai_memory/00_INDEX.md b/ai_memory/00_INDEX.md
index e4c67921d20e1bd36f0d1b81b24fd8a6cc733c81..a38e2a42ac850d2274ad89280efc98bd359233ed
--- a/ai_memory/00_INDEX.md
+++ b/ai_memory/00_INDEX.md
@@ -1,5 +1,7 @@
 # MEMORY INDEX — rocat-app
 
+- **Fase 42 (2026-08-26)**: [Network interception & XHR capture](task_20260826_1200_Fase_42.md) — monkey-patch fetch/XHR pada headless WebView, bridge response thread-safe, API `page.waitForResponse`, dan pembatasan payload/memori; build debug sukses.
+
 - **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.
 
 - **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
diff --git a/ai_memory/task_20260826_1200_Fase_42.md b/ai_memory/task_20260826_1200_Fase_42.md
new file mode 100644
index 0000000000000000000000000000000000000000..289198747bb4680977b92562289e112082abe08c
--- /dev/null
+++ b/ai_memory/task_20260826_1200_Fase_42.md
@@ -0,0 +1,8 @@
+# Fase 42 (2026-08-26): Network interception & XHR capture
+
+- Menambahkan monkey-patch JavaScript untuk `window.fetch` dan `XMLHttpRequest` pada headless WebView.
+- Response body dikirim melalui `RoCatBrowserBridge.onNetworkResponse(url, body)` dan disimpan thread-safe di `HeadlessWebViewManager`.
+- Payload dibatasi 1 MiB dan koleksi dibatasi 64 URL untuk mencegah pertumbuhan memori tanpa batas; hanya response text/JSON yang diteruskan.
+- Menambahkan primitive bridge `interceptedResponse()` dan API browser `page.waitForResponse(urlPattern, timeout)` dengan polling sinkron dan timeout.
+- Patch menjaga fungsi asli fetch/XHR (fetch mengembalikan Promise asli; XHR open/send didelegasikan).
+- Verifikasi: `bash ./gradlew assembleDebug` — BUILD SUCCESSFUL.
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

exec
/bin/bash -lc 'git diff --stat; git status --short' in /home/runner/work/rocat-app/rocat-app
 succeeded in 0ms:
 ai-response-recap.txt                              | 6151 +++++++++++++-------
 ai_memory/00_INDEX.md                              |    2 +
 .../app/rocat/scripting/AppScriptBrowserBridge.kt  |    3 +
 .../app/rocat/scripting/HeadlessWebViewManager.kt  |   79 +
 .../java/app/rocat/scripting/RoCatBrowserBridge.kt |    5 +-
 .../app/rocat/scripting/api/ScriptBrowserBridge.kt |    3 +
 .../rocat/scripting/rhino/RoCatBrowserWrapper.kt   |   16 +-
 .../app/rocat/scripting/rhino/RoCatPageBridge.kt   |    3 +
 8 files changed, 4010 insertions(+), 2252 deletions(-)
 M ai-response-recap.txt
 M ai_memory/00_INDEX.md
 M app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
 M app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
 M app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
 M scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
 M scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
 M scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
?? ai_memory/task_20260826_1200_Fase_42.md

diff --git a/ai_memory/00_INDEX.md b/ai_memory/00_INDEX.md
index e4c67921d20e1bd36f0d1b81b24fd8a6cc733c81..a38e2a42ac850d2274ad89280efc98bd359233ed
--- a/ai_memory/00_INDEX.md
+++ b/ai_memory/00_INDEX.md
@@ -1,5 +1,7 @@
 # MEMORY INDEX — rocat-app
 
+- **Fase 42 (2026-08-26)**: [Network interception & XHR capture](task_20260826_1200_Fase_42.md) — monkey-patch fetch/XHR pada headless WebView, bridge response thread-safe, API `page.waitForResponse`, dan pembatasan payload/memori; build debug sukses.
+
 - **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.
 
 - **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
diff --git a/ai_memory/task_20260826_1200_Fase_42.md b/ai_memory/task_20260826_1200_Fase_42.md
new file mode 100644
index 0000000000000000000000000000000000000000..289198747bb4680977b92562289e112082abe08c
--- /dev/null
+++ b/ai_memory/task_20260826_1200_Fase_42.md
@@ -0,0 +1,8 @@
+# Fase 42 (2026-08-26): Network interception & XHR capture
+
+- Menambahkan monkey-patch JavaScript untuk `window.fetch` dan `XMLHttpRequest` pada headless WebView.
+- Response body dikirim melalui `RoCatBrowserBridge.onNetworkResponse(url, body)` dan disimpan thread-safe di `HeadlessWebViewManager`.
+- Payload dibatasi 1 MiB dan koleksi dibatasi 64 URL untuk mencegah pertumbuhan memori tanpa batas; hanya response text/JSON yang diteruskan.
+- Menambahkan primitive bridge `interceptedResponse()` dan API browser `page.waitForResponse(urlPattern, timeout)` dengan polling sinkron dan timeout.
+- Patch menjaga fungsi asli fetch/XHR (fetch mengembalikan Promise asli; XHR open/send didelegasikan).
+- Verifikasi: `bash ./gradlew assembleDebug` — BUILD SUCCESSFUL.
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

codex
Perintah dalam `prompt.md` telah dieksekusi.

Perubahan utama:

- Menambahkan interceptor JavaScript untuk `fetch` dan `XMLHttpRequest`.
- Menambahkan bridge native `onNetworkResponse(url, body)`.
- Menyimpan response secara thread-safe dengan batas:
  - Maksimal 1 MiB per body.
  - Maksimal 64 response tersimpan.
- Menambahkan API:
  - `RoCatPage.interceptedResponse(urlPattern)`
  - `page.waitForResponse(urlPattern, timeout)`
- Menjaga fungsi asli `fetch` dan XHR tetap berjalan.
- Membuat dokumentasi:
  - [task_20260826_1200_Fase_42.md](/home/runner/work/rocat-app/rocat-app/ai_memory/task_20260826_1200_Fase_42.md)
  - Memperbarui [00_INDEX.md](/home/runner/work/rocat-app/rocat-app/ai_memory/00_INDEX.md)

Verifikasi berhasil:

```text
bash ./gradlew assembleDebug
BUILD SUCCESSFUL
```
diff --git a/ai_memory/00_INDEX.md b/ai_memory/00_INDEX.md
index e4c67921d20e1bd36f0d1b81b24fd8a6cc733c81..a38e2a42ac850d2274ad89280efc98bd359233ed
--- a/ai_memory/00_INDEX.md
+++ b/ai_memory/00_INDEX.md
@@ -1,5 +1,7 @@
 # MEMORY INDEX — rocat-app
 
+- **Fase 42 (2026-08-26)**: [Network interception & XHR capture](task_20260826_1200_Fase_42.md) — monkey-patch fetch/XHR pada headless WebView, bridge response thread-safe, API `page.waitForResponse`, dan pembatasan payload/memori; build debug sukses.
+
 - **Fase 41 (2026-08-26)**: [Aspect ratio video & immersive full-screen player](task_20260826_0000_Fase_41.md) — rasio video dinamis, orientasi fullscreen adaptif, dan system bars dialog disembunyikan secara immersive.
 
 - **Fase 40 (2026-08-25)**: [Decode gambar panjang & memori reader](task_20260825_0000_Fase_40.md) — software bitmap + decode sesuai constraint Compose; cache Coil dibatasi 25% RAM/100 MiB; build debug sukses.
diff --git a/ai_memory/task_20260826_1200_Fase_42.md b/ai_memory/task_20260826_1200_Fase_42.md
new file mode 100644
index 0000000000000000000000000000000000000000..289198747bb4680977b92562289e112082abe08c
--- /dev/null
+++ b/ai_memory/task_20260826_1200_Fase_42.md
@@ -0,0 +1,8 @@
+# Fase 42 (2026-08-26): Network interception & XHR capture
+
+- Menambahkan monkey-patch JavaScript untuk `window.fetch` dan `XMLHttpRequest` pada headless WebView.
+- Response body dikirim melalui `RoCatBrowserBridge.onNetworkResponse(url, body)` dan disimpan thread-safe di `HeadlessWebViewManager`.
+- Payload dibatasi 1 MiB dan koleksi dibatasi 64 URL untuk mencegah pertumbuhan memori tanpa batas; hanya response text/JSON yang diteruskan.
+- Menambahkan primitive bridge `interceptedResponse()` dan API browser `page.waitForResponse(urlPattern, timeout)` dengan polling sinkron dan timeout.
+- Patch menjaga fungsi asli fetch/XHR (fetch mengembalikan Promise asli; XHR open/send didelegasikan).
+- Verifikasi: `bash ./gradlew assembleDebug` — BUILD SUCCESSFUL.
diff --git a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
index 7f2d14f360cfa1b0cf2b574ba1b22313887a4768..69e92281a0a5ca9506a4f390a45cb2fcf5c1c817
--- a/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/AppScriptBrowserBridge.kt
@@ -25,6 +25,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
diff --git a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
index 9cd146c31925e93af0354f4571f2066b969b8068..8fc96655608d93076cbcc5dc87a57723119c935c
--- a/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
+++ b/app/src/main/java/app/rocat/scripting/HeadlessWebViewManager.kt
@@ -12,6 +12,7 @@
 import android.view.MotionEvent
 import android.view.View
 import android.webkit.CookieManager
+import android.webkit.JavascriptInterface
 import android.webkit.WebView
 import android.webkit.WebViewClient
 import app.rocat.core.common.util.WebViewUtil
@@ -21,6 +22,8 @@
 import java.io.File
 import java.io.FileOutputStream
 import java.util.concurrent.CountDownLatch
+import java.util.concurrent.ConcurrentHashMap
+import java.util.concurrent.ConcurrentLinkedQueue
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicBoolean
 import java.util.concurrent.atomic.AtomicReference
@@ -45,6 +48,20 @@
     @Volatile
     private var webView: WebView? = null
 
+    private val interceptedResponses = ConcurrentHashMap<String, String>()
+    private val interceptedUrls = ConcurrentLinkedQueue<String>()
+
+    private inner class NetworkResponseBridge {
+        @JavascriptInterface
+        fun onNetworkResponse(url: String?, body: String?) {
+            if (url.isNullOrBlank() || body == null || body.toByteArray(Charsets.UTF_8).size > MAX_RESPONSE_BYTES) return
+            if (interceptedResponses.put(url, body) == null) interceptedUrls.offer(url)
+            while (interceptedResponses.size > MAX_RESPONSE_ENTRIES) {
+                interceptedUrls.poll()?.let(interceptedResponses::remove) ?: break
+            }
+        }
+    }
+
     private fun onMain(block: () -> Unit) {
         if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
     }
@@ -61,6 +78,7 @@
                 val wv = WebView(appContext)
                 WebViewUtil.setDefaultSettings(wv)
                 wv.setBackgroundColor(Color.TRANSPARENT)
+                wv.addJavascriptInterface(NetworkResponseBridge(), NETWORK_BRIDGE_NAME)
                 wv.webViewClient = WebViewClient()
                 webView = wv
                 ref.set(wv)
@@ -79,10 +97,16 @@
      */
     fun open(url: String, timeoutMs: Long): Boolean {
         val wv = ensureWebView() ?: return false
+        interceptedResponses.clear()
+        interceptedUrls.clear()
         val latch = CountDownLatch(1)
         onMain {
             try {
                 wv.webViewClient = object : WebViewClient() {
+                    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
+                        view?.evaluateJavascript(NETWORK_INTERCEPTOR_JS, null)
+                    }
+
                     override fun onPageFinished(view: WebView?, url: String?) = latch.countDown()
                 }
                 wv.loadUrl(url)
@@ -413,6 +437,14 @@
         }
     }
 
+    /** Returns the newest captured response whose URL contains [urlPattern]. */
+    fun interceptedResponse(urlPattern: String): String {
+        if (urlPattern.isBlank()) return ""
+        return interceptedResponses.entries
+            .lastOrNull { (url, _) -> url.contains(urlPattern, ignoreCase = true) }
+            ?.value.orEmpty()
+    }
+
     /** Runs [script] in the live page and returns the raw JSON-encoded result. */
     fun evaluate(script: String): String =
         evaluateJs(script, DEFAULT_EVAL_TIMEOUT_MS) ?: "null"
@@ -765,6 +797,53 @@
         const val VIEWPORT_SETTLE_TRIES = 3
         const val MIN_TAP_SCALE = 0.1f
         const val MAX_TAP_SCALE = 10f
+        const val MAX_RESPONSE_BYTES = 1024 * 1024
+        const val MAX_RESPONSE_ENTRIES = 64
+        const val NETWORK_BRIDGE_NAME = "RoCatBrowserBridge"
+
+        val NETWORK_INTERCEPTOR_JS = """
+            (function() {
+                if (window.__rocatNetworkInterceptorInstalled) return;
+                window.__rocatNetworkInterceptorInstalled = true;
+                var bridge = window.RoCatBrowserBridge;
+                function report(url, body) {
+                    try {
+                        if (!bridge || !bridge.onNetworkResponse || typeof body !== 'string') return;
+                        if (new Blob([body]).size > 1048576) return;
+                        bridge.onNetworkResponse(String(url || ''), body);
+                    } catch (e) {}
+                }
+                if (window.fetch) {
+                    var originalFetch = window.fetch;
+                    window.fetch = function() {
+                        var requestUrl = arguments[0] && arguments[0].url ? arguments[0].url : arguments[0];
+                        return originalFetch.apply(this, arguments).then(function(response) {
+                            try {
+                                response.clone().text().then(function(body) { report(response.url || requestUrl, body); }, function() {});
+                            } catch (e) {}
+                            return response;
+                        });
+                    };
+                }
+                var originalOpen = XMLHttpRequest.prototype.open;
+                var originalSend = XMLHttpRequest.prototype.send;
+                XMLHttpRequest.prototype.open = function(method, url) {
+                    this.__rocatRequestUrl = url;
+                    return originalOpen.apply(this, arguments);
+                };
+                XMLHttpRequest.prototype.send = function() {
+                    this.addEventListener('load', function() {
+                        try {
+                            if (!this.responseType || this.responseType === 'text' || this.responseType === 'json') {
+                                var body = this.responseType === 'json' ? JSON.stringify(this.response) : this.responseText;
+                                report(this.responseURL || this.__rocatRequestUrl, body);
+                            }
+                        } catch (e) {}
+                    });
+                    return originalSend.apply(this, arguments);
+                };
+            })();
+        """.trimIndent()
 
         fun enableWholeDocumentDrawing() {
             if (!wholeDocumentDrawingEnabled.compareAndSet(false, true)) return
diff --git a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
index 0e4f6105cec9d7fc27a387045685f75d251649a8..779e9d818d5633c5f708bae949b3c5f775f8c019
--- a/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
+++ b/app/src/main/java/app/rocat/scripting/RoCatBrowserBridge.kt
@@ -39,6 +39,9 @@
     override fun waitForSelector(selector: String, timeoutMs: Long): Boolean =
         runCatching { manager.waitForSelector(selector, timeoutMs) }.getOrDefault(false)
 
+    override fun interceptedResponse(urlPattern: String): String =
+        runCatching { manager.interceptedResponse(urlPattern) }.getOrDefault("")
+
     override fun evaluate(script: String): String =
         runCatching { manager.evaluate(script) }.getOrElse { "null" }
 
@@ -94,4 +97,4 @@
 
     override fun clearCookies(): Boolean =
         runCatching { manager.clearCookies() }.getOrDefault(false)
-}
\ No newline at end of file
+}
diff --git a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
index 2204a18c95096abe19d6636171bb5499f14ca325..c7c92c9c5f01ab4dda74fb9870573c6eb74865d6
--- a/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
+++ b/scripting/api/src/main/java/app/rocat/scripting/api/ScriptBrowserBridge.kt
@@ -60,6 +60,9 @@
      */
     fun waitForSelector(selector: String, timeoutMs: Long = DEFAULT_TIMEOUT_MS): Boolean
 
+    /** Returns a captured fetch/XHR response whose URL contains [urlPattern], or `""`. */
+    fun interceptedResponse(urlPattern: String): String = ""
+
     /**
      * Runs [script] inside the page's own JavaScript context and returns the value the
      * browser produced as a **JSON-encoded string** (WebView's `evaluateJavascript`
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
index 0f335ea1f6df9dae698d5e137dddbb5cb32b758f..7b9aecb4394f4527915a30664141c9f023d36bbb
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatBrowserWrapper.kt
@@ -317,6 +317,19 @@
         return this.locator(selector).waitFor(timeout);
     };
 
+    Page.prototype.waitForResponse = function (urlPattern, timeout) {
+        timeout = (typeof timeout === "number") ? timeout : _defaultTimeout;
+        var pattern = String(urlPattern || "");
+        var deadline = Date.now() + timeout;
+        while (true) {
+            var body = (hasPage() && RoCatPage.interceptedResponse) ?
+                RoCatPage.interceptedResponse(pattern) : "";
+            if (body !== null && body !== undefined && body !== "") return String(body);
+            if (Date.now() >= deadline) throw new Error("Timeout waiting for response: " + pattern);
+            busyWait(100);
+        }
+    };
+
     Page.prototype.url = function () {
         var live = hasPage() ? RoCatPage.url() : "";
         return live || this._url;
@@ -441,6 +454,7 @@
     return {
         goto: function (url, options) { return current().goto(url, options); },
         waitForSelector: function (selector, timeout) { return current().waitForSelector(selector, timeout); },
+        waitForResponse: function (urlPattern, timeout) { return current().waitForResponse(urlPattern, timeout); },
         waitForTimeout: function (ms) { return current().waitForTimeout(ms); },
         click: function (selector) { return current().click(selector); },
         type: function (selector, text, delay) { return current().type(selector, text, delay); },
@@ -463,4 +477,4 @@
         close: function () { return current().close(); }
     };
 })();
-"""
\ No newline at end of file
+"""
diff --git a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
index f65dd0cbfcd8ed80a9ca2d196095acf59f5f4adc..2fec8b120fcf764206a08f6a35a08bfbb587a4ae
--- a/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
+++ b/scripting/rhino/src/main/java/app/rocat/scripting/rhino/RoCatPageBridge.kt
@@ -53,6 +53,9 @@
         put("waitForSelector", this, PageFn { args ->
             browser.waitForSelector(pageArgString(args, 0), pageArgLong(args, 1, ScriptBrowserBridge.DEFAULT_TIMEOUT_MS))
         })
+        put("interceptedResponse", this, PageFn { args ->
+            browser.interceptedResponse(pageArgString(args, 0))
+        })
         put("evaluate", this, PageFn { args ->
             evaluateResult(browser.evaluate(pageArgString(args, 0)))
         })

tokens used
358,962
Perintah dalam `prompt.md` telah dieksekusi.

Perubahan utama:

- Menambahkan interceptor JavaScript untuk `fetch` dan `XMLHttpRequest`.
- Menambahkan bridge native `onNetworkResponse(url, body)`.
- Menyimpan response secara thread-safe dengan batas:
  - Maksimal 1 MiB per body.
  - Maksimal 64 response tersimpan.
- Menambahkan API:
  - `RoCatPage.interceptedResponse(urlPattern)`
  - `page.waitForResponse(urlPattern, timeout)`
- Menjaga fungsi asli `fetch` dan XHR tetap berjalan.
- Membuat dokumentasi:
  - [task_20260826_1200_Fase_42.md](/home/runner/work/rocat-app/rocat-app/ai_memory/task_20260826_1200_Fase_42.md)
  - Memperbarui [00_INDEX.md](/home/runner/work/rocat-app/rocat-app/ai_memory/00_INDEX.md)

Verifikasi berhasil:

```text
bash ./gradlew assembleDebug
BUILD SUCCESSFUL
```
